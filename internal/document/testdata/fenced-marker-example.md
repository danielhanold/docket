---
id: 18
slug: fenced-marker-example
status: proposed
---

# Fenced Marker Example

The renderer writes a marker pair that looks like this:

```markdown
<!-- docket:artifacts:start (generated — do not hand-edit) -->
- an artifact row
<!-- docket:artifacts:end -->
```

Only the fenced text above is marker-shaped; the document has no managed block.
