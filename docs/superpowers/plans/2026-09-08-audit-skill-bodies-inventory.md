<!-- docket:backlink:start (generated — do not hand-edit) -->
> ↩ **[Change 0154 — Remove stale Bash instructions and duplicated runtime contracts from Docket skills](https://github.com/danielhanold/docket/blob/docket/docs/changes/active/0154-audit-skill-bodies-for-the-stale-restatement-class-change-01.md)**
<!-- docket:backlink:end -->
# Change 0154 — Skill-body audit inventory

The reviewable per-file audit record for change 0154. Every task appends dispositions to the
rows below; Task 9 verifies no unresolved row remains. One row per tracked Markdown file under
`skills/`.

## Header — baseline and discovery

- **Baseline HEAD (feature checkout):** `f0673b5e233a24caa41038a9a5afbc8d8576712f`
  (feature branch `docs/audit-skill-bodies-for-the-stale-restatement-class-change-01`; branched
  from the change-0154 baseline `d7363492` named in the plan's Global Constraints).
- **Discovery command:** `git ls-files 'skills/**/*.md' 'skills/*.md' | sort`
- **File count:** 29 (matches the plan's expected shape of 29 at baseline).
- **Reference-op population** (for later owner-verification, Task 4/etc.): captured from
  `docket capabilities | awk '{print $1}' | sort -u` — 72 operations. Snapshot lives in the
  build scratch dir (`cap-ops.txt`); re-derive from the running catalog when verifying, never from
  this note.

### Mechanical seed scans are seeds, not a parse

The columns below attach the raw hits from the Task 1 mechanical seed scans (removed script-tree
refs, count words, old report grammar, retired surfaces) plus their whitespace-collapsed variants.
**These scans are sampling, not a full read.** Per the plan, **every file still gets a full manual
read in Task 7**, and a `no-hit (mechanical)` row is not yet a cleared row — its disposition stays
unresolved until a task (2–6 for the seeded files, 7 for the rest) records a verdict. Anchors are
quoted clauses / section names, never line numbers (ADR-0054); parenthetical line numbers are
baseline reading aids only.

### Seed legend

- **Seed 1** — status report grammar (`backlog <status>`, `change <id> …`, `ready […]`, `pass ok`,
  `harvest <id>`).
- **Seed 2** — sweep posture.
- **Seed 3** — board / GitHub-mirror claims (`board-refresh.sh`, `render-board.sh`, `github-mirror.sh`,
  `issue-minted`/`project-minted`, `write-back` marker, `github-board-mirror.md`).
- **Seed 4** — convention config copies (`.docket.yml` sketch, config-layer key list, `board_surfaces`).
- **Seed 5** — legacy explanations / counts (`nine`/`eight` etc., derived-view script family, main-mode).

### Mechanical scan commands (recorded for reproducibility)

```bash
# A: removed script-tree references (retired Bash owners)
/usr/bin/grep -rn -E '(docket-status|board-checks|board-refresh|render-board|github-mirror|render-change-links|render-artifact-backlink)\.(md|sh)' skills/
# B: copied count words / enumerations
/usr/bin/grep -rn -E '\b(nine|eight|seven|six|five)\b' skills/
# C: old report grammar tokens
/usr/bin/grep -rn -E 'pass ok|harvest <id>|backlog <status>|swept <id>|issue-minted|project-minted' skills/
# D: retired surfaces
/usr/bin/grep -rn -E 'main-mode|main mode|github-mirror|Projects v2|write-back' skills/
# Whitespace-collapsed variant: per file, tr -s '[:space:]' ' ' then /usr/bin/grep -qF each phrase
# (surfaced the same file set as the line-based scans — no wrapped-prose-only hits were hidden)
```

## Inventory

| path | disposition | hits (section / quoted clause) | owner verified against | notes |
| --- | --- | --- | --- | --- |
| `skills/docket-status/SKILL.md` | seeds 1–2 hit: fixed (Task 2); seed 3 hit: fixed (Task 3) | **Seed 1/2/3.** Scan A: "The per-line shapes and failure postures stay documented in `scripts/docket-status.md`" (Overview ~l12); "see `scripts/docket-status.md` for the output-line shapes and failure postures" (~l63); "`board-refresh.sh` is its only writer" (~l87); "the orchestrator (contract: `scripts/docket-status.md`)" (~l107); "`board-refresh.sh` (contract: `scripts/board-refresh.md`) … the pure renderer `/render-board.sh` (contract: `scripts/render-board.md`)" (~l113); "`github-mirror.sh`, mechanics in `skills/docket-convention/github-board-mirror.md`" (~l115); "`scripts/board-checks.md`, and the `check <check-id>` report-line row in `scripts/docket-status.md`" (~l131). Scan C: "`backlog <status> <count>` and `change <id> <status> <readiness> <slug>` lines, plus a trailing `ready [<id> …]`" (~l12, ~l80); "**`pass ok`**" (~l82); "**`harvest <id> <path>` lines**" (~l93); "`issue-minted`/`project-minted` lines to record back" (~l115). Scan D: "`minted issue <id> <n>` / `minted project <owner> <n>` lines" (~l96); "one-way Issues + Projects v2 mirror" + `write-back` marker (~l115). | seeds 1–2: `internal/app/status_result.go` (`StatusResult`: summary/changes/ready/findings), `internal/app/status_human.go` (`HumanText`), `docket status --json`, `docket schema --operation status`/`maintenance.sweep` (entries: id/kind/disposition/operation/reason/message). Seed 3 owners deferred to Task 3. | **Task 2 (seeds 1–2) — fixed.** Report contract regrounded to the structured `StatusResult` payload (summary counts, `changes`, `ready`, `findings`); removed the `backlog <status>`/`change <id>`/`ready [<id> …]` line grammar, the `pass ok` completion line (→ envelope `result`), and the `harvest <id> <path>` line grammar (harvest deferred). Sweep posture rewritten to the surviving obligations against `maintenance.sweep` (read-vs-mutation, terminal envelope, per-entry `disposition`/`reason` under `applied`, preserve `unknown`, surface-don't-repair, sweep-recovery-distinct-from-finalize-merge; caller-variance sentence preserved). Removed non-existent script pointers `scripts/docket-status.md`, `scripts/board-checks.md`, `scripts/board-refresh.md`, `board-refresh.sh`, `render-board.sh`; board-writer narrative regrounded to the Go metadata transaction. Historical `health checks failed <exit>` line NOT touched (per plan). **Guards:** whole test surface (internal/, tests/, incl. whitespace-flattened) searched for every removed clause — **no dependent guard keyed on the retired copy** (board-checks/render-board hits are synthetic fixtures + anchor-scanner strings; `swept` in inline_role_stop is the guard's own vocabulary). No guard file changed. Surviving-property guards over this file all still green and non-vacuous: `change_0389_sweep_scope`/`change_0397_preflight_op` sentinel phrases sit outside edit regions (preserved verbatim); `TestSkillSizeBudgets` (3056 ≤ 3065 words) and `TestCapabilitySurface` migrate-pin (kept at 9; no new `docket <argv>` literal) reconciled by editing prose, not the pin. Mutation matrix: (a) budget — reddened at 3134w, green at 3056w; (b) capability-surface — reddened on injected `docket schema --operation` literal + migrate count 8, green after removing literal/restoring migrate; (c) `TestProseContracts` — stripped "never that every item succeeded" → RED, restored → green; (d) migrate pin — dropped occurrence → count 8 RED, restored → green. **Task 3 (seed 3) — fixed.** Board-off contradiction resolved: the two "it must not exist" lines now state the current property (disabled rendering commits nothing to `BOARD.md`, a pre-existing board is left untouched, disabled rendering never authorizes deleting a board, the skill stays read-only over `BOARD.md`), pointing at the convention's *Board refresh on status writes* owner. Removed the active mirror instructions: the `minted issue`/`minted project` write-back bullet (with its `docket:config-read-channel: write-back` marker), the `github` mirror-reachability judgment bullet (and its echo in the Health-checks section, now "one judgment check"), the Final-summary "GitHub mirror, if enabled" pointer, and the Board-section `github` one-way-mirror paragraph (`github-mirror.sh` / `issue-minted`/`project-minted` / write-back marker) — replaced by a compatibility pointer grounded in `internal/config/capability.go`. **Owners verified:** `internal/app/planning.go` (`fenceBoardSurface`: a `github` token is refused before any transaction, `ReasonUnsupportedBoardSurface`), `internal/app/derived_views.go` (`includeBoard`: renders/writes `BOARD.md` only when the enabled surfaces produce it, never deletes), `internal/config/capability.go` (`dispSupportedOrDropped`: `github` → Deferred + `blockerDiag`, mutation-blocking); classification discoverable via `diagnostic.config` (`docket diagnostic config --for-mutation`) and `docket capabilities`. **Guards:** removed the write-back marker's population — the config-read-channel scanned corpus (all skills minus the two excluded convention files) now names the config file nowhere; reconciled `internal/repoguard/config_read_channel_test.go` (retired the now-empty `classified >= 3`/`writeBacks >= 1` occurrence floors, added a reader-liveness floor probing the excluded convention body for >= 3 real occurrences, retargeted the deleted-file sibling check to `references/stacked-changes.md`); added a narrow negative guard `internal/repoguard/mirror_writeback_test.go` (`TestNoActiveMirrorWriteBack`: no skill line names a minted mirror identifier AND a record-it-back obligation on the same line; non_vacuity subtest exercises the classifier). Mutation matrix (all via the task gate, `/usr/bin/grep -cF` on whitespace-flattened copies): (a) new mirror guard — planted `issue-minted … record … back` → RED (1 violation), removed → green; (b) config-read-channel RULE — planted unmarked `.docket.yml` → RED, restored → green; (c) reader-liveness floor — probe pointed at a token-free file → RED (0 < 3), restored; (d) config sibling check — retargeted to a nonexistent sibling → RED, restored; (e) budget forward check — stale `github-board-mirror.md` row → RED (missing file), removed. `go test ./internal/repoguard/ -count=1` green. |
| `skills/docket-convention/SKILL.md` | seed 3 (mirror) hit: fixed (Task 3); seed 4 hit: fixed (Task 4); seed 5 hit: fixed (Task 5); Scan-A residue hit: fixed (Task 6); harvest wording no hit: verified current (Task 6) | **Seed 3/4/5.** Scan A: "The one writer allowed to touch them afterward is `render-artifact-backlink.sh`" (~l211); "rendered by `render-change-links.sh` from frontmatter" + "written solely by `render-artifact-backlink.sh`" (`## Artifacts`, ~l220); mirror section "owned by the deterministic `github-mirror.sh`" (~l369); "**Derived-view script family**" listing `board-refresh.sh`/`render-board.sh`/`github-mirror.sh`/`render-change-links.sh`/`render-artifact-backlink.sh` (~l371). Scan B: "**Seven skills** get a wrapper"/"except **nine**"/"Those **eight** wrapper-bearing exceptions" (~l113); "superpowers for … docket's own for `build`/`review`" five-step (~l123); "### Lifecycle — eight states" (~l258); "author any of the five surfaces" (~l336); "status→issue mapping across all eight states" (~l369). Scan D: "`github_project:` … auto-create on first github sync" (~l43); mirror paragraph "Projects v2" (~l369); "surviving for frozen / main-mode paths" (~l371); "`PROCEED` (migrated or main-mode)" + "The guard is a no-op in `main`-mode" (~l385). | seed 4: `internal/config/schema.go` (key set + per-key classification/scope), `internal/config/capability.go` (`dispSupportedOrDropped`/`dispInert`), shipped `.docket.example.yml`, resolved `diagnostic.config --for-mutation` | **Task 3 (seed 3 mirror) — fixed:** the *GitHub board mirror (shared definition)* section (former ~l369) rewritten to one compatibility sentence grounded in `internal/config/capability.go` (`github` unsupported + mutation-blocking; historical `issue:` data preserved, never acted on), dropping the `github-mirror.sh` writer claim, the "all eight states" mapping restatement, and the `[github-board-mirror.md](…)` link (that file is deleted); the incoming *Script contracts* parenthetical link to `github-board-mirror.md` (former ~l63) removed as a dangling reference. **Task 4 (seed 4) — fixed.** Verified every sketch/config claim against the four named owners; per-claim verdicts: `integration_branch: auto` / `changes_dir` / `adrs_dir` / `results_dir` / `change_types` (REPLACE not merge) / `auto_capture` (map; old scalar hard error) / `terminal_publish` (deferred, repo-fenced) / `build.*` / `finalize.*` (incl. `skip_results_only_delta` fenced) / `learnings.*` / `agent_harnesses` / `agents` — all **true (owner-readable)**, kept. **False/stale, fixed:** (a) sketch `board_surfaces` comment "inline (BOARD.md) and/or github" — `github` is a dropped surface (capability.go `dispSupportedOrDropped`: unsupported + mutation-blocking), corrected to name only `inline` and mark github dropped; (b) sketch `github_project` comment "auto-managed Projects v2 board; auto-create on first github sync" — schema.go `dispInert` + `.docket.example.yml` "NOT WIRED TODAY … nothing written back over auto", corrected to inert/read-by-nothing; (c) **the absorbed change-0257 item** — the `skills:` block comment "unset key = the superpowers default shown" is stale (build/review default to docket's OWN roles since change 0193, not superpowers), **fixed** to "unset key = the default shown (superpowers for brainstorm/plan/finish, docket's own for build/review — change 0193)"; (d) *Config layers* paragraph — deleted the copied parenthetical coordination-key inventory (`metadata_branch, integration_branch, changes_dir/adrs_dir/results_dir, github_project, terminal_publish, finalize.skip_results_only_delta, board_surfaces' github`), keeping the layer model, per-field precedence, and the FENCE concept + posture (warned-and-ignored, never fatal, ADR-0019), letting the existing `internal/config` pointer stand alone (added `diagnostic.config`/schema as the discovery channel); (e) *`board_surfaces`* paragraph — removed the mint-and-write-back clause and the "one-way mirror" active-surface framing, aligned to capability.go (`github` unsupported + mutation-blocking; `github_project` inert, never minted/written back). **Recorded false but NOT edited (Task 5's main-mode territory):** sketch `metadata_branch: docket # docket (default) | main` — schema.go marks `metadata_branch` an **obsolete tombstone** (change 0363, `dispObsolete`; absent from `diagnostic.config` effective view), so the `| main` main-mode comment is stale; left for Task 5 (main-mode) per the task boundary. **Guards:** whole test surface (internal/, tests/) searched for every removed phrase (`superpowers default shown`, `and/or github`, `auto-create on first github sync`, `minted-and-written-back`, `one-way mirror`, the enumerated coordination keys) incl. whitespace-flattened — **no dependent guard keyed on any removed prose; no guard file changed.** The one guard over this body — `config_read_channel_test.go`'s reader-liveness floor (probes this excluded convention body for >= 3 config-file-token occurrences) — is preserved: no `.docket.yml`/`.docket.local.yml`/`config.yml` token removed, count stays 8 (>= 3). `TestSkillSizeBudgets` convention ceiling 7969 words: net edit is a deletion, file 7962 words (under). `TestProseContracts` convention sentinels (`test_dispatch_capability`: "Dispatch-capability resolution"/"never from a tool name") untouched. `go test ./internal/repoguard/ -count=1` **PASSED** via the task gate driver. **Task 5 (seed 5) — fixed.** Verified current reality: `/usr/bin/grep -rn 'main-mode\|DOCKET_MODE' internal/` shows no surviving main-mode path (only test-comment/fixture references to the *removed* topology — `operational_context.go` "removed main-mode topology", `planning_git_test.go` "0363 removed the retired main-mode row"), and `ls scripts/` confirms none of `board-refresh.sh`/`render-board.sh`/`github-mirror.sh`/`render-change-links.sh`/`render-artifact-backlink.sh` exist. Current Go owners: `internal/app/derived_views.go` (`includeBoard` — board rendered inside the metadata transaction), the `artifact.backlink` operation (per-artifact `docket:backlink`), the app-layer `## Artifacts` link-block renderer inside the same transaction. **Edits:** (1) the "Derived-view script family" paragraph rewritten to "**Derived views.**" — derived views (inline board, `## Artifacts` blocks, `docket:backlink` blocks) rendered by the Go app inside the owning metadata transaction / via `artifact.backlink` (change 0369); sole-writer + never-hand-edited invariant kept as the ADR-0012 boundary evolved (the model, not a Bash renderer, owns the write); "surviving for frozen / main-mode paths" removed. (2) main-mode clauses dropped: `PROCEED` (migrated or main-mode)→`PROCEED` (migrated); "The guard is a no-op in `main`-mode" removed (both fenced `docket repository migrate`/`docket repository init` occurrences on that line preserved → capability_surface migrate-pin intact); "for their frozen and `main`-mode contracts"→"for their frozen Bash contracts" (Reaching-operations para); the *Branch model* incoming reference "and the `main`-mode degradation live in [terminal-close-out.md]" retargeted to "Ordering and per-caller failure postures live in …" (no dangling anchor to the deleted section). (3) sketch `metadata_branch: docket # docket (default) | main`→`# obsolete tombstone (change 0363; ADR-0099 one topology): docket is the sole metadata topology — any value is warned-and-ignored, never resolved` (schema: `config.go`/`decode.go` `CodeObsoleteSetting`, `dispObsolete`, absent from Effective). **Counts (0170 exemption withdrawn — bare counts removed, no guard floors any of them):** "Seven skills get a wrapper"→"The autonomous skills get a wrapper"; "except **nine**:"→"except these:"; "Those **eight** wrapper-bearing exceptions"→"These wrapper-bearing exceptions" (+ dropped the "four `docket-build-*`"/"three `docket-review-*`" glob cardinalities); "**Five** of the **seventeen** generated wrappers"→"Several generated wrappers"; "docket's five workflow steps"→"docket's workflow steps"; "### Lifecycle — eight states"→"### Lifecycle"; dummy-mode "Five surfaces are eligible"→"The eligible surfaces" and "any of the five surfaces"→"any of these surfaces". Each removed count merely restated an adjacent enumeration (the wrapper list, the roles table, the lifecycle table, the surfaces list). **Guard/test-dependent check (whole test surface, incl. whitespace-flattened):** no guard keys on any removed prose count — `skill_handoff_sites_test.go` floors *discovered* `len(wrappers) >= 10` (agent `.md` files), not the prose "seventeen"/"Seven"; "seventeenth" hits are code comments + synthetic-agent test scaffolding (`internal/install`, `internal/harness/*`), not prose-count asserts; no incoming reference to the `#lifecycle-eight-states` anchor exists outside this inventory/plan. Sentinels over this file preserved verbatim (test_composition_wiring "to await a task-notification"; change_0389 "no longer implies a full historical sweep"; change_0397 "runs the `maintenance.preflight` operation inline"; test_dispatch_capability; test_skill_handoff_precedence; test_learnings_ledger; test_results_artifact; test_dummy_mode; test_role_skill_self_description; change_0410 results). config-read-channel reader-liveness floor preserved (no `.docket.yml`/`.docket.local.yml`/`config.yml` token removed). Word/line budget: 7941w / 395L (ceiling 400/7969). **No guard file changed → no guard mutation required (RED/GREEN unsuitable: docs-only prose edits with no executable behavior; replaced by the whole-suite `go test ./internal/repoguard/ -count=1` green + preserved-sentinel and budget checks; residual risk: embedded bundle now diverges from `skills/` until Task 8 regenerates — expected).** **Adjacent finding NOT edited (out of Task 5's `main-mode` scope — uses the "single-branch"/"`main`" spelling, not "main-mode", so it is deliberately outside the focused-verification grep):** the *Backward-compatible opt-out* sentence (~l55, "pinning `metadata_branch: main` … reproduces single-branch behavior exactly — no `docket` branch, no `.docket/` worktree") and the *Branch model* "the primary working tree on the integration branch in single-branch (`main`) mode" (~l389) describe a topology that 0363/ADR-0099 retired; the same live single-branch narrative persists in the out-of-scope shipped `.docket.example.yml` (metadata_branch comment, ~l63–69). Recommend a coordinated follow-up doc sweep (one change across skill + example config), not a piecemeal Task-5 edit. `go test ./internal/repoguard/ -count=1` PASSED via the task gate driver (owner task). **Task 6 — Scan-A residue fixed.** Two dead-Bash-owner refs in the `## Artifacts`/backlink prose that Task 5's *Derived views.* rewrite did not reach (they contradicted it): l211 "The one writer allowed to touch them afterward is `render-artifact-backlink.sh`" → "…is the `artifact.backlink` operation"; l220 "rendered by `render-change-links.sh` from frontmatter" → "rendered from frontmatter by the Go app-layer link-block renderer inside the owning metadata transaction", and "written solely by `render-artifact-backlink.sh` (change 0136…)" → "written solely by the `artifact.backlink` operation (change 0136…)". **Owners verified:** `artifact.backlink` in the catalog (`docket artifact backlink --artifact <path> --change <path>`); the `## Artifacts` render owner is `internal/render/artifacts.go`. No guard keyed on the l211/l220 phrases (test-surface `render-artifact-backlink` hits are the Go owner's provenance comment + fixture `PROVENANCE.md`; no `prose_contracts` sentinel on "writer allowed to touch"/"sole writer"/"## Artifacts"/`docket:backlink`). **Task 6 — harvest wording no hit: verified current.** l358 "automated learnings harvest is deferred from Go v1, so a finding is created or extended by editing `learnings/` files directly" — same evidence as `references/learnings.md`: no automated harvest/index/capacity/promotion operation exists; the only learnings ops are the manual `learning.record`/`learning.update` (which do not touch the index). Current guidance, not edited. **Adjacent (NOT a Scan-A token — Task 7):** l227 `reclaim-claims.sh` is a retired Bash owner but does not match Scan A's script set. **Task 7 — adjacent findings resolved.** (l55) deleted the "Backward-compatible opt-out: pinning `metadata_branch: main` … reproduces single-branch behavior exactly" sentence — `metadata_branch` is an obsolete tombstone (0363/ADR-0099), so the opt-out contradicted Task 4's own sketch edit. (l199) the `issue:` frontmatter comment "GitHub mirror issue number; minted on first `github` sync (one-way)" → "historical GitHub mirror issue number (mirror retired per capability.go; preserved as data, never acted on)", matching the section's own *GitHub board mirror* statement (l369). (l227) the `## Reclaim log` dead-Bash owner `reclaim-claims.sh` → the `change.reclaim` operation (catalog: `docket change reclaim --id --version`). (l389) *Branch model* "the primary working tree on the integration branch in single-branch (`main`) mode, the persistent `.docket/` worktree in `docket`-mode" → "the persistent `.docket/` worktree (ADR-0099: docket is the sole metadata topology)". **Kept** the legitimate bootstrap 2×2 single-branch refs (l83/l382/l385) — they describe a repo STATE that triggers `docket repository migrate`, current/correct guidance, not an active operating mode. No test-surface guard keys on any removed phrase (whole internal/ tests/ search incl. whitespace-flattened; the "main-mode"/"single-branch" hits there are frozen corpus + test comments confirming the topology is RETIRED, and the app-layer's own legacy-single-branch→migrate refusal strings). Budget: net deletion, convention stays under the 7969-word ceiling. **Out-of-scope follow-up re-confirmed:** the shipped `.docket.example.yml` still carries the live single-branch `metadata_branch` narrative (out of the `skills/` population; no `skills/` edit here required touching it) — recommend the coordinated doc sweep noted in Task 5. |
| `skills/docket-convention/github-board-mirror.md` | hit: fixed (Task 3 — file deleted) | **Seed 3/5.** Scan A/D: whole file is the active GitHub Issues + Projects v2 mirror recipe — "The `github` board surface mirrors each change to one GitHub issue (and one Projects v2 item)" (~l7); "`github-mirror.sh`" external-write owner (~l7); "**Projects v2.** … writes its `{owner, number}` back into `.docket.yml`" + `write-back` marker (~l17); "logs the skipped status (`scripts/github-mirror.md`)" (~l17). Scan B: "**Status → issue mapping (all eight).**" (~l11). | n/a — file deleted (Task 3) | **Task 3 — DELETED** (`git rm`; preferred retirement of an active recipe for a retired feature). The compatibility statement now lives as one sentence in the convention's *GitHub board mirror* section. Incoming maintained links fixed: convention *Script contracts* parenthetical (removed) and the mirror-section link (removed); `internal/repoguard/budgets_test.go` row deleted; `internal/repoguard/config_read_channel_test.go` sibling-probe retargeted to `references/stacked-changes.md`. Remaining `github-board-mirror` mentions are point-in-time `docs/results/*` records (untouched, per the never-modernize-history rule) and the generated `internal/assets/embedded` copy (Task 8 regenerates). Historical `issue:` data/fixtures NOT removed. |
| `skills/docket-convention/references/stacked-changes.md` | hit: fixed (Task 6) | **Seed 5.** Scan A: "parent-side **Stacked children** row is derived at render time by `render-change-links.sh`" (~l21). | `internal/render/artifacts.go` (Go link-block renderer; `ls scripts/` confirms `render-change-links.sh` absent) | **Task 6 — fixed.** Dead Bash owner `render-change-links.sh` → "rendered by the Go link-block renderer" (owner `internal/render/artifacts.go`, whose header comment records it reproduces the former script's block body). Phrasing kept word-neutral vs the baseline script-name clause so `TestSkillSizeBudgets` (this file was at its 2140-word ceiling) stays green without a ceiling bump. No guard keyed on the prose: the `render-change-links` test-surface hits are the Go owner's own provenance comments (`internal/render/artifacts.go`) + a fixture `PROVENANCE.md`, not asserts; `config_read_channel_test.go:170` only probes this file's corpus presence, which is unchanged. **Adjacent (NOT a mechanical hit — Task 7):** l50 "The board and the mirror … keeps its issue **open**" is retired-mirror narrative (the `github` board surface is unsupported/mutation-blocking since Task 3); the space form "the mirror" escaped Scan D's `github-mirror` token. **Task 7 — adjacent finding resolved (l50).** "**The board and the mirror** — it renders in its own section and keeps its issue **open**" → "**The board** — it renders in its own section" (retired-mirror active narrative removed; the `github` board surface is unsupported/mutation-blocking since Task 3). No test-surface guard keys on the phrase. |
| `skills/docket-convention/references/terminal-close-out.md` | hit: fixed (Task 5) | **Seed 5.** Scan D: TOC entry "· [main-mode degradation](#main-mode-degradation) ·" (~l12); "## main-mode degradation" section (~l124). | ADR-0099 (one topology) / change 0363 (main-mode removed) | **Task 5 — fixed.** Deleted the `## main-mode degradation` section (both paragraphs) and its TOC entry; the `## The sequence (docket-mode)` heading + `#the-sequence-docket-mode` anchor are left unchanged (no anchor churn). Incoming references to the deleted section retargeted so no dangling anchor remains: convention SKILL.md *Branch model* ("Ordering and per-caller failure postures live in …"), `docket-implement-next/references/edge-paths.md:9` ("owns invocations and ordering"), `docket-new-change/SKILL.md:57` ("mechanics and ordering live there"). Verified via `/usr/bin/grep -rn 'main-mode' skills/`: no `#main-mode-degradation` link and no reference to the section survives. Word/line budget 1478w/151L (ceiling 240/2150). No guard keys on this file's main-mode prose (whole-surface search); repoguard suite green. |
| `skills/docket-convention/references/dummy-mode.md` | hit: fixed (Task 6) | **Seed 5 (count word).** Scan B: "space-separated subset of these **five**" (~l12). | the adjacent *Surface tokens (v1)* table (5 rows); no Go guard floors the count | **Task 6 — fixed.** "these **five**" → "these" — the count merely restated the adjacent 5-row surface-token table (dialogue/reports/results/change-sections/pr); redundant cardinality removed under the withdrawn-0170 rule, consistent with Task 5's convention-side dummy-mode edits ("Five surfaces are eligible"→"The eligible surfaces"). No guard floors it: no `these five` test-surface hit; the `test_dummy_mode` sentinel over this file anchors on "In plain terms" (untouched). **Adjacent (NOT a mechanical hit — Task 7):** l64 "GitHub mirror issue bodies" names the retired mirror surface; the space spelling escaped Scan D. **Task 7 — adjacent finding resolved (l64).** Removed "GitHub mirror issue bodies" from the not-eligible *Script-generated views* list (a retired surface named as an active generated view). `test_dummy_mode` sentinel over this file ("In plain terms") untouched. Kept the generic "Script-generated views" label — a category descriptor for deterministic renderer output, not a named dead owner. No test-surface guard keys on the phrase. |
| `skills/docket-finalize-change/references/gate-failure.md` | no hit: verified current (Task 6) | **Seed 5 (count word).** Scan B: "force every derived view to say **six** different things about one label" (~l95). | the file's own *abort-and-report points* set (8 bullets) | **Task 6 — no hit.** "six different things" is **rhetorical**, not a cardinality restating an adjacent list — the abort-and-report set on this page has 8 bullets, not 6; the phrase argues that encoding a transient multi-cause abort as a status would overload one label. Accurate current prose, not stale; the count rule (delete a count that restates an adjacent list) does not apply. Not edited. The `test_finalize_gate_waiting` sentinel over this file is untouched. |
| `skills/docket-build/references/delegation-execution.md` | hit: fixed (Task 6) | **Seed 5 (count word).** Scan B: "until change 0370 deletes it. The **six** required capabilities are defined once, in `gate-execution.md`" (~l5). | the count is owned by `gate-execution.md`'s capability list | **Task 6 — fixed.** "The **six** required capabilities are defined once" → "The required capabilities are defined once" — a cross-reference restating the count owned by `gate-execution.md`; removed for consistency with that file's own count removal (below). Only the redundant cardinality was touched; the point-in-time hermetic-measurement evidence and per-harness verdicts on this page are untouched (not modernized). No guard floors the count (no `six required` test-surface hit). |
| `skills/docket-build/references/gate-execution.md` | hit: fixed (Task 6) | **Seed 5 (count word).** Scan B: "## The **six** required capabilities" (~l8); "### The **seven** probe scenarios" (~l133); "observed against all **seven**" (~l135). | the adjacent numbered lists (capabilities 1–6; scenarios 1–7); no Go guard floors either count | **Task 6 — fixed.** "## The **six** required capabilities" → "## The required capabilities"; "### The **seven** probe scenarios" → "### The probe scenarios"; "observed against all **seven**:" → "observed against all of them:". Each restated the immediately-adjacent numbered list; redundant cardinalities removed under the withdrawn-0170 rule (kept the rule and the list). No guard floors any: no `six required` / `seven probe` test-surface hit; the `test_gate_execution_posture` sentinel over this file asserts only the `gate-caller-loop` pointer (preserved verbatim). |
| `skills/docket-build/references/gate-execution-evidence.md` | hit: fixed (Task 6) | **Seed 5 (count word).** Scan B: "the **six** required capabilities, the mitigation, and each harness's verdict" (~l4). | the count is owned by `gate-execution.md`'s capability list | **Task 6 — fixed.** "the **six** required capabilities" → "the required capabilities" — a cross-reference restating the count owned by `gate-execution.md`; removed for consistency with that file's count removal. Point-in-time probe-evidence measurements untouched (not modernized). No guard floors the count. |
| `skills/docket-build/references/gate-caller-loop.md` | hit: fixed (Task 6) | **Seed 5 (count word).** Scan B: "The **five** raw verbs — `gate.launch`, `gate.observe`, `gate.stop`, …" (~l117). | the adjacent inline verb list (`gate.launch`/`gate.observe`/`gate.stop`/`gate.recover`/`gate.cleanup`); no Go guard floors it | **Task 6 — fixed.** "The **five** raw verbs — …" → "The raw verbs — …" — the count restated the adjacent inline 5-verb list; redundant cardinality removed under the withdrawn-0170 rule. No guard floors it: the only `raw verbs` test-surface hit is a comment in `gatedriver_test.go` ("The raw verbs (`docket gate launch\|observe\|stop`)") that names no count; the `test_gate_caller_loop` sentinel (present: "## The disposition vocabulary", "## Handoff") and the `TestGateDriveJSONCapture` contract clauses (JSON-capture section) are far from the edit and untouched. |
| `skills/docket-adr/adr-template.md` | no hit: verified current (Task 7) | no hit | n/a (pure ADR template) | **Task 7 — full read.** Pure ADR frontmatter + section template (Context/Decision/Consequences). No runtime-owned list/count restated, no dead owner, no retired-behavior-as-active claim. Current. |
| `skills/docket-adr/SKILL.md` | hit: fixed (Task 7) | Retired-topology active narrative: `l74` "In `main`-mode the metadata working tree *is* the integration branch …" (backtick form escaped the seed `grep 'main-mode'`). | change 0363 / ADR-0099 (single metadata topology); `internal/app/operational_context.go` ("removed main-mode topology") | **Task 7 — fixed.** Deleted the standalone `main`-mode paragraph — 0363/ADR-0099 retired main-mode, so docket is the sole topology and "writing the ADR to the metadata tree = the record" is already covered by the deferred-publication paragraph directly above it. Rest of body current: every "terminal publication is deferred from Go v1" statement describes a deferred feature AS deferred (current guidance, kept); the typed `adr.record`/`adr.supersede`/`adr.reverse` ops and the `repository.check` / `docket repository migrate --repair-frontmatter` drift-repair path verified against the catalog. **Guard:** `test_docket_metadata_branch` sentinel over this file (present "adr-unpublished", ~l72) preserved — the deletion is below it; no test-surface guard keys on the removed `main`-mode phrase (whole internal/ tests/ search incl. whitespace-flattened — those hits are frozen corpus + test comments confirming the topology is RETIRED). Budget: net deletion, under ceiling. |
| `skills/docket-auto-groom/SKILL.md` | no hit: verified current (Task 7) | no hit | catalog (`change.groom`/`change.defer`, `status`); `docket-auto-groom-critic` agent | **Task 7 — full read.** Drain-loop over typed ops (`change.groom` spec/trivial, plain-git abstain, `status --json` version pin) + adversarial critic dispatch + learnings-index read. No dead owner, no retired-behavior-as-active, no runtime-owned list restated. `test_auto_groom` sentinel ("Kill and defer are NEVER autonomous" / "docket-auto-groom-critic") untouched. Current. |
| `skills/docket-brainstorm/SKILL.md` | no hit: verified current (Task 7) | no hit | ADRs 0006/0018/0049; `docket-brainstorm-consultant` agent | **Task 7 — full read.** Single-dispatch consultant-author flow; dialogue inline with the human, one consultant dispatch, stop-at-spec, ADR-0018 degrade rule. No dead owner or stale runtime contract. `test_consultant_brainstorm` sentinel ("docket-brainstorm-consultant") untouched. Current. |
| `skills/docket-build-task/SKILL.md` | no hit: verified current (Task 7) | no hit | catalog (`gate.drive.start`/`gate.drive.handoff`) | **Task 7 — full read.** Per-task worker contract: focused cycle through the native gate driver, `gate.drive.handoff` on WAITING, COMPLETE/WAITING/NEEDS_ESCALATION/BLOCKED outcomes. No dead owner, no retired behavior, no runtime-owned list restated. Current. |
| `skills/docket-build/references/task-routing.md` | no hit: verified current (Task 7) | no hit | its own rubric (character→profile) | **Task 7 — full read.** Shared character→profile rubric read by docket-build and the fix loop; economy/standard/premium/max defined by reasoning + reversibility. No runtime-owned enumeration, no dead owner, no retired behavior. Current. |
| `skills/docket-build/SKILL.md` | no hit: verified current (Task 7) | no hit | catalog (`gate.drive.*`, `evidence.record`); `install.sh` exists at repo root | **Task 7 — full read.** Profile-routed plan execution: `gate.drive.start`/`advance`/`handoff`/`claim` driver, `evidence.record`, build-evidence marker block, escalation ladder. References to `install.sh` (re-run to regenerate wrappers) point at a live file present at the repo root — not a dead owner. No retired-behavior-as-active, no stale runtime restatement. Current. |
| `skills/docket-convention/references/agent-layer.md` | no hit: verified current (Task 7) | no hit | `install.sh` + `link-skills.sh` (both exist at repo root); catalog (`development.install`, `install.check`); change 0371 | **Task 7 — full read.** Layered `agents:`/`agent_harnesses:` config + install mechanics. References to `install.sh` and `link-skills.sh` point at live files present at the repo root (the Go `development.install` is the wrapper generator; `install.sh` is its bootstrapper) — not dead owners. Cross-harness `runner:` delegation is described AS retired (change 0371); ADR-0015 harness-portable IDs current. No dead owner, no retired-behavior-as-active. Current. |
| `skills/docket-convention/references/learnings.md` | no hit: verified current (Task 6) | **Seed-adjacent (harvest wording).** "automated learnings harvest is deferred from Go v1 — record or update findings by editing `learnings/` files directly" (~l48); plus the same deferral for index rendering (~l20), capacity, and promotion (~l73). | running binary `docket v0.9.3-884-g2217ca64`; capability catalog; `internal/app/learning_ops.go` docstring | **Task 6 — no hit: verified current.** Verified against the running binary and the catalog: **no** automated learnings *harvest*, index-rendering, capacity, or promotion operation exists — the ONLY learnings operations are `learning.record` / `learning.update`, described by their Go owner (`internal/app/learning_ops.go`) as "the two **manual-authoring** mutations," and "neither touches the learnings README/index." So every "deferred from Go v1" statement on this page is **current guidance**, not staleness; not edited (per the change's deferred-feature rule). **Adjacent nuance (NOT a mechanical hit; recommend coordinated follow-up):** the typed manual-authoring ops `learning.record`/`learning.update` (present since before baseline) are surfaced **nowhere** in `skills/` prose (`/usr/bin/grep` clean); the "edit `learnings/` files directly" framing omits them. Enriching the prose to name the typed record/update operations alongside hand-editing is outside this change's dead-owner/stale-restatement class and is not a mechanical-scan hit — left for a dedicated docs change. |
| `skills/docket-finalize-change/SKILL.md` | no hit: verified current (Task 7) | no hit | catalog (`context.finalize`, `finalize.*`, `repository.sync-integration`); `internal/config` (dummy_mode gate) | **Task 7 — full read.** Typed finalize sequencer end-to-end (`context.finalize`, `finalize.retarget-children`/`rebase`/`rebase-continue`/`publish`/`merge`/`closeout`/`cleanup`, `repository.sync-integration`, `change.repair-identity`). Dummy mode correctly described as a **deferred/rejected** capability (config gate refuses it). The post-merge rebuild step (12) matches this repo's AGENTS.md rebuild rule. No dead owner, no retired-behavior-as-active. `test_docket_metadata_branch` (absent "checkout origin/docket") and `test_finalize_closeout_notes` ("never pauses after merge") sentinels untouched. Current. |
| `skills/docket-groom-next/SKILL.md` | no hit: verified current (Task 7) | no hit | catalog (`change.groom`/`change.defer`, `status`) | **Task 7 — full read.** Interactive groomer: selection bands, cold-start recap, `$SKILL_BRAINSTORM` resolution, four typed exits (`change.groom` spec/trivial, kill via new-change, `change.defer`), CAS/no-claim concurrency. No dead owner, no retired restatement, no runtime-owned list. Current. |
| `skills/docket-implement-next/references/edge-paths.md` | hit: fixed (Task 7) | Retired-mirror active instruction: `l73` PR→issue reference gated on "when the `github` board surface is enabled" (dead — github unsupported/mutation-blocking) with rationale "the mirror sync stays the sole writer of issue state and close reason". | `internal/config/capability.go` (github dropped); `internal/domain/entities.go` (`issue:` optional field); no Go emitter for the ref | **Task 7 — fixed.** Rewrote the PR→issue bullet to a mirror-free courtesy: dropped the dead "when the `github` board surface is enabled" gate and the retired-mirror sole-writer rationale; `issue:` is now framed as historical data (mirror retired, mints none) and the plain `#<issue>` courtesy (never `Closes #N`, so merge never auto-closes) is kept because `issue:` remains an optional domain field and the reference is skill-authored (no Go emitter, so nothing to point at). Rest of file current (reconcile-kill posture, resume/plan/results seams, build-evidence expected-staleness, the deferred `finalize.skip_results_only_delta` permit described AS deferred). No test-surface guard on the phrases (whole internal/ tests/ search). Budget: net-neutral. |
| `skills/docket-implement-next/references/fix-loop.md` | no hit: verified current (Task 7) | no hit | `task-routing.md` rubric; `docket-build-task` contract | **Task 7 — full read.** Bounded fix loop: character→profile via the shared rubric, blocker floor / never-`max` ceiling, `REVIEW_MIN_FIX_SEVERITY`/`REVIEW_MAX_FIX_TASKS`, revert-and-record suite gate (≤2 runs), PR-body disposition table. Deferred automatic change-capture described AS deferred. No dead owner, no retired behavior. Current. |
| `skills/docket-implement-next/results-template.md` | no hit: verified current (Task 7) | no hit | catalog (`artifact.backlink`); change 0410 | **Task 7 — full read.** Required close-out artifact template; `docket:backlink` block owned by `artifact.backlink`, conditional sections, no `None`/`N/A` filler. `change_0410_results_template` sentinel untouched. No dead owner, no retired behavior. Current. |
| `skills/docket-implement-next/SKILL.md` | hit: fixed (Task 7) | Two retired-topology restatements: `l64` "`main`-mode: the primary tree's tip equals `origin/<integration_branch>`" (SHA-compare branch); `l197` "`origin/<integration_branch>` in BOTH modes" (backtick/"BOTH modes" forms escaped the seed scan). | change 0363 / ADR-0099 (single topology); convention *Branch model* ("ALWAYS cut from `origin/<integration_branch>`") | **Task 7 — fixed.** (l64) dropped the `main`-mode branch of the reconcile-push SHA-compare, keeping the single docket-mode assertion (`git -C .docket rev-parse @ == git rev-parse origin/docket`). (l197) "in BOTH modes" → "in the ordinary case" (one topology; the convention's Branch model already says ALWAYS cut from origin/<integration_branch>). The `#<issue>` PR element at l127 is mirror-free already (no edit). The parenthetical "(in `docket`-mode, `origin/docket`)" clarifiers were LEFT — they name the concrete metadata ref, not a retired mode, and are true of the sole topology. **Guard:** sentinels over this file (`change_0397_preflight_op`, `change_0407_gate_context_claim`, `change_0410_implement_next_results`, `test_loop_continuation`, `test_plan_writer_step4`, `test_convention_extraction`) untouched — none near the edits; no test-surface guard keys on the removed phrases (whole internal/ tests/ search incl. whitespace-flattened — "main-mode" hits are frozen corpus + test comments confirming RETIRED). Budget: net deletion. |
| `skills/docket-new-change/change-template.md` | no hit: verified current (Task 7) | no hit | `internal/config`/schema (frontmatter field set) | **Task 7 — full read.** Pure change-record frontmatter + section template; `auto_groomable` tri-state, `discovered_from`, `stacked_on`, the `## Artifacts` marker block all match current schema. No dead owner, no retired behavior, no runtime-owned list restated. Current. |
| `skills/docket-new-change/SKILL.md` | no hit: verified current (Task 6) | **Seed-adjacent (count/harvest vocabulary).** "the whole scan harvest becomes autonomously groomable" (~l51). | scan-mode behavior described in the same section | **Task 6 — no hit: verified current.** "scan harvest" is **judgment vocabulary** — it names the batch of scan-minted `proposed` stubs produced by scan mode, NOT the learnings harvest. The sentence accurately describes scan mode (stubs inherit the repo `auto_groomable` default; in an `auto_groom: true` repo the batch becomes autonomously groomable). Not stale; not edited. Full manual read of the rest of this file remains Task 7. |
| `skills/docket-review/SKILL.md` | no hit: verified current (Task 7) | no hit | ADR-0066 (read-only reviewer); ADR-0054 (anchor style) | **Task 7 — full read.** Read-only whole-branch reviewer: returns severity-tiered findings, never fixes/dispatches/runs the suite, `unverified-build-state` blocker on stale evidence, `location` anchors on `path:symbol`/verbatim clause not line number. No dead owner, no retired behavior, no runtime-owned list restated. Current. |

## Disposition vocabulary (for tasks 2–7)

Each row's final disposition is one of:

- `hit: fixed` — a genuine stale/duplicated-contract defect of this change's class, corrected here
  (with the guard reconciliation recorded).
- `hit: reported (out of scope)` — a real defect outside this change's class; recorded with its
  existing change id when known, not fixed here.
- `no hit: verified current` — read in full and confirmed accurate against the named current owner
  (a true statement about a deferred feature counts as current guidance, not staleness).

No row may remain unresolved after Task 7 (Task 9 verifies).
