<!-- docket:backlink:start (generated — do not hand-edit) -->
> ↩ **[Change 0446 — Orphaned halted gate drive blocks every new worktree's first gate admission](https://github.com/danielhanold/docket/blob/docket/docs/changes/archive/2026-09-24-0446-orphaned-halted-gate-drive-blocks-every-new-worktree-s-first.md)**
<!-- docket:backlink:end -->

# Change 0446 — Keep historical bookkeeping from blocking current execution

## Scope after the 2026-09-23 split

This change was split into three, built in succession and all critical:

1. **0446 (this change):** gate history and run bookkeeping. Old drive, slot, and epoch records must not block unrelated worktrees or runs from starting, cancelling, resuming, completing, or entering finalize.
2. **0448:** a named implement-next start skips the unrelated maintenance preflight (formerly §7 and acceptance test 9 here). It `depends_on` 0446.
3. **0449:** scoped metadata validation, a board that tolerates unrelated invalid records, and bounded named branch-fact reads (formerly §8, the metadata grounding, and acceptance tests 10–12 here). It `depends_on` 0448 and owns the end-to-end named-workflow test across all three.

References below to "B" and "A" keep their meaning: B is the explicitly requested change/worktree/run, A is an unrelated one.

## Required outcome

An explicitly named change B's runtime execution bookkeeping—gate admission, run ownership, cancellation, resume, successful ownership closeout, and the transition into finalize—must not be blocked by unrelated historical gate records belonging to change A. Metadata-write and startup-maintenance blockers are handled by 0449 and 0448. A start may be refused for an execution/owner conflict only when evidence connects the conflict to the requested canonical worktree or the explicitly requested run. Historical uncertainty is not repository-wide ownership.

The same rule applies to cancellation, resume, successful ownership closeout, and the transition into finalize. Fix the class of false blockers, not the particular HALTED cause, schema, change id, age, or temporary path in the incident. A new exception for `stopped-not-initiated` is not an acceptable implementation of this spec.

Preserve the real exclusion guarantee: two executions cannot run concurrently in one worktree, an active workflow owns its worktree between sequential tests, and an old fenced execution cannot launch after a replacement is admitted. Genuine unresolved obligations of the affected worktree/run must be accounted for, with an exact locator and applicable recovery action.

Automatic finalize queue ordering and its stop/continue policy are explicitly excluded. Shared repository/configuration/transport failures, defects in B's own runtime records, and genuine ownership conflicts remain valid refusals; a gate-history defect attributed only to independent A does not.

## Grounding and prior work

Inspected main `442770e11bd05baf6200bec53e9c4757622811a6` and metadata revision `d1c7776dde985f679d1f45b97186a9079f8ca3fb` on 2026-09-23. The original change record at that metadata revision preserves the incident, quarantine locator, and manually edited epoch/backup details. The explicit-ID preflight/metadata revision was checked against the same main commit and published spec at metadata `c31420b4f36d52d14cdb1842ad02f4eb6d14fbda`. No live runtime records were changed during grooming. A read-only review on 2026-09-23 re-verified every grounding claim below against the same main commit and corrected those the code contradicted; the corrections are folded into the bullets and design sections rather than listed separately.

- **#0375 and ADR-0118:** the original spec's compatibility section says to inventory existing drives **“for that canonical worktree.”** It also requires quiescing old executors during a protocol upgrade; it does not promise that old binaries honor new fences. The current `reserveWorktreeExecution` instead calls a repository-wide `inventoryLegacyDrives` on every absent worktree slot. Any retained record vetoes admission. Every newly allocated feature worktree encounters this condition.
- **#0428 and ADR-0120:** `loadHistoricalDrive` separates historical assessment from executable compatibility, and `classifyLegacyDrive` lets PASSED/FAILED bypass removed paths. Its remaining rule treats an unresolvable historical path as possible ownership of every requested worktree. An unreadable record (retained by `inventoryLegacyDrives` before the classifier runs) or a HALTED record without temporary evidence can therefore block unrelated work indefinitely. A HALTED record whose run directory was deleted is retained too: `process.ClassifyRun` reports a missing directory as `foreign`, which falls to the retained branch. Keep the historical reader and manual cleanup; correct their authority boundary.
- **#0437:** `accountEpochLaunches` reads every drive, reports unreadable records or lost linkage against every epoch it accounts (cancellation, terminal repair, resume, and success closeout), and reaches `reconcileEpochDrive`'s existing terminal shortcut only after linkage resolution. That shortcut is `isTerminalOutcome`, which is PASSED, FAILED, **or HALTED**; it treats the drive axis as settled because slot/participant reconciliation accounts for teardown separately. The census reads through `store.Load` (schemas 3 and 4 only), so a supported historical schema-2 record appears as `record-unreadable` here. Its `worktreeRoot` argument filters nothing, and an empty `worktreeRoot` returns "accounted" without checking anything. `resolveDriveEpoch` is appropriate for authorizing a particular drive's new execution; using every failure of that predicate as an obligation of every epoch is not. Scopeless linkage depends on a slot token that legitimate later executions replace. The review fix intentionally protected genuinely attributable pending launches; preserve that protection without its global inference.
- **#0435:** `runCancel`, `repairTerminalEpoch`, and `validateResumeQuiescence` share launch accounting: `runCancel` through `reconcileEpochTeardown`, the other two through `verifyTerminalEpochQuiescence`, all ending in `ReconcileEpochLaunches`. They do **not** share retirement code. `runCancel` uses `retireWorktreeSlotOwnership`; `repairTerminalEpoch` and `validateResumeQuiescence` each carry an inline copy (classify, check released, `seams.retireSlot`, re-read once) whose re-read outcome differs (`runCancel` reports `slot-replaced-by-successor`; the copies return already-cancelled or neutral). The shared primitives are `seams.retireSlot` → `RetireWorktreeExecutionEpoch` and `classifySlotOwnership`. Repair the shared accounting inputs and apply the retirement correction to all three sites—preferably by consolidating the inline copies onto `retireWorktreeSlotOwnership`—with one defined outcome when a successor holds the slot. Keep exact-token/epoch retirement and protection of a successor's slot. A superseded epoch has an empty `Worktree` (see owner selection below), so today `validateResumeQuiescence` on the superseded branch proves nothing about launches and skips the slot check.
- **#0441 and ADR-0124:** success uses the same launch census in observation-only mode, then retires the released slot. Consequently unrelated historical findings can also prevent success closeout and block finalize. `accountCompletionSlot` additionally reopens temporary run evidence even after the matching slot records released state. Preserve durable completion facts instead of allowing scratch cleanup to undo them.
- **#0439:** a completed raw launch intentionally retains its slot until stop under the current contract; only `releaseRawSlotForStop`, called by `GateStop`, releases it. Several checks can refuse before authoritative admission is reached: `WorktreeAdmissionRefusal` (called by `startBudgetedBuild` before `suiteBudgetPrecheck` and `engine.Admit`), `rawStaleEpochRefusal` (called by `GateLaunch` before `ReserveRawWorktreeExecution`), and, inside `Driver.Admit` before reserve, the command/budget checks, `precheckScopedStart`, `ComputeFingerprint`, and the `epochGated` liveness gate. This list was found by reading, not proven exhaustive; implementation must derive the pre-reserve refusal population by search. A repair only inside the final reserve function would be unreachable for those callers. The revised scope explicitly includes bounded reconciliation of already-finished incumbents through the common admission path, including raw runs.
- **Owner selection:** `findEpochByWorktree` walks `os.ReadDir` order, returns the first matching epoch, and excludes only completed epochs. A *superseded* predecessor cannot mask its replacement: `SupersedeCancelledEpoch` clears `rec.Worktree` ("a superseded epoch no longer owns the worktree fence"), and `armResumeReplacement` supersedes before binding the replacement. The real masking case is a **cancelled or cancelling epoch that was never superseded** beside a fresh (non-resume) run that `bindEpochWorktree` binds to the same path. The effect is over-fencing: the live run's mutations are refused with `ErrRunCancelled`. It is not a safety hole. `FindEpochByChange` already resolves replacement chains (prefers the single non-superseded match, reports `ErrEpochAmbiguous` for several, walks `ReplacementReserved` to the chain tail) rather than relying on directory order. Extend that established approach to worktree ownership; do not add an owner registry.
- **Path fence versus epoch fences:** `findEpochByWorktree`'s only production caller is `admitWorkflowMutation(repoDir, op)`, reached from the engine `MutationAdmissionHook`, `pr.publish`, and `workspace.publish`. It receives no epoch id: it asks "who owns this path now," not "is the caller's epoch still valid." Explicit-epoch fences are separate and unaffected by owner selection: `epochLaunchGate` (by id; `ErrRunCancelled`/`ErrStaleRunEpoch`/`ErrRunCompleted` plus `epochOwnsWorktree`), `epochRevokedResolver` (takeover), gate-drive admission's `RunEpochID` check (`ErrStaleRunEpoch`), and keyed operations checking `expectEpoch` (cancel, completion, `RegisterEpochParticipant`, `FenceEpochCompleting`).
- **Existing scanners are not a precedent for required-record strictness:** `findEpochByWorktree`, `scanEpochsByID`, and `FindScopeDriveIDs` all skip **every** unreadable record—including I/O errors, not only absent or corrupt files—and fail only when the root directory cannot be listed. Fail-closed reads of a required record happen later (`epochLaunchGate` and `epochCAS` re-read under lock). Consequently the path fence fails **open** today when the owning epoch's own record cannot be read (`!found` → no-op journal), and `admitWorkflowMutation` also admits unfenced when the caller's `repoDir` cannot be canonicalized. `epochOwnsWorktree` checks `stored == canon` and then `canonicalWorktree(stored) == canon`; an `EvalSymlinks` failure returns false, so a deleted stored path matches nothing.
- **Admission slot facts:** the slot never records its drive: `admissionRecord.DriveID` is never set in production code, so the "preserves DriveID" comments and `incumbentRemedyMessage`'s `DriveID` branch are vacuous. The only drive↔slot link is `ReservationToken == AdmissionToken`, and the next reserve or rotate replaces that token. `ReleaseWorktreeExecution` verifies only the token, not the prior state. `RunEpochID` survives release and is cleared only by `RetireWorktreeExecutionEpoch`. Slots are never deleted, so "absent" means this canonical path never held a successful reservation. The slot key is derived through `filepath.EvalSymlinks`, so a slot whose worktree path was removed cannot be addressed by re-canonicalizing that path, while a path recreated at the same location inherits the old slot, skips the legacy inventory, and can hit `ErrStaleRunEpoch`. `releaseAdmissionIfProven`'s comment that scopeless drives carry no admission token is stale; `admitScopeless` sets one.
- **Incident evidence:** the preserved schema-4 HALTED drive for #0368 has one stopped attempt and deleted runtime evidence. Separately, a read-only census of the present registry found 623 PASSED/FAILED records; obsolete linkage explains the reported cancellation failures, but grooming did not replay cancellation. #0444 remains the blocked consumer, with its independent publication-journal fix and uncommitted work preserved.
- **Moved grounding:** the maintenance-preflight grounding (#0389/ADR-0101, #0397/ADR-0106) now lives in 0448's spec. The metadata-transaction, ADR-0093, board-renderer, and branch-fact grounding (#0309/#0310/#0312, change 0337) now lives in 0449's spec.

Relevant learnings: verify the root-cause hypothesis against code; do not build recovery machinery around an unnecessarily broad policy; do not mistake missing evidence for a fact about some unrelated subject; test the actual producer/consumer path and the negative counterpart; printed remedies must work in the state that produced them.

## Design

### 1. Establish relevance before deciding whether uncertainty blocks

Use the existing canonical worktree identity, execution slot, scope/epoch links, reservation tokens, and registered participants. Reuse/extract their matching predicates; do not create a new registry, identity format, liveness probe, or policy framework.

There are two different reads:

- **Discovery:** a historical record is encountered while looking for ownership. It does not become an obligation merely because it is unreadable, has an unsupported schema, refers to a deleted worktree, or has lost its old linkage. Unmatched history can produce bounded diagnostics in the existing findings/cleanup surface, but cannot refuse another worktree or run.
- **Required evidence:** an authoritative record for the requested worktree/run names an execution, drive, scope, or reservation. Failure to read or settle that exact obligation remains a local blocker. Missing a named current record must never be mistaken for an empty slot.

A nonempty, validated stored worktree identity matching the requested canonical root, or an existing resolvable alias of it, establishes same-worktree relevance. Failure to resolve some other historical path does not establish a match. Exact epoch linkage establishes run relevance. Current slot/scope references remain evidence of relevance even when the referenced document cannot be loaded. Reject contradictory identities on a positively identified current obligation; never spread that refusal to unrelated worktrees.

The existing scanners do not yet implement the "required evidence" half of this rule (see grounding): they skip every unreadable record, so a required record that cannot be read currently looks like no record. Apply the rule to them through positive references, not by failing closed on every unreadable sibling (which would recreate the global veto). In particular, when the requested worktree's slot names a `RunEpochID`, ambient owner lookup for that worktree must resolve that epoch to a readable record; if no readable epoch record carries it, the worktree's current owner is unresolved and the path fence refuses locally with that locator instead of admitting unfenced. An unreadable epoch or drive record that no current reference names stays diagnostic. Resolving a positive reference must never depend on reading the unreadable record itself: the reference comes from the current slot or scope, and failure to satisfy it is the blocking fact.

**Accepted residual risks.** An unreadable pre-slot legacy drive that really belongs to the requested worktree is not treated as a blocker, because nothing current names it. This is safe only under #0375/ADR-0118's upgrade contract that old executors are quiesced before the new protocol runs; this spec relies on that contract rather than inferring ownership from an unreadable record. Likewise, an unreadable epoch that has bound a worktree but not yet reserved a slot is invisible to the path fence; its epoch-carrying operations remain fenced because `epochLaunchGate` and `epochCAS` re-read by id and fail closed. The path fence also fails open when the caller's own `repoDir` cannot be canonicalized; keep that behavior unless implementation shows a named caller relies on it, and record the decision.

### 2. Make admission depend on the requested worktree

Keep the existing admission slot as the authority for modern executions. An absent slot triggers only the narrowly scoped pre-slot compatibility assessment: supported historical records positively bound to this worktree that have no applicable modern admission record. Modern historical drives do not constitute a second admission authority after their reservation has been released or replaced.

A matched pre-slot execution that could still run in this worktree retains the existing process assessment and upgrade/quiescence contract. Other worktrees' records, unmatchable damaged records, stray registry entries, unknown historical schemas without a current reference, and absent old scratch roots cannot veto admission. A failure of historical discovery alone is diagnostic, not evidence that every worktree is occupied; errors accessing this worktree's authoritative slot remain errors. Never infer safety for a named current obligation from a failed read.

A released slot is evidence that **its latest execution** was released—no more. The slot does not record a drive id, and release checks only the token, not the prior state. So "released" is exactly as trustworthy as the callers allowed to write it: every writer that releases a slot (existing callers and the §3 reconciliation) must hold positive teardown or never-launched proof before calling `ReleaseWorktreeExecution`. Do not add a state check to the store primitive as a substitute for caller proof. The slot's ScopeID, RawRunDir, and overwritten predecessor tokens are not fresh obligations. Its surviving `RunEpochID` is not a live owner either: a released slot whose `RunEpochID` names an epoch that is completed, or cancelled with confirmed accounting, is settled through the existing exact-token retirement (§5), not refused with `ErrStaleRunEpoch`. This covers a worktree recreated at a previously used path, which re-canonicalizes to the old slot and skips the legacy inventory. Preserve live epoch ownership between drives until its existing cancellation/success closeout completes. Do not recover every old drive before allocating a new worktree.

The slot key is derived by `EvalSymlinks`, so a removed worktree's slot cannot be found by re-canonicalizing its missing path. Cancellation, retirement, and completion of an epoch whose worktree was removed must address the slot through the canonical identity already stored on the epoch/slot rather than re-canonicalizing; a canonicalization failure on a missing path is not proof that the slot is absent or released. This reuses existing stored identities and adds no schema.

Keep `gate.history.cleanup` as the explicit repository-wide assessment surface. It passes no requested worktree, so its retained set is a superset of any single worktree's concerns. It may honestly retain damaged history without implying that every retained record blocks a start. Correct the current shared comments/reporting that equate cleanup's repository-wide retained count with worktree admission refusal: at least the legacy-inventory refusal message ("historical gate drives block this admission; inspect or recover them with docket gate history cleanup") and the `HistoryCleanupOutcome` doc ("Retained > 0 means blockers remain"), found by search rather than taken as this list. No new cleanup command, forced abandonment marker, retirement receipt, or registry mutation is needed.

### 3. Reconcile a finished incumbent at the normal admission boundary

A slot may still say executing/stopping/unresolved because the process finished before its owner persisted release. Before refusing a fresh start, the existing admission flow must inspect the exact incumbent and settle only facts the existing machinery can prove, then continue that same admission. This is a bounded synchronous reconciliation of one slot, not a retry controller or background sweeper.

Reuse the current drive outcome, process observation/recovery, reservation resolution, release, and epoch-retirement helpers. A matching PASSED/FAILED drive is already authoritative completion evidence. For other incumbent shapes, require the existing positive teardown or never-launched proof. A bare HALTED label, absent file, expired timestamp, free lock, or changed process id is insufficient. Do not introduce a cause-name allowlist.

Protect against pending launch and relaunch: acquire the existing nonblocking claimant where applicable, validate the exact reservation, and retain exclusion while any already-admitted launcher can still act. In particular, `never-launched` is not enough if a delayed ticket retains launch authority. Settle that authority through the existing fenced/terminal path before releasing its slot. An active or unprovable incumbent is not stopped merely because another caller wants to start; use the existing explicit cancellation/stop authority.

Probe outside outer locks; apply changes under existing CAS/lock ordering with the expected reservation and epoch. If a successor wins meanwhile, leave it untouched and report/re-evaluate that actual incumbent once. Do not clear a slot using a newly observed successor token. A failed release/retirement write cannot be reported as successful admission.

Use one behavior for task, build, finalize, scoped/scopeless, and participating raw starts. Adjust or remove early short-circuits that would refuse before this reconciliation runs. Known sites include `startBudgetedBuild`'s `WorktreeAdmissionRefusal` call, `GateLaunch`'s `rawStaleEpochRefusal`, and `Driver.Admit`'s pre-reserve checks (`precheckScopedStart`, the `epochGated` liveness gate, command/budget checks). Derive the complete set by searching the callers of the admission/reserve functions; do not treat this list as exhaustive. Checks that validate the request itself (bad command, budget, fingerprint) may keep refusing first, since they do not depend on incumbent state. Admission still precedes suite charging, and a refused start launches nothing and consumes no suite attempt.

For a completed raw run, positive existing terminal/teardown proof is sufficient for normal admission to persist release; another manual stop should not be required solely to update bookkeeping. This deliberately narrows ADR-0118's explicit-stop-only rule for raw-slot release. It grants no automatic signalling authority and does not release an active workflow's between-drive epoch ownership.

### 4. Account for the target epoch's obligations, not all history

Retain one shared inventory for `ReconcileEpochLaunches` and `ObserveEpochLaunches`. First identify obligations using the target worktree's slot, scopes linked to the target epoch, exact reservation matches, and existing participant links supplied by the app layer where relevant. Follow current/pending scope references so a corrupt or missing named drive is still detected. Supported readable drives with positive epoch linkage remain candidates, including pending/replacement launches that have not yet appeared in participants or the slot's raw-run field.

Existing records suffice: scopes carry RunEpochID and current/pending drive ids; slots carry epoch/scope/reservation identity; drive records carry scope and admission/relaunch reservations. Enumerating existing stores for matching references is allowed. Adding a global reverse index or another persistent per-run journal is not needed.

Apply the existing terminal-drive shortcut before obsolete linkage lookups. That shortcut is `isTerminalOutcome` and includes HALTED. This is consistent with "no blanket trust in HALTED" only because it settles the **drive record's launch axis**, not the execution: teardown is still accounted by the slot and participant checks, which require positive proof (§3, §5). HALTED must therefore never be used as slot-release proof, completion evidence, or a substitute for those checks. If implementation finds a HALTED drive whose slot/participant accounting does not cover its process, that drive stays a candidate rather than being settled by the shortcut. For nonterminal candidates, retain the claimant, delayed-ticket, exact-reservation, and replacement-process checks from #0437. A corrupt required drive, a corrupt scope named by this epoch's current slot, or an unresolved pending reservation must still keep that epoch unaccounted. Strengthen #0437's lost-linkage regression to create a real admitted drive and retain its independent current-slot reference before corrupting the scope; a hand-seeded orphan with no surviving ownership association cannot justify a repository-wide veto. An unlinked orphan elsewhere is an informational historical finding, not `linkage-unresolved` for every epoch. The same rule holds for old runs on a reused worktree when current ownership references establish a different epoch/reservation.

Because the slot records no drive id and each reserve or rotate replaces its token, a positive current-slot reference reaches only the drive whose `AdmissionToken` equals the slot's current `ReservationToken`. An older nonterminal scopeless drive whose token no longer matches has lost launch authority: its late release or launch fails `verifyAdmissionToken`. It is therefore historical for census purposes, and the current token holder carries any live obligation. This holds only if every launch/relaunch path verifies the token. Confirm that by searching the launch paths; any path that launches without that check keeps such drives as candidates. Scoped drives remain reachable through their scope's `RunEpochID` and current/pending drive ids regardless of token rotation.

An empty worktree identity is not proof of quiescence. `accountEpochLaunches` must not return "accounted" merely because `worktreeRoot` is empty. A superseded epoch has an empty `Worktree`, but its scope-linked drives are still enumerable by `RunEpochID`, and they must be accounted. Only the slot check needs a worktree. For a superseded epoch, that check uses the replacement's worktree slot to confirm the predecessor's token and epoch no longer hold it.

Do not relax `resolveDriveEpoch` when it authorizes a particular drive to launch/relaunch. Losing that drive's ownership proof still refuses its execution. The correction is to census attribution, not to launch authority.

Cancellation continues stopping only its own processes. Success closeout remains observation-only. Resume and terminal repair consume the corrected shared accounting, with no copied exception lists. Their retirement step gets the same correction as `runCancel`'s (preferably by consolidating onto `retireWorktreeSlotOwnership`) and reports one defined outcome when a successor holds the slot. The existing mutation journal and #0444's uncertain-publication semantics remain intact for genuinely owned unfinished effects.

### 5. Keep current ownership and settled facts stable

Replace first-directory-match ownership selection with deterministic resolution from the existing replacement chain and exact worktree/claim/slot bindings, following `FindEpochByChange`'s established shape. The defect being fixed is a cancelled or cancelling epoch that was never superseded, sorting ahead of a fresh run bound to the same path and wrongly fencing it. Superseded epochs already cannot match because supersession clears their worktree. Selection rules:

- A single active (or completing) owner is the owner, regardless of directory order or of cancelled/cancelling records for the same path.
- With no active owner, a cancelled or cancelling non-superseded epoch bound to the path is still returned, so the existing `ErrRunCancelled` mutation fence keeps holding until the recovery/replacement workflow authorizes current work. Simply dropping all terminal epochs from the lookup is not a substitute.
- A completed run is not an ambient owner.
- Two or more active owners for one canonical path are an ambiguous contradiction and refuse locally, as `ErrEpochAmbiguous` does for changes. Do not choose by timestamp or order, and do not silently ignore one.
- A slot-named `RunEpochID` that resolves to no readable epoch refuses locally (§1).

Keep the distinction between ambient lookup and an explicit old identity, and be exact about where each is enforced. Operations that **carry an epoch**—launch (`epochLaunchGate`), takeover (`epochRevokedResolver`), slot admission, and keyed cancel/completion/participant/fence operations—must still refuse a cancelled, superseded, or completed epoch even when a new owner uses the same path. That guarantee lives in those epoch fences and is unchanged by this section. The path-based fence (`admitWorkflowMutation`, used by engine mutations, `pr.publish`, and `workspace.publish`) receives no epoch. It answers only "who owns this worktree now," so it cannot refuse a stale caller once a verified replacement is the current owner. Such a caller was only refused today when the cancelled record happened to sort first, which was never a real guarantee. This spec does not claim that guarantee for the path fence and adds no epoch input to it. Threading the caller's epoch into these three boundaries is a possible follow-up, not part of this change.

Reconcile a stale RunEpochID on a released slot using existing exact-token retirement and the epoch's applicable completion/cancellation accounting. Do not ask a user to cancel a successfully completed run. A genuinely active, cancelling, or completing owner with outstanding obligations continues to hold the worktree.

A durably released execution, completed epoch, or confirmed terminal observation must not become unknown merely because optional scratch evidence later disappears. Consumers must use the sufficient exact durable proof already recorded before reopening ephemeral evidence. In particular, remove redundant process re-observation of a matching released slot in completion accounting; this proves that slot's execution only, not unaccounted participants or launches. For execution-participant checks, accept an exact matching existing released-slot or persisted PASSED/FAILED drive record as the same sufficient execution proof; preserve native participants' separate recorded terminal-status requirement. Do not synthesize terminal evidence from missing files or add process observations to native turn fields. Never accept a HALTED drive as that execution proof (§4). Keep genuinely pending journals and contradictory current references blocking. Do not add a new receipt store.

Audit the existing terminal-result and cleanup paths as part of this change. `Driver.Advance` currently discards release errors (`_ = d.releaseAdmissionIfProven(rec)`, and `driveAndPersistClaim` does the same twice), and `recordedDoc` exposes RunRoot for every terminal outcome, including HALTED, even when the slot was only marked stopping/unresolved. Correct `releaseAdmissionIfProven`'s stale comment that scopeless drives carry no admission token. Do not make a still-required process record disposable merely because the drive has stopped advancing. Persist/check sufficient release or completion evidence before advertising/removing its temporary root; retain evidence and surface a local persistence/teardown finding if that step failed. Finalize's `mapDriveOutcome` cleanup (a deferred `removeGateRunRoot` on any terminal outcome) and its caller's removal of the root when Start fails without a drive document must obey the same ordering. This extends existing release and cleanup calls, not the storage topology. Test interruption at this boundary so normal cleanup cannot recreate the reported time bomb.

### 6. Diagnostics and recovery must preserve progress

Each admission refusal must identify the affected worktree/current owner or an exact matched legacy execution, the unresolved obligation, and the applicable existing continuation, cancellation, stop, or repair action. Today `LegacyFinding` carries only `{DriveID, Class, Reason}` and never the matched record's worktree path. The raw path also discards the legacy summary entirely (`ReserveRawWorktreeExecution` drops it, and the refusal carries `unresolved-execution` with an empty cause). Carry the matched worktree identity in the in-memory diagnostic and surface it on raw refusals too. This is a report field, not a persisted schema. Do not present historical cleanup as a mandatory precondition for unrelated work or claim repeating an operation can recreate lost evidence.

History-only findings remain inspectable but do not change the operation's success disposition. Repeated cancellation, completion, and admission after safe reconciliation converge using existing operations; no manual edits or deletion of `.git/docket` records are part of the design.

### 7. Moved sections

The former §7 (named implementation skips unrelated maintenance) is change 0448. The former §8 (scoped metadata validation, bounded branch facts, and tolerant generated views) is change 0449.

## Acceptance tests: the class of failure

The implementation is incomplete if it merely makes the quarantined record nonblocking. Derive the caller and registry-reader population from repository searches; do not test only a handpicked entry point.

1. **History isolation matrix:** seed old PASSED, FAILED, HALTED (several causes), WAITING, missing-scope, mismatched-token, missing/empty-run-directory, removed-worktree, unsupported-schema, malformed-record, and stray-entry history. With no ownership connection to the target, new task/build/finalize drives and participating raw starts succeed. Repeat with many mixed records, different orderings, and before/after deleting scratch files. No historical record is rewritten or deleted.
2. **Same-worktree generations:** after proven release or replacement, old records for the same path cannot block the next permitted drive, cancellation, resume, or finalize. Cover delete/recreate of an old worktree path with a properly released slot whose `RunEpochID` still names the completed/cancelled predecessor, symlink aliases, several superseded epochs, and terminal records whose ownership links no longer resolve. Cover cancellation, retirement, and completion of an epoch whose worktree directory was removed: the slot must be reached through its stored identity, not re-canonicalization. Also prove a genuinely live incumbent at that same canonical path still blocks.
3. **Targeted corruption:** corrupt an unrelated record and the operation still succeeds. Corrupt the exact drive/scope/slot named by current ownership and that worktree/run refuses with the right locator. Corrupt (and separately make I/O-unreadable) the epoch record named by the worktree slot's `RunEpochID`: the path fence refuses locally instead of admitting unfenced. The same corruption of an unreferenced epoch record does not affect any worktree. The companion unrelated worktree continues. Preserve positive-reference coverage across reserved-before-launch and replacement-before-attach windows.
4. **Finished incumbent:** leave a proven-finished driven or raw execution's slot occupied by interrupting release. The next normal admission settles it and starts once, without a manual stop or second start. Cover lost final epoch retirement and completed-run-to-finalize. Busy claimant, live process, unresolved establishment, delayed ticket, pending relaunch, and failed release write remain safe refusals. A HALTED drive with no positive teardown proof never releases its slot. Admission sends no stop signal to make room. Exercise each derived pre-reserve refusal site (including `Driver.Admit`'s liveness/precheck gates) with a finished incumbent to prove none bypasses reconciliation.
5. **Real cancellation/resume flow:** with unrelated damaged/obsolete history present, cancel an otherwise quiescent epoch, then arm exactly one replacement and start its gate. Cover both cancelled and superseded resume branches, interrupted retirement, repeated cancellation, and new history added between cancellation and resume. On the superseded branch (empty `Worktree`), add an unaccounted scope-linked drive for the predecessor epoch and prove resume refuses rather than reporting "accounted." Drive all three retirement sites (`runCancel`, `repairTerminalEpoch`, `validateResumeQuiescence`) through a successor-holds-slot case and assert one consistent outcome. Owned pending mutations, processes, or launch reservations still prevent premature completion.
6. **Real implementation-to-finalize flow:** complete a run, close out ownership, remove its optional scratch files, then start finalize on the same feature worktree. Include a cancelled, never-superseded predecessor bound to the same path whose directory sorts first (a superseded one cannot match, because supersession clears its worktree), plus unrelated unaccountable history. Add the owner-selection matrix: sole cancelled owner still fences (`ErrRunCancelled`); active owner wins over cancelled/cancelling regardless of order; two active owners refuse as ambiguous; completed is never the owner. Separately, prove the epoch-carrying fences still refuse a stale cancelled/superseded/completed epoch after a new owner binds the path. Use the production shared census/admission paths, not permissive fake accounting that bypasses them. This is an acceptance requirement alongside fresh-worktree admission.
7. **Concurrency and fencing:** race same-worktree starts across owners/scopes/raw and symlink paths and get one launch; allow independent worktrees to progress. Race reconciliation with successor reservation, cancellation, delayed StartAdmitted, and automatic/recovered relaunch. Old tokens never release a successor, old epochs never regain launch authority, and pre-admitted work cannot first appear after cancellation is reported complete.
8. **Mutation checks:** restore the global historical veto, restore linkage-before-terminal ordering, remove relevance checks, restore first-match epoch selection, or bypass the incumbent reconciliation from an advisory precheck; each relevant behavioral test must fail. Also: restore the empty-worktree "accounted" shortcut, restore skip-on-unreadable for a slot-named epoch, drop the cancelled-sole-owner fence, or accept HALTED as slot-release proof; each safety test must fail. Separately remove current-ownership/claim/token checks and prove the safety countertests fail. Update old tests that enshrined repository-wide blockage only alongside these stronger paired tests.
9. **Build gate:** run the whole suite via the source Go runner using resolved `build.test_command`, and inspect budget findings. Use existing fixtures and process seams; no new test framework. Grooming claims no passing implementation tests.

The sanitized quarantine fixture at `/Users/homer/dev/docket-quarantine/gate-drives/b66ce1405cd813e5519c344360f61bd4/record.json` is one matrix case. Copy it into an isolated fixture repository, never into the live registry. Preserve the original and epoch backup; the manually edited live cancelled state is not a test oracle.

## Final design logic check

Checked against production callers and prior contracts on 2026-09-23. This is a design review, not implementation-test evidence. For this change the boundary is execution and run closeout:

| Boundary for named B | Why unrelated A cannot stop it | What must still stop B |
|---|---|---|
| Gate admission (task, build, finalize, scoped/scopeless, raw) | Relevance is established from B's worktree slot, scope/epoch links, and exact tokens; historical discovery is diagnostic only. | Live/unproven incumbent, corrupt record named by B's current slot/scope, failed slot access, or a genuine same-worktree owner. |
| Cancellation, resume, terminal repair | Census counts only obligations linked to the target epoch; terminal drives are settled before linkage lookups. | Owned pending mutation/launch, corrupt required ownership evidence, unresolved pending reservation. |
| Success closeout and entry into finalize | Durable release/completion facts are not reopened after scratch cleanup; owner selection is deterministic. | Failed release persistence, unaccounted participants, or an active owner with outstanding obligations. |

The full cross-change boundary table (startup, metadata writes, generated views, finalization through archive) lives in 0449's spec, which lands last.

The 2026-09-23 code review surfaced issues that this revision resolves in the design text: the real owner-masking case and the path fence's missing epoch input (§5), fail-open scanners (§1), HALTED in the terminal shortcut (§4), the slot's missing drive id and token-only release (§2, §4), removed-worktree slot addressing (§2), empty-worktree accounting (§4), and divergent retirement copies (§4). Two items are **accepted limits, not solved problems**: unreadable unreferenced legacy/epoch records rely on #0375's upgrade quiescence and on the epoch-carrying fences (§1); and the path fence does not refuse an epoch-less stale caller after a verified replacement (§5). Implementation review must verify the derived caller population and execute the tests before claiming the running binary meets the guarantee.

## Architecture and scope limits

Record the changed authority boundaries through the ADR workflow during implementation: historical discovery has no global veto; authoritative local uncertainty remains protected; positive teardown can settle a finished raw incumbent during admission. Supersede the conflicting clauses of ADR-0120/ADR-0118 through the ADR workflow, preserving their other guarantees and ADR-0087/0095 process evidence rules. ADR-0124's observation-only successful closeout remains binding. The ADR-0101/ADR-0106 startup-preflight narrowing belongs to 0448, and the #0309 scoped-validation departure belongs to 0449. Keep `gate.history.cleanup` as the repository-wide assessment surface. Accepted ADR text is not rewritten in place.

No new commands, force flags, daemon, background cleanup, registry, persistent schema, lease/TTL, age cutoff, cross-store transaction, configuration, retry allowance, or second liveness implementation. No blanket trust in HALTED, no dropping positively owned pending work, no automatic cancellation to make room, and no trusting change status as process-death evidence. No storage relocation or general garbage collection. This corrects the existing ownership/admission/accounting paths only; named-workflow startup is 0448, and metadata validation/rendering is 0449. Automatic finalize candidate ordering, head-of-queue recovery, and driver stop/continue behavior are excluded by the user; do not make queue-policy changes under this spec.

No dependencies or stack base. Related design history: 368, 375, 428, 435, 437, 439, 441, 444; follow-on changes 0448 and 0449 carry the startup and metadata history. Discovered from 444. ADR references: 87, 95, 118, 120, 124, which match the change record's `adrs:` field.

After implementation merges and the installed binary is rebuilt, verify #0444's recovery through its existing blocked/halted lifecycle and gate workflow, preserving uncommitted work. The change is currently blocked; its bare resume-arm command is insufficient until the owning workflow restores the required lifecycle state. Grooming does not implement or resume 444.
