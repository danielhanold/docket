<!-- docket:backlink:start (generated — do not hand-edit) -->
> ↩ **[Change 0423 — Certify native Multi-Agent V2 orchestration through Docket ImplementNext](https://github.com/danielhanold/docket/blob/docket/docs/changes/archive/2026-09-14-0423-certify-native-multi-agent-v2-orchestration-through-docket-i.md)**
<!-- docket:backlink:end -->

# Certify native Multi-Agent V2 orchestration through Docket ImplementNext

## Decision and purpose

Certify a bounded native Codex ImplementNext → actual planner → actual standard worker flow under the user-approved option 2. Feature children may inherit the primary checkout as startup cwd; before repository work they validate the assigned registered feature worktree and then explicitly target every feature operation there. This is an observable instruction and validation contract, not native startup placement or hard filesystem isolation.

2026-09-14 reconciliation: the user accepted option 2, ended Luna/control testing, and authorized a manual POC/evidence completion path for 423. These decisions replace the earlier startup-equality and paired V1-control acceptance requirements. No implementation of production routing is delivered here; that belongs to 425, with model-capability policy in 424.

## Existing context

Historical failures and successful partial runs remain immutable. The earlier binding2 run established a continuous chain but had handoff/audit gaps. The checked focused worker fixture passed on standard Docket v0.9.3-1016-g06ebb52c (06ebb52c058894b564ac2a8432922ddf4b2d56b3): one native standard worker, genuine baseline/RED/GREEN, one task commit, durable scope acknowledgement, clean final full suite, unchanged primary, typed halt. It used an imported plan and does not alone establish the final continuous case.

Known corrections to carry forward: complete planner payload with actual selected skill; exact Python entry argv and executable mode checks; complete real child capability with private parent capability; independent worker catalog; no worker repository.prepare; fixed feature paths and test argv reloaded for every test; clean committed final gate; collect terminal work and preserve normal ownership. Source of each conclusion and unavailable child telemetry must be distinguished.

## Deliverables and isolation

Deliver a self-contained reusable fixture package, operator runbook, deterministic helper and evidence-validator tests including mutations, sanitized prior evidence index, and final acceptance report under docs/codex/fixtures/native-implement-next on the metadata branch. The POC package may include executable setup/validation helpers and pinned experimental role/skill snapshots; none is installed as production routing. No Docket build workflow is required to deliver this manually conducted experiment. Run package checks directly and retain their receipts; do not call them production-suite or live-certification results.

Each execution uses a fresh disposable primary repository and its own local bare origin, exactly one proposed unclaimed nontrivial candidate, no feature worktree and no plan at launch. Preparation cannot preclaim, import a plan or start agents/scopes. Pin exact binary/role/skill/helper hashes and modes. No previous fixture, production source, remote code branch, global registration or Codex harness is changed by test execution.

## Native topology and worktree feasibility

A fresh local parent starts at the fixture primary checkout on Terra/low and natively dispatches the registered docket-implement-next coordinator on Terra/low. That same coordinator creates the feature worktree and natively dispatches docket-plan-writer (Sol/medium), then docket-build-standard (Terra/medium), sequentially with fresh child contexts. These are tested exact model assignments, not a family-name capability rule. Retain actual runtime evidence where exposed and distinguish configuration from observed child runtime.

For each feature child the first shell call uses login=false and no cwd override to honestly record startup. A read-only checker validates canonical distinct registered feature root, branch, current clean HEAD, change identity and workspace creation receipt. Inherited primary cwd is allowed. All subsequent feature shell commands set feature workdir; file and skill paths are absolute within the verified feature root. Role-authorized synchronized metadata reads remain allowed. Feature source, plan and results never land in primary. Necessary .git/.docket/.worktrees administration and named external gate/evidence files are explicit exceptions. Snapshot integrity does not establish absence of transient restored writes; path evidence and gaps must be reported.

No agent.enter under any alias, subprocess agent runner, App Server root promotion, generic substitute, root relocation, custom transport/tool wrapper or host patch. Docket test drivers are allowed. This POC does not certify parallel writers.

## Positive execution and bounded checkpoint

Arm the existing run.gate-before facade once; retain the exact key and dispatch-context. Pass the unchanged context into coordinator claim and every scope/start that requires it. The same coordinator claims, reconciles, creates the new worktree, sends the complete validated planner payload, verifies the new plan commit/trailer/backlink and attaches it before the worker is dispatched. The planner authors exactly one meaningful standard-profile task. No setup or parent-authored plan is permitted.

The coordinator invokes the actual docket-build skill. Before scope creation, construct immutable worker inputs from the actual new attached plan, feature identity, current HEAD and configured test. Validate the exact entry command. Mint one normal scope, retain the parent capability privately, and produce a complete native worker message preserving the actual child capability and outer gate context. The worker independently loads its catalog and fixed inputs, runs baseline/assertion-RED/GREEN through the normal scoped task driver with predecessor receipts, commits only the two source/test files, acknowledges the final drive, and returns its structured receipt. No worker metadata mutation or coordinator implementation is allowed. A WAITING or blocked result is settled through the existing ownership contract; no replacement test or child.

Only after a verified successful worker commit and clean feature tree does the coordinator run the configured full build suite and record exact-HEAD evidence. Write/commit/push/attach the normal bounded results checkpoint through the existing artifact contract. If results change HEAD, preserve the implementation-test and checkpoint commits separately and run the configured gate on the final committed checkpoint when required by the real results contract; never label prior evidence as testing a later commit. Deliberately halt before review, PR or merge. The parent collects actual terminal coordinator output before the exact keyed gate verdict; gate-stop/run-halted is the expected bounded endpoint, not certification by itself.

## V1 control and verdicts

No additional Luna or matched V1-control run is required or authorized. Preserve prior observations and capability provenance, but make no stronger matched-causal claim than those records support. This change now establishes the positive workflow under the selected models and approved option-2 boundary.

Verdicts: continuous-functional-passed means all workflow stages, actual native role lineage, fresh-plan dependency, credential use/acknowledgement, exact commit/gate receipts, primary integrity and bounded terminal gate are supported in this one run. Record evidence-audit-complete separately: unavailable child runtime or tool-path logs remain explicit gaps, never fabricated evidence. A downstream or boundary failure is incomplete/failed with the precise unmet stage. Documentation or deterministic tests alone never pass live acceptance. User acceptance may acknowledge a specifically enumerated host-observability limitation; it cannot turn a missing execution stage into success.

## Evidence and verification

Keep a portable sanitized evidence index linking prior immutable local reports and their hashes, with committed copies of selected safe receipts and conclusions. The final run bundle records fixture/binary/role/helper provenance, exact native dispatch identities and collected terminal returns, startup versus operation roots, plan/worker/results commits and attachment, scope final acknowledgement, test manifests and genuine RED stdout, exact final gate HEAD, primary audits, typed halt, and keyed parent verdict. Never publish scope capabilities, gate-context tokens, ownership credentials, encrypted payloads or unrelated host logs.

Deterministic validation rejects omitted or inconsistent stages, substituted roles, absent fresh plan dependency, wrong feature paths, incomplete child payloads, wrong gate context, missing acknowledgement, dirty or mismatched final HEAD, missing final verdict and false claims from child prose alone. Mutation tests remove each load-bearing fact. Keep observed facts, child-reported facts, configuration-only facts and unavailable telemetry distinct. Preserve safe raw evidence references sufficient to review the functional predicates without exposing capabilities.

## Alternatives and boundaries

Strict native feature startup was rejected after investigation: the user does not want an obscure Codex workaround or a new harness feature. Reverting to agent.enter was rejected for this POC. Option 2 deliberately accepts inherited primary cwd with verified explicit feature targeting. Other harnesses and production ADRs remain unchanged until 425.

Manual completion: after the continuous test succeeds, the reusable package and final report are durable, evidence gaps are explicitly accepted, and the user accepts the POC, close 423 through a documented manual metadata exception. The installed finalize.closeout operation requires a merged PR and cannot perform this exception. Do not invent PR/merge/build evidence or call a fixture halt completion of 423. Record status done and archive with a Manual POC completion note, accepted evidence links, date and scope; keep pr empty, repair derived board/backlinks using their official renderers, and validate the metadata. This is a human-authorized one-off lifecycle exception, not a new automatic completion path. If renderer/validation requirements cannot be fulfilled, report close-out pending rather than claiming done.

425 consumes this evidence to implement Codex-only progressively disclosed native routing and worktree binding; 424 supplies capability validation. Reconcile 426's obsolete assumption that feature agent.enter must remain the normal route when designing its explicit legacy-retirement scope. Production delivery and parallel-safety certification remain out of scope for 423.
