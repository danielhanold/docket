# Native Codex candidate acceptance

Prepare this track from a clean, pinned candidate source with an absolute candidate `docket` binary. Generate the candidate's complete production agent definitions and skill resources into a new disposable fixture; do not replace stable global Codex or Docket installations. Record the source commit, binary commit and hash, every rendered definition/resource hash, and the operator-authored model pins.

The named prerequisite is `codex-candidate-loading-unverified`. A fresh Codex parent must show that it loaded the fixture's candidate definitions and absolute executable, and each real child must independently report the same role/resource and executable provenance before substantive work. A parent-only file check or inherited PATH is insufficient. If the host cannot safely select these project definitions, stop with that prerequisite and the observed host evidence.

Run one continuous disposable workflow: arm the real outer gate; let native ImplementNext create the feature; dispatch the planner with complete resources; prepare and validate a real task scope and final worker payload; preserve each first gate response; run baseline, RED, GREEN, acknowledgement, and the configured implementation suite; dispatch the pinned read-only reviewer; author, publish, and attach results; then run the independent clean final-head checkpoint. Stop at the bounded typed halt and collect the original parent's keyed verdict.

The receipt records native lineage, canonical runtime roots, plan/task/results commits, scope sequence, review HEAD, publication and metadata revisions, implementation and final gates, and primary-tree audit limits. It contains no live capabilities or gate keys. Report deterministic-only WAITING coverage and transient-write visibility honestly; matching final snapshots do not prove hard isolation.

Change 424 remains a later, separately authorized stacked dogfood run after 425 source verification and review. Do not mutate its dependency or precreate its worktree during candidate preparation.
