# Docket 423 continuous native POC — authoritative fixture boundary

This disposable change1 implements the user-approved option2 experiment. A fresh Local parent starts in primary on Terra/low. Through ordinary native named-agent dispatch it starts the real ImplementNext coordinator on Terra/low; that same coordinator creates a new registered feature worktree, invokes the real Sol/medium planner, verifies/attaches the new plan, invokes the actual docket-build skill and real Terra/medium standard worker, verifies tests/commit/acknowledgement, obtains configured suite evidence and a durable results checkpoint, then halts. The parent collects the actual terminal coordinator and obtains the keyed outer gate verdict. Read external LAUNCH.md for the full sequence.

Preparation leaves one proposed unclaimed candidate with no feature worktree, plan, scope, gate or agent. It may run ordinary baseline tests. A used fixture cannot be reset or resumed. No imported or parent-authored plan/implementation counts in the live run. The separate explicitly labeled deterministic rehearsal may use synthetic plan/code and never counts as native evidence.

The user authorizes native routing instead of production agent.enter instructions in this fixture. Zero agent.enter calls under any name; no shell/subprocess agent runner, App Server root, generic substitute, parent relay, host relocation, tool wrapper, custom native-cwd mechanism, global change or Luna probe. Normal Docket test-driver processes are permitted. Only the three registered roles belong to the bounded chain; review, PR, merge, cleanup and other agents are excluded.

A feature child may inherit primary startup cwd. Its first shell call uses the exact Python-bearing entry argv, login=false and no workdir override to validate canonical assigned worktree, current branch/clean HEAD, registration, creation receipt and change identity. The checker is read-only. All later feature shell calls explicitly set feature workdir; all feature file/skill accesses use canonical absolute feature paths. Only role-authorized synchronized metadata and named input/driver paths are exceptions. Only the coordinator performs repository.prepare and metadata duties. Necessary .git/.docket/.worktrees administration and named external evidence/gate paths are permitted; primary source/index/HEAD and all fixture instructions remain unchanged. Unexpected dirt is preserved and reported.

The planner gets the complete validated payload and real selected planning-skill snapshot from .codex/POC-PLAN-DISPATCH.md. The worker uses .codex/POC-WORKER-DISPATCH.md: credential-free fixed task inputs from the actual attached plan; complete exact native message containing real scoped child capability and outer gate context; private parent capability; conditional preservation of a supplied run epoch; independent live catalog; fixed values reloaded on every test; real baseline/assertion-RED/GREEN; one code/test commit and durable acknowledgement. No child searches the fixture parent or other sessions for inputs/credentials. Child WAITING follows normal handoff/controller claim/advance for the same drive. BLOCKED means settle/preserve/halt, no parent repair, new worker or final suite.

Require clean implementation HEAD before the configured build-owned suite. Preserve that evidence separately if a results checkpoint moves HEAD; follow the real results contract to certify the final committed checkpoint as well. Local commit, push to the local bare origin and metadata attachment are distinct required facts. Stop with typed change.halt before review or PR, and obtain the terminal outer keyed verdict only after coordinator completion. No child's prose or green inner test replaces the gate's authority.

Save sanitized run-report.md, coordinator-terminal.md and acceptance.json with source references/hashes. Verify primary integrity after each child and at halt. Distinguish observed runtime/tool events, configured pins, child receipts and unavailable telemetry. Preserve the known feature-only plan artifact-missing diagnostic; do not copy plans to primary or suppress unrelated findings. continuous-functional-passed requires the complete executed chain; report audit completeness and limitations separately. A clean snapshot cannot exclude transient restored writes, and no hard-isolation, parallel-safety or production-readiness claim is made. The user evaluates this evidence before manually closing423; production implementation belongs425.
