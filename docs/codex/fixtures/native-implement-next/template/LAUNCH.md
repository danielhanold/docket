# Final continuous option-2 POC — manual launch

Use a NEW Local Codex app task at @@FIXTURE_ROOT@@/repo on gpt-5.6-terra / low. Do not use Worktree mode. The actual native coordinator is Terra/low, planner Sol/medium, standard worker Terra/medium. This runs disposable change1, never production423. Read AGENTS.md and RUN.md.

## Preflight and parent dispatch

Run `python3 @@FIXTURE_ROOT@@/check-runtime.py` from primary, login=false. Require current-task RUNTIME_OK. Verify every hash and permission mode in evidence/launch-manifest.json and the exact current binary identity against its live capability catalog. Verify arm.json, primary-baseline.json and current live status: one proposed ready unclaimed candidate, no feature worktree, plan, worker inputs, results or prior gate. No baseline/instruction/permission rewrites after launch; a used fixture cannot be reset or resumed.

Before arming the gate or dispatching any agent, run `python3 @@FIXTURE_ROOT@@/check-results-template.py --repo @@FIXTURE_ROOT@@/repo` with primary workdir/login=false. Require RESULTS_TEMPLATE_OK and location primary. Save the JSON as evidence/results-template-primary.json. The template is tracked inside the hidden .agents directory. Its exact relative path is `.agents/skills/docket-implement-next/results-template.md`; do not try to discover it with a default file search. Pass this exact relative path and its validated hash explicitly to the native coordinator as required results-template inputs. The coordinator must resolve it against its newly created feature worktree, not use the primary copy for authoring.

Fetch and validate the parent catalog; resolve every operation from it. Run the normal parent gate-before implement-next once and retain the exact key, dispatch-context and any returned epoch privately. Recheck runtime immediately beforehand. Native-dispatch the real registered docket-implement-next once, fork_turns none. Pass these entire instructions, the exact fixture root, unchanged key/context and any epoch, and their purpose for change.claim and all scope/start operations. No availability probe is needed; no shell agent runner, agent.enter, App Server, generic role, root relocation or fallback is permitted.

## Same coordinator, fresh planner, corrected worker

The same native coordinator follows the real implement-next snapshot to claim with the outer context, reconcile and create the new feature worktree. Save the successful workspace.prepare response as @@FIXTURE_ROOT@@/evidence/workspace-created.json. Before feature operations/children verify live claim, canonical registered worktree, branch and current clean HEAD. Use explicit feature paths after allocation. Only the coordinator runs repository.prepare; feature children do not perform its metadata duties.

Immediately after validating the created feature worktree, BEFORE planner or worker dispatch, the coordinator runs `python3 @@FIXTURE_ROOT@@/check-results-template.py --repo <exact canonical feature root>` with explicit feature workdir/login=false. Require RESULTS_TEMPLATE_OK, location feature, and the same template hash passed by the parent. Preserve the complete receipt as evidence/results-template-feature.json. Retain its results_template_file as the exact authoring source for Step6.5. This preflight writes no repository files and starts no agent or gate. On failure preserve the diagnostic and halt; do not search elsewhere or synthesize a replacement template.

After claim/reconcile/workspace creation, the coordinator runs repository.prepare again to synchronize the metadata working tree, and uses its returned metadata_worktree_path literally. Typed mutations can advance metadata refs without updating the checked-out metadata files. Before planner dispatch read FEATURE/.codex/POC-PLAN-DISPATCH.md. Resolve the full real planning payload, including feature snapshot of superpowers:writing-plans and exact metadata inputs. Validate with Python and validate-plan-payload.py, then pass the COMPLETE validated payload and full first-entry argv/boundary labels. The planner is the registered agent, not a nonexistent same-name skill. Native-dispatch it once, fork_turns none. It first checks binding using Python/login=false/no workdir override, then authors and commits a fresh single-task standard-profile plan. No imported or coordinator-authored plan. Preserve PLAN_PATH, plan commit, trailer/backlink and exact native receipt. Verify and attach it before build.

Invoke the actual docket-build skill at FEATURE/.agents/skills/docket-build/SKILL.md. Follow FEATURE/.codex/POC-WORKER-DISPATCH.md for fixed worker inputs, exact entry argv, complete real capability-bearing native message and outer gate context. This combines the already successful worker correction with the fresh planner chain. Do not inherit the earlier focused test's instruction to omit gate-context. Use the actual standard worker once, sequentially after the planner. Child receipts, boundary stdout and independent catalog/path evidence must be retained with their source/visibility limits.

## Gates, results and terminal collection

Only after actual worker COMPLETE, real RED/GREEN, the sole task commit, clean feature tree and durable scope acknowledgement may the coordinator run the normal build-owned final configured suite. Resolve diagnostic.config at feature and use the configured driver command, without an explicit test override, with an external run root under evidence/final-build-gates and unchanged outer gate-context plus the supplied epoch when present. Record evidence for its exact tested implementation HEAD. Do not use context.implementation as a config lookup after claim.

Recheck the SAME feature template with the read-only checker, then read results_template_file literally from the saved feature receipt. It is FEATURE/.agents/skills/docket-implement-next/results-template.md, relative to the ImplementNext skill directory. A default rg --files search omits hidden directories and is not evidence that this file is absent. Follow the real results checkpoint contract and this template: author accurate bounded results in the feature worktree, backlink, commit only that artifact, push to the local origin and attach it. State review/PR/merge not run. Results may be created at earlier normal checkpoints; preserve them, never truncate. A results commit changes HEAD: retain implementation-test and checkpoint heads separately and certify the FINAL clean checkpoint HEAD through the normal configured build driver. That checkpoint gate is mandatory for this fixture. This is justified by the changed commit, not an unnecessary repeat. Never edit results just to insert the final gate SHA. Save final exact-HEAD evidence externally.

On BLOCKED or missing mandatory input, preserve the work and settle all existing drives under the normal handoff/claim contract. Do not repair or commit child work in the coordinator, replace a worker, or run the final suite after an incomplete worker. WAITING continues the SAME drive/ownership; no new test or scope. Preserve any live child identity and collect actual terminal output before reporting.

At the bounded endpoint use typed change.halt with live schema/current version. No review, PR, merge, cleanup or production changes. The outer parent must collect the coordinator's actual terminal output before its exact keyed run.gate-verdict. gate-stop/run-halted is expected and forbids redispatch; it is not by itself certification. Never treat a child completion notification as gate authority.

## Evidence and acceptance

Use external @@FIXTURE_ROOT@@/evidence. Audit primary after planner, worker, and final halt using primary-audit.py against the original primary-baseline.json. Preserve original source/instructions and current feature-only plan artifact-missing finding. No file searches outside assigned paths or named evidence; capabilities remain private. Each agent fetches its own live catalog, never a parent cache.

Write run-report.md and coordinator-terminal.md, and capture sanitized acceptance-coordinator.json using @@FIXTURE_ROOT@@/ACCEPTANCE-SCHEMA.md. Include native parent/coordinator/planner/worker lineage, observed versus configured pins, initial absence/creation of worktree, fresh plan verification/attachment and worker consumption, fixed input hashes/exact argv, boundary stdout, operation paths, genuine baseline/RED/GREEN, committed task delta, acknowledgement, implementation/final-checkpoint gates, results attachment, primary audits, typed halt and terminal outer keyed verdict. Retain hashes/references for supporting receipts. Never save parent/child capabilities, raw owner generations or outer tokens in public evidence.

The coordinator's acceptance-coordinator.json is a draft: the outer verdict is not yet available to that child. Preserve that file and all referenced reports after hashing. The OUTER PARENT owns final evidence assembly AFTER collecting the coordinator's terminal output and the keyed run.gate-verdict. Save a sanitized outer-gate-verdict.json with decision/outcome/terminal/attributed change, omit raw keys and tokens, and record whether the actual key equals the original arm key. Copy the coordinator draft into final acceptance.json, add the real outer_verdict facts and its evidence hash, and remove only the now-resolved outer-verdict-unavailable gap. Retain any genuinely missing stages and every other visibility limitation. Do not rewrite source reports after hashing them. Run `python3 @@FIXTURE_ROOT@@/validate-evidence.py @@FIXTURE_ROOT@@/evidence/acceptance.json` and preserve its output as evidence/acceptance-validation.json. A missing results checkpoint or failed final gate must remain certification-incomplete; never fill absent facts to make validation pass.

Report continuous-functional-passed only if all actual stages and their receipts hold in this run. Mark evidence_audit_complete separately; opaque native messages, unavailable child runtime/path logs and inability to exclude transient restored writes must stay explicit limitations. No hard isolation, parallel safety or production readiness claim. No more Luna probes. After this manual run, the user and reviewer evaluate acceptance before manually closing423 and implementing425.
