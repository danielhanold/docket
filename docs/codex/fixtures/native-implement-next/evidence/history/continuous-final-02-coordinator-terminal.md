# Coordinator terminal output

TERMINAL: HALTED

`change.halt`: `applied`, disposition `halted`, metadata revision `d8a5a86b261e91d2d89de6af0c8478b0f6e8f1d7`.

`run.verify`: `run-halted`.

Reason: the mandatory results checkpoint cannot be created because `results-template.md` is absent from the verified feature worktree. No review, PR, merge, cleanup, results attachment, or final exact-checkpoint gate was performed.

Public evidence: `evidence/run-report.md`, `evidence/workspace-created.json`, `evidence/planner-payload.json`, `evidence/planner-payload-validation.json`, `evidence/final-build-gates/`, and `evidence/halt-request.json`.
