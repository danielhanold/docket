# Independent failure analysis

The run is certification-incomplete. Native parent → ImplementNext → planner/worker lineage and the intended Terra/low, Sol/medium and Terra/medium runtime pins are independently corroborated by the exact host records. Entry BINDING_OK and the independent worker catalog are present. The newly authored plan was committed and attached.

The coordinator prepared the real task scope using `--task-id 1`. The immutable worker-input file says `task_id: task-1`, and the worker loaded that value for its start. The durable scope pins `1`; the driver compares task identifiers exactly. Child capability and gate-context hashes match the supplied values, and the supplied epoch also matches. The scope has zero drives. This establishes a handoff identity mismatch before test launch. The driver's scopedIdentityMatch/scopeIdentityMatch path maps that mismatch to scope-identity-mismatch invalid input, consistent with exit2. The original JSON error was not retained, so this diagnosis derives from the retained request/state and source rather than an observed original error string.

The worker discarded the captured CLI output when exit2 occurred, printing only the exit code. Its subsequent jq expression also expected top-level drive_id/owner_generation/outcome. The verified real response has a nested drive with drive_id and generation; that parser would reject a successful start too. Neither error is evidence that native dispatch or worktree binding is unavailable.

The existing deterministic rehearsal prepared scope fields directly from the immutable worker record. The native coordinator retyped a different task id. The payload validator checked token presence/equality and exact entry argv, but did not compare scope identity against the fixed record. The preparation checks therefore missed this boundary; adding more successful helper-case counts does not close that gap.

Required correction: derive scope creation arguments from the same fixed record; compare the complete actual scoped identity before dispatch; preserve the first full JSON reply and process outcome on every drive invocation, including errors; use the live nested drive schema and captured generation for all later calls. Add a mutation test changing only 1/task-1 that fails before native dispatch, and a valid/error reply parsing regression. These are data/receipt checks using ordinary native tools, not a replacement agent runner. Keep this failed run unchanged, and use a fresh fixture for any subsequent live run.
