# Manual completion of change423

The user authorized a manually conducted POC rather than Docket's build process. The delivered result is the reusable experiment and accepted evidence; it is not a production-code merge. Change425 consumes the findings for production implementation, with capability policy supplied by424.

## Acceptance before status change

1. Review the final fresh continuous native run: actual ImplementNext, fresh planner commit and verified attachment, corrected actual worker, real RED/GREEN, task commit/ack, configured gates at exact clean commits, durable results checkpoint, unchanged primary and typed halt followed by the parent's keyed gate verdict.
2. Verify the portable package, hashes and deterministic tests. Preserve explicit evidence gaps, distinguish observations from child receipts, and obtain the user's acceptance of any host-observability limits. Do not mark a missing execution stage as satisfied.
3. Write the final423 POC results/acceptance report, with the final artifact locations and hashes. Cite425 as the production implementation successor. Keep historical runs unchanged.

## Truthful metadata exception

The installed binary exposes no manual-complete operation. `finalize.closeout` only archives after a verified merged PR; `change.mark-implemented` also requires a real PR. Neither can honestly express this POC outcome. Do not invent a PR, merge, review or build receipt, and do not use killed for successful work.

After acceptance, perform one explicit manual metadata close-out: synchronize the Docket metadata branch and pin its current423 version; set423 status to done and dated archive placement; clear only active claim/reconciliation bookkeeping that should not survive completion; leave pr empty; preserve historical metadata as historical. Add a Manual POC completion section stating the user-authorized lifecycle exception, accepted scope/evidence/date and no production merge. Reference the durable report as a metadata-branch POC artifact, avoiding the normal results field if its renderer would falsely link an unmerged file on main.

Regenerate the board and metadata artifact/backlink blocks through the existing Docket Go renderers, under an exact metadata lease. Do not hand-render derived blocks or bypass consistency checks. Validate record uniqueness, terminal directory/status agreement, references and dependency readiness, then commit/push the single coherent metadata change. This is a one-off human-authorized exception, not a new automatic API, and does not require Docket's build workflow. If official renderer/transaction integration cannot be completed, keep close-out pending and state that limitation rather than claiming done.

Do not invoke merge-driven cleanup or rebuild the installed binary: there was no production merge and the experimental worktrees are evidence. Preserve them until evidence packaging is confirmed. The spec and report retain the distinction between done-as-accepted-POC and production425 delivery.425 remains dependent on424 even after423 is done.

## Current state

423 is in progress for manual POC work, with the agreed option2 requirements reconciled.425 has a linked spec covering native coordinator and feature-child routing, Codex-only progressive disclosure, complete payloads, scope ownership, exact-commit gates and feature-owned artifact diagnostics. Its dependency on423 remains until the accepted close-out above. The final continuous native run is prepared for manual launch, not yet certified.
