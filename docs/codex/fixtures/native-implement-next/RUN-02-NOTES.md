# Continuous run 02 preparation

The failed run01 remains certification-incomplete. Its planner succeeded; its worker scope pinned task `1` while the immutable worker record specified `task-1`. The original worker also discarded the first error JSON and would have rejected a valid response using the wrong top-level receipt fields. See the immutable reports and independent analysis under evidence/history/continuous-final-01-*.

The new fixture generates scope identity arguments from the same fixed input consumed by the worker. Before dispatch, a controller-only read-only check compares the actual minted scope with that input, including repository, task, change, phase, branch, worktree, capability hashes, outer context, epoch and unused lifecycle. The exact file-loaded seed and verified grant must pass unchanged into the native payload. The fixture checker reads the installed binary's pinned v3 scope record; an unknown schema halts. It does not create scopes, launch agents, replace a Docket operation, or constitute a new production API.

The worker uses the documented ordinary shell call to retain first stdout, stderr and process status privately before interpreting them. It reads the actual nested drive object and generation. Valid FAILED/WAITING receipts remain available for their normal contracts; malformed or invalid-input replies retain diagnostics and never authorize a second start.

Verification includes a read-only replay of run01's actual mismatched scope,40 focused identity/receipt cases, missing/altered verified-handoff checks, and mutation removal of the identity guard and correct receipt parser. The existing45 fixed-input,41 payload,29 planner and79 acceptance checks also pass. Tests that remove the new guards fail as expected. Counts describe preparation checks, not native certification.

A separate disposable rehearsal used the live standard binary, actual minted scope/checker/payload, and the EXACT worker response-capture block. Baseline, genuine assertion RED, GREEN, task commit/acknowledgement, implementation suite, results checkpoint/attachment, final checkpoint suite, typed halt and keyed verdict completed. Primary integrity remained unchanged. See evidence/scope-rehearsal-binary-compatibility.json and checks/scope-receipt-mutations.json. No native test agents were used in that rehearsal.

The manual fixture is /Users/homer/dev/docket-poc-0423-continuous-final-02. It remains unclaimed, without a feature worktree, worker inputs or gate scopes. All68 launch-manifest entries and their permission modes match; source templates and binary identity were independently rechecked. See checks/final-preparation-02.json and MANUAL-RUN.md. The user launches a new Local Codex task at its repo on Terra/low. The continuous native result is still pending, and423 remains in progress.
