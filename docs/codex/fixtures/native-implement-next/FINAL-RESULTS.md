<!-- docket:backlink:start (generated — do not hand-edit) -->
> ↩ **[Change 0423 — Certify native Multi-Agent V2 orchestration through Docket ImplementNext](https://github.com/danielhanold/docket/blob/docket/docs/changes/archive/2026-09-14-0423-certify-native-multi-agent-v2-orchestration-through-docket-i.md)**
<!-- docket:backlink:end -->
# Change 0423 — accepted native Codex POC

## Outcome

Completed as a manually conducted and accepted POC on 2026-09-14. The successful continuous run started from a fresh primary checkout and used native ImplementNext → planner → standard worker dispatch. The coordinator created a new registered feature worktree; children inherited primary startup and explicitly targeted the validated feature worktree under agreed option 2.

The user reviewed the successful functional result and its observation limits, then explicitly requested final evidence packaging and manual closeout. This is a documented exception to the normal PR-based Docket lifecycle. There was no production implementation, PR, review, merge or installed-binary rebuild for 423. Production capability policy and routing work remain 424 and 425.

## Verified evidence

- Parent/coordinator: gpt-5.6-terra / low. Native planner: gpt-5.6-sol / medium. Native standard worker: gpt-5.6-terra / medium. Host records corroborate the native parent/child lineage and observed runtime settings.
- Fresh plan: `5b1af86bb03e6ba7494ee69a86a4650b10a47143`.
- TDD implementation: `9e8ac68dd82ea66c78785b4448b08d8e8b8aabd3`. Real baseline/RED/GREEN records and final task-scope acknowledgement are retained.
- Final results checkpoint: `a9b1be9539b0e2078db3d1b9faca8e8473b2fe6a`. The results artifact was committed, pushed to the local bare origin, and attached at authoritative metadata revision `cea1c7bcaf9836936bc87f0d66fa68e258f1a1c9`.
- The configured full suite passed at both the implementation and final results commits; the final feature tree was clean.
- Typed halt and the parent's terminal `gate-stop / run-halted` verdict used the original arm key. All scoped drives carried the same outer context. No agent.enter route was used by the recorded native chain.
- Independent primary audit: `PRIMARY_UNCHANGED`. The unchanged acceptance validator reports `continuous-functional-passed`.

The self-contained [final evidence](evidence/final-run/README.md) includes unchanged acceptance/receipt copies, their [source hashes](evidence/final-run/SOURCE-INDEX.json), the independent review, readable committed artifacts and a verified Git bundle of the feature and metadata history. [PACKAGE-SHA256.json](PACKAGE-SHA256.json) pins all package files. Original run: `/Users/homer/dev/docket-poc-0423-continuous-final-04`; its raw gate state and host records remain private and are preserved locally. Original failed/preflight fixtures also remain intact.

## Accepted limits

Functional completion does not establish hard isolation, absence of transient restored writes, parallel safety or production readiness. Evidence audit completeness remains false. Native-message opacity and clean snapshots cannot exclude temporary changes subsequently restored. The known metadata lookup issue reports feature-only plan/results as missing; this review independently verified their actual Git objects and attachment in the authoritative metadata revision.

The plan selected focused TestGreet runs for RED/GREEN, followed by independent configured full suites. The acceptance bundle abbreviates the worker run root; independent review records its actual canonical absolute path. Its results-stage template receipt alone does not establish publication or attachment; the independent review additionally verifies both against Git and current metadata.

## Reusable deliverable and successor

The included setup generator, pinned experimental role/skill snapshots, path/binding checks, complete scope-capability handoff, first-response capture, template preflight, acceptance contract and deterministic regressions remain available to reproduce the POC. Historical preparation reports describe their point-in-time states; this final report supersedes their pending-completion statements.

[Change 425's production specification](change-0425-design.md) consumes these findings. Change 424 supplies model capability policy, and 425 continues to depend on 424 after 423 is done. Broader legacy retirement remains separate. The experiment does not install its role routing into production.

## Manual metadata closeout

The archived 423 record leaves PR, plan and results lifecycle fields empty and references this report in authored prose, avoiding a false main-branch results link. Its historical branch label is retained; active claim and reconciliation bookkeeping are cleared. Docket's official document patcher, artifact/backlink renderers, board renderer, snapshot/evolution validators and metadata transaction engine perform the coherent archive update under exact entity expectations and an expected-ref lease. The one-off administrative adapter is not an installed CLI capability or an agent launcher. No experiment worktree is removed and no merge-driven cleanup is invoked.
