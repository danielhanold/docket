# Continuous run03 preparation

Run02 verified native ImplementNext → planner → standard worker through the implementation suite. It stopped before the results checkpoint because the coordinator's default file search skipped the hidden .agents directory. The required template was present in the feature worktree throughout. Its acceptance draft also retained an unavailable outer verdict after the parent had actually collected one. The original evidence and independent review are preserved under evidence/history/continuous-final-02-*.

The correction makes the template's path and hash explicit coordinator inputs. Setup rejects a missing packaged source before allocating the fixture. A direct read-only preflight verifies the template in primary before any native dispatch, then in the newly registered feature worktree before planner dispatch. Results authoring rechecks and reads the exact feature path returned by that preflight. It never relies on a default file search. The fixture requires a gate at the final results checkpoint HEAD.

The coordinator writes an immutable acceptance-coordinator.json draft. After terminal collection and its keyed verdict, the parent writes final acceptance.json using the actual sanitized outer receipt, retains all remaining gaps, and runs the unchanged completeness validator. This resolves stale evidence without relaxing any missing-stage requirement.

The missing-template setup regression failed before the correction and passes now. Ten direct-lookup cases include the original hidden-directory symptom, a real linked feature worktree, missing and changed files, redirection and hash mismatch. Removing the file-hash guard makes the mutation test fail. The corrected checker also succeeds read-only on run02's actual feature template. Existing input, dispatch, planner, handoff and acceptance tests pass.

The separate disposable results-rehearsal-03 used the standard installed binary, the exact worker handoff and capture sequence, and the actual shipped results template. It completed baseline/assertion RED/GREEN, task commit and acknowledgement, implementation suite, results authoring/commit/local publication/attachment, final checkpoint suite, typed halt and keyed verdict. Primary integrity remained unchanged. This is non-native preparation evidence, not certification. See evidence/results-rehearsal-binary-compatibility.json and evidence/results-rehearsal-results-template-consumption.json.

The fresh manual fixture is /Users/homer/dev/docket-poc-0423-continuous-final-03, with its primary at repo. All launch-manifest entries and modes were verified against the packaged source; its template preflight passes. It remains unclaimed with no feature worktree, plan, worker inputs, results, gate scope or native test agent. See checks/final-preparation-03.json and MANUAL-RUN.md.

Use a fresh Local Codex task on Terra/low. Model roles and option2 worktree targeting remain unchanged. No agent.enter, Luna, review, PR, merge or production implementation is authorized by this fixture. Production423 remains in progress until the complete native run is reviewed and manually closed under the documented exception.
