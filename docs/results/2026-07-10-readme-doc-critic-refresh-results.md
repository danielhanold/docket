# Results — 0052 README doc-critic refresh

**Date started:** 2026-07-10
**Change:** 0052 · **Branch:** feat/readme-doc-critic-refresh
**Baseline:** README.md @ origin/main d7f4a96 (336 lines, post-0051)
**Final:** README.md @ this branch, 7d9af20 (404 lines) — restructured (a3e5f43) + prose pass (7d9af20); 405 lines after the whole-branch review fix pass below (line-count and line-number citations in this doc reflect the 405-line post-fix-pass README).
**Task 4 close-out:** 2026-07-10 — full suite run, checklist reconciled against the final README, results doc finalized.
**Review fix pass close-out:** 2026-07-10 — 4 whole-branch review findings applied (lifecycle-coverage rows, drift-enumeration third location, `--check` fourth leg, `docket-config.sh` gloss moved to first use); see `## Whole-branch review fix pass` in `.superpowers/sdd/task-4-report.md`.

## Claims audit

Row locations below are the **final** README's section/line, reconciled by Task 4 against the post-restructure (a3e5f43) + post-prose-pass (7d9af20) text. Every row from the original 95-row audit is retained; none were dropped. Four rows carry a **Task 4 note** where the restructure/prose pass reworded or merged the cited text (verdict unchanged, location and phrasing updated). Rows 96–105 are new: claims introduced by the rewrite's new sections (Table of contents, Quickstart, Prerequisites) that had no antecedent in the baseline, audited with the same rigor.

| # | README § (line) | Claim | Verdict | Evidence |
|---|---|---|---|---|
| 1 | Lead (l.3) | docket "provides eight skills" | correct | `ls skills/` = 8 dirs: docket-new-change, docket-groom-next, docket-auto-groom, docket-implement-next, docket-finalize-change, docket-status, docket-adr, docket-convention. **Task 4 note:** the lead now reads "ships a set of agent skills" (count word dropped); the explicit "eight" count is consolidated solely in `## The eight skills` (l.384 heading, l.386 body) so the count guard has one place to hold, not three. Verdict unaffected — the count itself is still correct and still asserted, just once. |
| 2 | Lead (l.3) | "all coordinated through git, no CLI or database" | correct | repo root has no CLI entrypoint/binary or DB code; every mechanism is git commits/branches/worktrees driven by `scripts/*.sh` (plumbing only). Final wording: "all coordinated through git, with no CLI to install and no database to run" — same claim, same line. |
| 3 | Why docket (l.65) | "a thin lifecycle layer — plain markdown files in your repo, [skills], no CLI" | correct | same evidence as #1; `skills/*/SKILL.md` files are the only entry points, no binary/CLI shipped. **Task 4 note:** relocated from the old "What docket is" section (baseline l.13) to "Why docket" (l.65); "eight skills" reworded to "a handful of skills" for the same count-consolidation reason as #1. |
| 4 | Why docket (l.65); Configuration § Workflow roles (l.189) | "each invocation point is rebindable via `skills:` in `.docket.yml`" | correct | `scripts/docket-config.sh:195-221` (`skill_role()` resolves brainstorm/plan/build/review/finish from the `skills:` block). **Task 4 note:** the "Why docket" mention (l.65) is now a pointer ("each step is individually rebindable; see Configuration"); the full mechanism is stated in the relocated Workflow-roles subsection (l.189), formerly its own "Workflow engine" H2 (baseline l.38). |
| 5 | Why docket (l.65) | "The code is always the current-state truth; docket carries no living-spec layer" | correct | `docs/adrs/` is an immutable decision ledger (`skills/docket-convention/SKILL.md:303`, "An `Accepted` ADR is immutable except its `status:` line"); no auto-generated "current architecture" doc exists anywhere in the convention or scripts. |
| 6 | The eight skills (l.386) | "The eight skills cover the full loop: create, groom, implement, finalize, report, decide — plus the shared contract" | correct | `skills/docket-new-change/SKILL.md:3` (create), `skills/docket-groom-next/SKILL.md:3` + `skills/docket-auto-groom/SKILL.md:3` (groom), `skills/docket-implement-next/SKILL.md:3` (implement), `skills/docket-finalize-change/SKILL.md:3` (finalize), `skills/docket-status/SKILL.md:3` (report), `skills/docket-adr/SKILL.md:3` (decide), `skills/docket-convention/SKILL.md:3` (shared contract). Relocated from baseline l.15 to l.385, then to l.386 after the review fix pass inserted one line above it (same section throughout). |
| 7 | How it works, table (l.34) | `docket-new-change` — "Interactive — you brainstorm ideas into a build-ready backlog" | correct | `skills/docket-new-change/SKILL.md:3,10`. Relocated from the old "The producer / implementer loop" H2 (baseline l.25) into the new "How it works" H2 (l.28). |
| 8 | How it works, table (l.35) | `docket-implement-next` — "Autonomous — drains that backlog to open PRs, unsupervised" | correct | `skills/docket-implement-next/SKILL.md:3,10`. Relocated from baseline l.26. |
| 9 | How it works (l.37) | "the producer only mints new `proposed` change ids; the implementer claims the next build-ready change atomically (compare-and-swap on the status field)" | correct | `skills/docket-new-change/SKILL.md:10` ("only ever mints new `proposed` ids — scanning the max existing id and incrementing"); `skills/docket-implement-next/SKILL.md:35,37` ("Step 2 — Claim (compare-and-swap)"). Relocated from baseline l.28. |
| 10 | How it works (l.39) | "The human stays in the loop at two points only — writing the change and merging the PR" | correct | `skills/docket-new-change/SKILL.md:10` (human brainstorms/writes); `skills/docket-implement-next/SKILL.md:91` ("STOP. The change stays in `active/` as `implemented` until a human merges it"). Relocated from baseline l.30. |
| 11 | How it works (l.53) | "dependency chains serialize on the merge gate... the board surfaces this as 'waiting on #N — needs your merge'" | correct | `scripts/lib/docket-frontmatter.sh:16` (`DEP_REASON[id]` = `"not yet built"` \| `"needs your merge"`); `scripts/board-checks.sh:87`; `scripts/render-board.sh:90`. Relocated from baseline l.32 — this caveat now sits at the end of the "How it works" section, after the change-lifecycle subsection was inserted between the loop table and it. |
| 12 | Configuration § Workflow roles (l.189) | "Each of the five workflow invocation points is a pluggable role... rebinds any of them to a different skill... or to the sentinel `auto`" | correct | `scripts/docket-config.sh:195-221` (5 roles resolved: brainstorm/plan/build/review/finish; `auto` is a literal value, not specially parsed — it flows through as the skill name and is checked by the invoking skill, per `skills/docket-convention/SKILL.md:206-209`). Relocated from the old standalone "Workflow engine" H2 (baseline l.38) into the new "Configuration" H2's "Workflow roles — the `skills:` map" H3. |
| 13 | Configuration § Workflow roles, table (l.193) | `brainstorm` → default `superpowers:brainstorming` | correct | `scripts/docket-config.sh:207`. Relocated from baseline l.42. |
| 14 | Configuration § Workflow roles, table (l.194) | `plan` → default `superpowers:writing-plans` | correct | `scripts/docket-config.sh:208`. Relocated from baseline l.43. |
| 15 | Configuration § Workflow roles, table (l.195) | `build` → default `superpowers:subagent-driven-development` | correct | `scripts/docket-config.sh:209`. Relocated from baseline l.44. |
| 16 | Configuration § Workflow roles, table (l.196) | `review` → default `superpowers:requesting-code-review` | correct | `scripts/docket-config.sh:210`. Relocated from baseline l.45. |
| 17 | Configuration § Workflow roles, table (l.197) | `finish` → default `superpowers:finishing-a-development-branch`, invoked by `docket-implement-next` and `docket-finalize-change` | correct | `scripts/docket-config.sh:211`; `skills/docket-finalize-change/SKILL.md:208-210` ("Where finishing-a-development-branch fits"). Relocated from baseline l.46. |
| 18 | Configuration § Workflow roles (l.199) | unset `skills:` keys default to the superpowers skills; a resolved-but-unavailable skill degrades to that role's `auto` fallback with a prominent warning | correct | `scripts/docket-config.sh:201-206` (per-key default fallback); `skills/docket-convention/SKILL.md:210-216` ("Missing-skill rule — degrade to auto + warn"). Relocated from baseline l.48. |
| 19 | Install § Prerequisites (l.107) | "installing superpowers is the consuming user's responsibility; docket does not bundle or install it" | correct | `install.sh`, `link-skills.sh`, `sync-agents.sh` only reference docket's own `skills/`/`agents/` dirs; no superpowers-fetching code anywhere in the repo. **Task 4 note:** relocated from the tail of the old "Workflow engine" section (baseline l.50) into the new Install § Prerequisites subsection (l.107), where it now reads "docket does not bundle or fetch it" and folds in the degrade-to-auto behavior for newcomer visibility before install. |
| 20 | Install (l.113-117) | `bash ~/dev/docket/install.sh` is "the whole install" | correct | `install.sh:1-30`. Relocated from baseline l.59. |
| 21 | Install (l.117) | "`install.sh` runs the three primitives in order — and is idempotent" | correct | `install.sh:4-14,17-30` (calls `link-skills.sh`, `sync-agents.sh`, `scripts/ensure-docket-env.sh` in order; each is independently idempotent). Relocated from baseline l.62. |
| 22 | Install (l.119) | `link-skills.sh` "creates absolute symlinks... only writes into harness directories that already exist" | correct | `link-skills.sh:20-46` (`HARNESS_SKILL_DIRS` loop; skips any harness dir not already present). Relocated from baseline l.64. |
| 23 | Install (l.120) | `sync-agents.sh` generates wrappers from 4-layer config; opted-in repos get the full per-repo agent set as machine-local, gitignored files, never committed | correct | `sync-agents.sh:206-214` (`per_repo_opted_in`), `sync-agents.sh:502-541` (`project_level_pass`, "Always generate the FULL built-in set"), `sync-agents.sh:365-422` (managed `.gitignore` block); corroborated `tests/test_sync_agents.sh:379` (opt-in gating: a tracking-only `.docket.yml` with no `agents:`/`agent_harnesses:` writes NO project-level wrappers), `tests/test_sync_agents.sh:390` (opt-in gating: `agent_harnesses:` alone, no `agents:` block, opts in and generates the full 8-file built-in set), `tests/test_sync_agents.sh:911` (gitignore-block leg: `.gitignore` created with the managed `# docket:generated:start/end` block). Relocated from baseline l.65. |
| 24 | Install (l.120) | "Run `sync-agents.sh --check` in CI to catch a missing/stale `.gitignore` block or an accidentally-tracked generated file" | correct | `sync-agents.sh:543-567` (`check_project_level` legs a/b, both CI-meaningful, `rc=1`); corroborated `tests/test_sync_agents.sh:1030-1031` (gitignore-block leg — chk-a: missing/stale block fails `--check`, `rc!=0`, names "gitignore"), `tests/test_sync_agents.sh:1046-1047` (tracked-file leg — chk-b: an accidentally-tracked generated file fails `--check`, `rc!=0`, names the tracked path). Same bullet as #23; relocated from baseline l.65. |
| 25 | Install (l.121) | `ensure-docket-env.sh` "exports `DOCKET_SCRIPTS_DIR`... into your shell profile (and Claude Code's user-level `settings.json` `env`)" | correct | `scripts/ensure-docket-env.sh:17-46` (shell profile), `:48-64` (settings.json). Relocated from baseline l.66. |
| 26 | Install (l.121) | "Without it the skills fail loud with the `run docket/install.sh` remedy" | correct | `skills/docket-convention/SKILL.md:49` (`"${DOCKET_SCRIPTS_DIR:?run docket/install.sh}"`). Relocated from baseline l.66. |
| 27 | Install (l.125) | "Migrating an *existing* repo to docket-mode is a separate step — `migrate-to-docket.sh`, run from inside that repo" | correct | `migrate-to-docket.sh:7,44-62` (`REPO_ROOT` resolved from `$PWD` via `git rev-parse --show-toplevel`). Relocated from baseline l.68. |
| 28 | Install (l.125) | "The change data... lives per consuming project, not in the docket repo itself" | correct | none of `install.sh`/`link-skills.sh`/`sync-agents.sh` reference `docs/changes`, `docs/adrs`, or `docs/results` paths — only `scripts/`/`agents/`/`skills/` are the docket-repo-owned machinery. Relocated from baseline l.70; now the same paragraph as #27. |
| 29 | Configuration § `.docket.yml` (l.162) | ".docket.yml... committed on your repo's default branch (`origin/HEAD`)... every clone, agent, and device needs the same values" | correct | `skills/docket-convention/SKILL.md:16` ("committed... never gitignored"), `:43` ("lives on the repo's default branch (`origin/HEAD`)"). **Task 4 note:** the whole `.docket.yml` subsection (yaml example included) moved out of "Install" into the new "Configuration" H2 (baseline l.72-97 → final l.160-185), consolidating all config material in one place. |
| 30 | Configuration § `.docket.yml`, yaml (l.167) | `metadata_branch: docket # docket (default) \| main` | correct | `scripts/docket-config.sh:148-153` (`METADATA_BRANCH` default `"docket"`). Relocated from baseline l.77. |
| 31 | Configuration § `.docket.yml`, yaml (l.168) | `integration_branch: auto # auto (default → origin/HEAD) \| main \| develop` | **fix — confirmed applied** | `scripts/docket-config.sh:91-95` **fails closed** (`die`) when `origin/HEAD` is unresolvable — it never falls back to `"main"` (header: "Fail-closed: non-zero + stderr diagnostic on a hard error", `scripts/docket-config.sh:7-8`). **Verified in the final README:** l.168 now reads `integration_branch: auto     # auto (default → origin/HEAD) \| main \| develop` — the "fallback main" clause is gone. Only `migrate-to-docket.sh:76-82`'s one-time bootstrap resolver has a real "fallback to main," which is unrelated to this per-repo-settings comment. |
| 32 | Configuration § `.docket.yml`, yaml (l.169) | `changes_dir: docs/changes # default` | correct | `scripts/docket-config.sh:160`. Relocated from baseline l.79. |
| 33 | Configuration § `.docket.yml`, yaml (l.170) | `adrs_dir: docs/adrs # default` | correct | `scripts/docket-config.sh:161`. Relocated from baseline l.80. |
| 34 | Configuration § `.docket.yml`, yaml (l.171) | `results_dir: docs/results # default` | correct | `scripts/docket-config.sh:162`. Relocated from baseline l.81. |
| 35 | Configuration § `.docket.yml`, yaml (l.172) | `auto_groom: false # repo default...per-change auto_groomable overrides` | correct | `scripts/docket-config.sh:165` (default `false`); `skills/docket-convention/SKILL.md:338` (effective auto-groomable definition). Relocated from baseline l.82. |
| 36 | Configuration § `.docket.yml`, yaml (l.173) | `board_surfaces: [inline] # ...inline (BOARD.md) and/or github; [] disables the board` | correct | `scripts/docket-config.sh:167-193` (default `"inline"`); `skills/docket-convention/SKILL.md:53` (`[]` disables the board entirely). Relocated from baseline l.83. |
| 37 | Configuration § `.docket.yml`, yaml (l.175-178) | `finalize: gate: local`, `test_command:` unset ⇒ auto-detect, `require_pr_approval: false` | correct | `scripts/docket-config.sh:163-164` (`FINALIZE_GATE` default `local`, `FINALIZE_TEST_COMMAND` unset ⇒ empty); `skills/docket-finalize-change/SKILL.md:107,119-126` (`require_pr_approval` default `false`) — see also the discrepancy noted below: `docket-config.sh` does not itself emit a `REQUIRE_PR_APPROVAL`/similar variable. Relocated from baseline l.85-88. |
| 38 | Configuration § `.docket.yml`, yaml (l.179) | `# agent_harnesses: [claude] # harnesses the per-repo agent pass generates machine-local files for` | correct | `sync-agents.sh:124-152` (`resolve_agent_harnesses`, default `"claude"`). Relocated from baseline l.89. |
| 39 | Configuration § `.docket.yml` (l.185) | "With no `.docket.yml` at all, docket runs in its default **docket-mode** (`metadata_branch: docket`, `integration_branch: auto`)" | correct | `scripts/docket-config.sh:99` (absent file ⇒ empty `CFG` ⇒ all defaults), `:148,155-158`. Relocated from baseline l.95. |
| 40 | Configuration § `.docket.yml` (l.162) | "`.docket.yml` is committed (not gitignored) because it governs cross-agent coordination" | correct | `skills/docket-convention/SKILL.md:16`. **Task 4 note:** the baseline had this as its own trailing sentence (l.97, right before the "Global config" H2); the restructure folded it into the end of the same paragraph as #29 (l.162: "...It is committed (not gitignored) for that reason."). |
| 41 | Configuration, chapeau (l.151,158) | "Every key resolves per key, across up to four layers, with precedence repo-local > repo-committed > global > built-in... Map-valued keys (`skills:`, `agents:`) merge field-by-field" | correct | `scripts/docket-config.sh:163-165` (per-key `lcl`→`CFG`→`gbl`→default chain), `:201-206` (`skill_role` field-by-field); `sync-agents.sh:265-285` (`resolve_agent_layers` field-by-field). **Task 4 note:** this general precedence statement moved from the old "Global config" H2 body (baseline l.101) up to the new "Configuration" H2's opening paragraph (l.151-158), since it now governs three subsections (`.docket.yml`, global config, `.docket.local.yml`), not just the global-config one. |
| 42 | Configuration § Global config (l.203) | "`${XDG_CONFIG_HOME:-~/.config}/docket/config.yml`. It accepts the **same schema as `.docket.yml`**" | correct | `scripts/docket-config.sh:108-110` (`GCFG_DIR`/`GCFG`, read with the same `yaml_get`/`yaml_block_body` readers as `.docket.yml`); `sync-agents.sh:58-60`. Relocated from baseline l.103. |
| 43 | Configuration § Global config, yaml (l.208-209) | `skills: build: auto` resolvable at the global layer | correct | `scripts/docket-config.sh:199,204` (`GSKILLS_BLK` read from `GCFG`). Relocated from baseline l.109. |
| 44 | Configuration § Global config, yaml (l.210-212) | `agents: default: implement-next: {model, effort}` resolvable at the global layer | correct | `sync-agents.sh:265-285` (`resolve_agent_layers` — line 271, `harness_agent_line "$f" default "$agent" 1`, resolves the `agents: default:` shape per layer), `skills/docket-convention/SKILL.md:75` (Global row of the Agent-layer table). Relocated from baseline l.110-112. |
| 45 | Configuration § Global config, yaml (l.217-218) | "`agent_harnesses: [claude]` # scopes `sync-agents.sh`'s user-level pass ONLY... never the per-repo generation pass" | correct | `sync-agents.sh:154-180` (`resolve_global_agent_harnesses` — user-level pass only). Relocated from baseline l.117-118. |
| 46 | Configuration § Coordination keys are per-repo-only (l.247) | "Coordination keys are per-repo-only": `metadata_branch`, `integration_branch`, `changes_dir`, `adrs_dir`, `results_dir`, `github_project`, and the `github` token of `board_surfaces`, all warned-and-ignored when set globally or in `.docket.local.yml` | correct | `scripts/docket-config.sh:139-146` (the 6-key fence loop, applied to both `GCFG` and `LCFG`), `:179-192` (github token fence). **Task 4 note:** this is now its own H3 subsection (baseline: a single paragraph inside "Global config", l.121). |
| 47 | Configuration § When a file is misplaced or malformed (l.251) | "A `~/.config/docket/.docket.yml` is never read — `docket-config.sh` warns and points at `config.yml`" | correct | `scripts/docket-config.sh:127-129`. Relocated from baseline l.123 (also now its own H3). |
| 48 | Configuration § When a file is misplaced or malformed (l.252) | "A malformed/unreadable `config.yml` (or `.docket.local.yml`) warns and falls back to built-ins for that layer only" | correct | `scripts/docket-config.sh:119-122,132-135`. Relocated from baseline l.123. |
| 49 | Configuration § Migrating from `agents.yaml` (l.256) | "the next `sync-agents.sh` (or `install.sh`) run rewrites [`agents.yaml`] under `agents:` in `config.yml` and renames the original to `agents.yaml.migrated`" | correct | `sync-agents.sh:100-114` (`migrate_legacy_global`). Relocated from baseline l.125 (also now its own H3). |
| 50 | Configuration § `.docket.local.yml` (l.223) | "an optional, gitignored sibling... accepts exactly the same **global-able** key set as `config.yml` above" | correct | `scripts/docket-config.sh:112-123` (`LCFG` reader, `lcl()`); `sync-agents.sh:62-68` (`LOCAL_CFG`). Relocated from baseline l.129. |
| 51 | Configuration § Coordination keys are per-repo-only (l.247); yaml comment (l.227-229) | "the coordination-key fence applies here too, verbatim: a fenced key set locally is loudly warned-and-ignored" | correct | `scripts/docket-config.sh:139-146` (loop checks both `GCFG` and `LCFG` identically). **Task 4 note:** the baseline stated this inline in the `.docket.local.yml` prose (l.129); the final README states it twice — once generally in the new "Coordination keys are per-repo-only" H3 (l.247, "...or in a repo's `.docket.local.yml`"), and once concretely in the `.docket.local.yml` yaml block's comment (l.227-229, naming all seven fenced keys). Both are consistent; no drift. |
| 52 | Configuration § `.docket.local.yml`, yaml (l.235-236) | `agent_harnesses: [claude] # can opt a tracking-only repo into per-repo agent generation on its own` | correct | `sync-agents.sh:206-214` (`per_repo_opted_in` checks `LOCAL_CFG` before `DOCKET_YML`). Relocated from baseline l.141. |
| 53 | Configuration § `.docket.local.yml` (l.243) | "Its own path (and every file `sync-agents.sh` generates) is kept out of git by a managed `# docket:generated` block" | correct | `sync-agents.sh:376-378` (`emit_gitignore_block` prints `.docket.local.yml` first). Relocated from baseline l.149. |
| 54 | Why docket § The reconcile superpower (l.81) | reconcile "runs at the *last responsible moment*... after the change is claimed... but before the worktree and plan are created" | correct | `skills/docket-implement-next/SKILL.md:104` ("last responsible moment: right before the worktree is created, after claim"). Relocated from the old standalone "The reconcile superpower" H2 (baseline l.169) into "Why docket"'s H3 of the same name (l.69-94) — the section is now a subsection of "Why docket" rather than its own top-level H2, and the reconcile motivation/problem framing (baseline l.153-167) was tightened into "Why docket"'s existing narrative. |
| 55 | Why docket § The reconcile superpower (l.83-87) | reconcile re-reads against `related`/recently-archived changes, cited/recent ADRs, current code; rewrites body+spec; dated `## Reconcile log` entry; `reconciled: true` audit record | correct | `skills/docket-implement-next/SKILL.md:45` (Step 3 — Reconcile). Relocated from baseline l.171-177. |
| 56 | Why docket § The reconcile superpower (l.91) | "entirely obsolete... killed and the implementer loops back to select the next one" | correct | `skills/docket-implement-next/SKILL.md:47-51` (OBSOLETE → kill, archive/publish/prune sequence, "Loop back to Step 1"). Relocated from baseline l.181. |
| 57 | Why docket § The reconcile superpower (l.92) | "fundamentally invalidated... the implementer stops and escalates to you" | correct | `skills/docket-implement-next/SKILL.md:57` ("FUNDAMENTALLY invalidated (not just scope-adjustable) → STOP and escalate to the human"). Relocated from baseline l.182. |
| 58 | Why docket § The reconcile superpower (l.94) | "On any resume of an `in-progress` change, if `reconciled` is still `false`... the full reconcile pass runs again" | correct | `skills/docket-implement-next/SKILL.md:114` (resume-safety guard). Relocated from baseline l.188. |
| 59 | docket-mode, two-branch model (l.268) | orphan `docket` branch "is created with `git checkout --orphan docket` (the same well-trodden pattern as `gh-pages`)" | **fix — confirmed applied** | No code path anywhere runs `git checkout --orphan`. `migrate-to-docket.sh:247` uses `git worktree add --orphan -b docket "$DOCKET_WT"`; a fresh repo's bootstrap creation (`scripts/docket-config.sh:49-57`, `create_orphan()`) uses plumbing only (`git mktree` + `git commit-tree` + push), no working-tree checkout at all. **Verified in the final README:** l.268 now reads "(There is no `git checkout --orphan` in the flow: `migrate-to-docket.sh` creates the branch with `git worktree add --orphan`, and a fresh repo's bootstrap builds it straight from git plumbing — `git mktree` + `git commit-tree` — with no working-tree checkout at all.)" — the `gh-pages` analogy is kept (still directionally sound: orphan, no shared history) but the wrong literal command is gone. |
| 60 | docket-mode, two-branch model (l.268) | docket branch "is **always pushed**" | correct | `migrate-to-docket.sh:273-281` (push guard); `scripts/docket-config.sh:54` (`create_orphan` pushes immediately); `skills/docket-convention/SKILL.md:378-380` ("always pushed to its remote immediately"). Same line as #59 (fix and this claim share the same bullet). |
| 61 | docket-mode, two-branch model (l.269) | integration branch "stays code-only, except for **published terminal records**... a copy of the archived change + spec + accepted ADRs" | correct | `scripts/terminal-publish.sh:85-113` (copy-set assembly: change file, `spec:`, Accepted-gated ADRs). Relocated from baseline l.199. |
| 62 | docket-mode, two-branch model (l.271) | "A change's `feat/<slug>` branch is always cut from `origin/<integration_branch>`, carries only plan + results + code, and never modifies docket metadata" | correct | `skills/docket-implement-next/SKILL.md:61-68` (cut from `origin/<integration_branch>`), `:126` ("never modifies docket metadata"). Relocated from baseline l.203. |
| 63 | docket-mode, artifact table (l.277) | Change file lives on `docket`; reaches integration via terminal-publish copy (`done`/`killed`); yes (archived) | correct | `scripts/terminal-publish.sh:85-113` (`--id` mode). Relocated from baseline l.209. |
| 64 | docket-mode, artifact table (l.278) | Spec lives on `docket`; terminal-publish copy; yes | correct | `scripts/terminal-publish.sh:95,98` (`spec_path`, included iff non-empty). Relocated from baseline l.210. |
| 65 | docket-mode, artifact table (l.279) | ADR lives on `docket`; terminal-publish copy, gated on `Accepted`; yes (the Accepted ADRs) | correct | `scripts/terminal-publish.sh:100-113` (the Accepted gate). Relocated from baseline l.211. |
| 66 | docket-mode, artifact table (l.280) | `BOARD.md` lives on `docket`; "Never" published; view on `docket` | correct | `scripts/terminal-publish.sh` never references `BOARD.md` in any copy-set; `scripts/terminal-publish.md:15-16` states it explicitly. Relocated from baseline l.212. |
| 67 | docket-mode, artifact table (l.281-283) | Plan/Results/Code live on the feature branch; reach integration via PR merge; yes (`done` only) | correct | `skills/docket-implement-next/SKILL.md:68` (plan written to `docs/superpowers/plans/` on the feature branch); manifest comment `plan:`/`results:` field semantics, `skills/docket-convention/SKILL.md:258-259`. Relocated from baseline l.213-215. |
| 68 | docket-mode, artifact table (l.284) | `.docket.yml` lives on the default branch; only on integration if default IS integration (trunk); under GitFlow stays on `main`, not `develop` | correct | `scripts/docket-config.sh:93-95` (`DEFAULT_BRANCH`, from `origin/HEAD`) computed independently of `:155-158` (`INTEGRATION_BRANCH`, from `.docket.yml`) — the two can differ, matching the GitFlow claim; `skills/docket-convention/SKILL.md:43`. Relocated from baseline l.216. |
| 69 | docket-mode § `integration_branch` and GitFlow (l.288-295) | `integration_branch: auto` follows `origin/HEAD`, "falling back to `main` if it can't be detected"; the GitFlow caveat that `auto` follows the *default* branch | **fix — confirmed applied** (same drift as #31) | `scripts/docket-config.sh:91-95` fails closed rather than falling back to `main`; the GitFlow-caveat framing itself is accurate (confirmed by #68's evidence). **Verified in the final README:** l.292 now reads "...If `origin/HEAD` can't be resolved, the per-skill runtime resolver (`docket-config.sh`) fails closed with a diagnostic rather than guessing a branch. (Only the one-time `migrate-to-docket.sh` bootstrap falls back to `main` in that case — and that runs once, before a repo is even migrated.)" — the wrong "falling back to main" clause for the runtime resolver is gone, and the parenthetical correctly scopes the real fallback to the one-time migration bootstrap only. The GitFlow caveat (l.295) is unchanged and was already accurate. |
| 70 | docket-mode § The `.docket/` metadata worktree (l.299) | "`.docket/` worktree... synced to `origin/docket` before any read. Your main working tree never switches branches" | correct | `skills/docket-implement-next/SKILL.md:29` (`.docket/` sync via `fetch`+`pull --rebase`). Relocated from baseline l.229-231. |
| 71 | docket-mode § The `.docket/` metadata worktree (l.301-305) | `.docket/` gitignored, kept separate from `.worktrees/` (slug-collision / lifecycle / cleanup-blast-radius reasons) | correct | `migrate-to-docket.sh:331` (`.docket/` + `.worktrees/` added to `.gitignore`); `scripts/cleanup-feature-branch.sh:2-4,33-42` (provenance guard scoped to `.worktrees/<slug>` only, never `.docket/`). Relocated from baseline l.233-237. |
| 72 | docket-mode § Finalize → selective publish (l.309) | "terminal-publish... a selective **file copy**, never a branch merge... the live board stays on `docket` and is never published" | correct | `scripts/terminal-publish.sh:144` (`git checkout ... -- <copyset>`, never `git merge`); no `BOARD.md` reference anywhere in the script. Relocated from baseline l.241. |
| 73 | docket-mode § Migrating an existing repo (l.313-318) | `migrate-to-docket.sh` "operates on the git repo containing your **current directory**"; usage `cd <target-repo> && bash /path/to/docket/migrate-to-docket.sh` | correct | `migrate-to-docket.sh:7,44-62` (`REPO_ROOT` from `git rev-parse --show-toplevel` on `$PWD`). Relocated from baseline l.245-250. |
| 74 | docket-mode § Migrating an existing repo (l.320) | prompts for confirmation, `--yes`/`-y` skips it; creates orphan `docket` seeded from planning dirs; prunes `active/`, changes README, `BOARD.md`; keeps terminal records + build artifacts; adds `.docket/` + `.worktrees/` to `.gitignore` | correct | `migrate-to-docket.sh:52-58` (`--yes`/`-y`), `:126-133` (prompt), `:226-282` (seed set = `SEED_CANDIDATES`), `:291-322` (prune set = `PRUNE_PATHS`), `:325-347` (`.gitignore`). Relocated from baseline l.252. |
| 75 | docket-mode § Migrating an existing repo (l.322-326) | migration grants a local, per-repo Claude Code permission (allow-rule for terminal-publish's push, written to `.claude/settings.local.json`, gitignored); a standalone re-grant via `scripts/ensure-claude-settings.sh` | correct | `migrate-to-docket.sh:369-379`; `scripts/ensure-claude-settings.sh:37,42-47` (`RULE="Bash(git -C * push origin HEAD:$INTEGRATION_BRANCH)"`). Relocated from baseline l.254-258. |
| 76 | docket-mode § Migrating an existing repo (l.328) | "bootstrap guard STOPs and points you at `migrate-to-docket.sh`... detects a half-finished migration and points back" | correct | `scripts/docket-config.sh:224-246` (`BOOTSTRAP=STOP_MIGRATE` for both `¬DOCKET∧LIVE` and `DOCKET∧LIVE`). Relocated from baseline l.260. |
| 77 | docket-mode § `main`-mode (l.330-339) | pinning `metadata_branch: main` + `integration_branch: main` reproduces single-branch behavior exactly: no `docket` branch, no `.docket/` worktree, no terminal-publish copy | correct | `scripts/docket-config.sh:148-153` (`METADATA_WORKTREE=.` for `main`); `scripts/terminal-publish.sh:58-61` (mode-guard no-op when branches are equal). Relocated from baseline l.266-271. |
| 78 | Tuning agent models & effort (l.345) | "Each **autonomous** docket skill runs as a model/effort-pinned subagent (`docket-implement-next`, `docket-auto-groom`, `docket-finalize-change`, `docket-status`, `docket-adr`; the two **interactive** skills... stay inline)" | correct | `agents/` contains exactly these 5 skill-wrapping files with `model:`/`effort:` frontmatter, plus 3 non-skill-wrapping helper agents; no `agents/docket-new-change.md` or `agents/docket-groom-next.md` exists (`ls agents/`). Relocated from baseline l.277 (section itself renamed from "Tuning an agent's model & effort" to "Tuning agent models & effort" — same meaning, TOC-friendlier heading). |
| 79 | Tuning (l.347-351) | 3 config layers (Global `config.yml`, Repo-committed `.docket.yml`, Repo-local `.docket.local.yml`) override the built-in default, precedence repo-local > repo-committed > global > built-in | correct | `skills/docket-convention/SKILL.md:70-77` (Layered config table). Relocated from baseline l.279-283. |
| 80 | Tuning (l.353) | "The config **shape**... is documented once in docket-convention's **'Agent layer'**" | correct | `skills/docket-convention/SKILL.md:64` (heading "Agent layer"). Relocated from baseline l.285. |
| 81 | Tuning (l.355) | `effort: auto` "drops the effort line entirely so the agent inherits the model default. Omitting the `effort:` key instead *keeps* the built-in effort" | correct | `sync-agents.sh:331` (`if (effort!="auto") print "effort: " effort; next` — `auto` drops the line; empty `effort` param, i.e. key omitted at every layer, leaves the built-in `effort:` line untouched). Relocated from baseline l.287. |
| 82 | Tuning (l.359-361) | `bash sync-agents.sh # or re-run install.sh, which calls it for you` | correct | `install.sh:24-25`. Relocated from baseline l.291-293. |
| 83 | Tuning (l.363) | "A **global** edit rewrites user-level wrappers into every **present** harness root" | correct | `sync-agents.sh:480-500` (`user_level_pass`), `:185-199` (`compute_user_targets`, presence-on-disk by default). Relocated from baseline l.295. |
| 84 | Tuning (l.364) | "A repo-committed or repo-local edit rewrites that repo's per-repo wrappers for each harness in its (local-then-committed) `agent_harnesses:` list (default `[claude]`)" | correct | `sync-agents.sh:124-152` (`resolve_agent_harnesses`, default `"claude"`), `:502-541` (`project_level_pass`). Relocated from baseline l.296. |
| 85 | Tuning (l.366) | "`sync-agents.sh` always writes **both** passes in one run... project wins over global at generation time" | correct | `sync-agents.sh:699,702` (`user_level_pass` and `project_level_pass` both called unconditionally per non-`--check` run); `:528` (`resolve_agent_layers` called with `LOCAL_CFG DOCKET_YML GLOBAL_CFG` — local/committed checked before global). Relocated from baseline l.298. |
| 86 | Tuning (l.368) | "Generated per-repo agent files are machine-local — gitignored, never committed"; `sync-agents.sh` owns the `# docket:generated` block; "prints a loud one-time notice to **commit it once**" | correct | `sync-agents.sh:404-423` (`ensure_gitignore_block`, logs "COMMIT THIS so machine-local generated files stay untracked"). Relocated from baseline l.300. |
| 87 | Tuning (l.370) | "Migrating a pre-0051 repo... deletes the stale tracked copies from the working tree, writes the `.gitignore` block, regenerates the local set fresh, and prints the single remedy commit" | correct | `sync-agents.sh:439-456` (`migrate_tracked_wrappers`). Relocated from baseline l.302. |
| 88 | Tuning (l.372-376) | `sync-agents.sh --check` **four-part** gate: `.gitignore` block + no tracked file + no legacy bare-agent-key `agents:` shape (all three CI-meaningful), content drift (advisory only, `rc` unaffected) | correct | `sync-agents.sh:543-602` (`check_project_level`: legs a/b set `rc=1`; leg c only logs `advisory:`; note the function's own header comment at `:543-544` still says "three legs: (a)...(b)...(c)," under-labeling this same fourth check — a pre-existing drift in the script's internal comment, not something this docs-only change can touch); `sync-agents.sh:563-567` (the committed-config-shape leg, sitting unlabeled between legs a and c in the code: `legacy_agent_keys "$DOCKET_YML" 1` — a non-empty result sets `rc=1` and names the offending keys plus the `agents.default.<agent>` reshape remedy); corroborated `tests/test_sync_agents.sh:1030-1031` (gitignore-block leg a: missing block, `rc!=0`), `tests/test_sync_agents.sh:1046-1047` (tracked-file leg b: tracked generated file, `rc!=0`), `tests/test_sync_agents.sh:613-614` (legacy-shape leg: `--check` flags a committed bare-agent-key `agents:` block with `rc!=0` and names it "legacy"), `tests/test_sync_agents.sh:1057-1058` (advisory-staleness leg c: content drift, `rc=0`, reported as `advisory`). **Review fix pass:** README previously said "three-part," omitting the legacy-shape leg (`sync-agents.sh:563-567`); README now says "four-part" and documents the fourth leg. Relocated from baseline l.304-307. |
| 89 | Tuning (l.378) | "A repo listing `cursor` also gets a generated `.cursor/rules/docket-dispatch.mdc`... `sync-agents.sh --check` covers both the generated agents and the dispatch rule" | correct | `sync-agents.sh:339-363` (`assemble_dispatch_rule`/`write_dispatch_rule`), `:586-598` (dispatch-rule drift check); corroborated `tests/test_sync_agents.sh:181` (rule-check passes for an in-sync committed rule), `tests/test_sync_agents.sh:185` (rule-check advisory-flags a hand-edited rule), `tests/test_sync_agents.sh:192` (rule-check advisory-flags a missing committed rule). Relocated from baseline l.309; line shifted from l.377 to l.378 by the review fix pass's one-line insertion at l.375. |
| 90 | Tuning (l.380) | "The clone-identical guarantee is retired... Generation is now all-local" | correct | `docs/adrs/0020-generated-agent-artifacts-machine-local.md` (Accepted, supersedes ADR-0017); `skills/docket-convention/SKILL.md:128-132`. Relocated from baseline l.311. Task 3's report flagged this paragraph as "mildly diff-anchored" prose; left as-is for meaning-safety per that pass's own note (see *Deviations from plan* below) — verdict and evidence unaffected. Line shifted from l.379 to l.380 by the review fix pass's one-line insertion at l.375. |
| 91 | The eight skills, table (l.390-397) | each skill's one-line role description matches its `SKILL.md` frontmatter `description:` | correct | `skills/docket-new-change/SKILL.md:3`, `skills/docket-groom-next/SKILL.md:3`, `skills/docket-auto-groom/SKILL.md:3`, `skills/docket-implement-next/SKILL.md:3`, `skills/docket-finalize-change/SKILL.md:3`, `skills/docket-status/SKILL.md:3`, `skills/docket-adr/SKILL.md:3`, `skills/docket-convention/SKILL.md:3`. Relocated from baseline l.319-326; line range shifted from l.389-396 to l.390-397 by the review fix pass's one-line insertion at l.375. |
| 92 | The eight skills (l.384,386) | section title/count "eight" matches the actual skill count | correct | same as #1 — no drift on the count guard; confirmed again by re-running `grep -in "eight\|seven\|six\b" README.md` post-fix-pass (3 hits, all "eight", all describing this same 8-skill set: TOC l.23, heading l.384, body l.386). Relocated from baseline l.315; heading/body lines shifted from l.383/l.385 to l.384/l.386 by the review fix pass's one-line insertion at l.375. |
| 93 | Status (l.403) | "docket-mode is the supported default... the bootstrap guard refuses to run against an un-migrated repo rather than touching your data" | correct | `scripts/docket-config.sh:148` (`metadata_branch` default `docket`), `:224-246` (`STOP_MIGRATE` verdict, never auto-mutates). Relocated from baseline l.332; line shifted from l.402 to l.403 by the review fix pass's one-line insertion at l.375. |
| 94 | Status (l.405) | "`main`-mode remains a simple, fully-supported opt-out: pin `metadata_branch: main` (and `integration_branch: main`)" | correct | `scripts/docket-config.sh:148-153`. Relocated from baseline l.334; line shifted from l.404 to l.405 by the review fix pass's one-line insertion at l.375. |
| 95 | — (cut, absent) | "Markhaus is the first planned dogfood project; a migration plan exists to move its existing changes and ADRs into the docket format" | **cut — confirmed absent** | Baseline l.336. **Verified in the final README:** `grep -in "markhaus" README.md` returns no matches. The line was unverifiable from this repo (only a *plan* document exists, `docs/superpowers/plans/2026-05-30-markhaus-docket-migration.md`, describing a migration of an external repo this repo cannot confirm ran or is current) and was cut per the audit's own recorded decision, not merely relocated. |
| 96 | Table of contents (l.14-25) | 9 links to `#how-it-works`, `#why-docket`, `#install`, `#quickstart-the-daily-loop`, `#configuration--docketyml-global-config-and-machine-local-overrides`, `#docket-mode-where-metadata-lives`, `#tuning-agent-models--effort`, `#the-eight-skills`, `#status` all resolve to real H2 headings | correct | Manually computed the GitHub heading-slug algorithm (lowercase; strip characters that are not `[a-z0-9_ -]`; spaces → hyphens; **no** collapsing of resulting double hyphens) against every H2 in the final README (`grep -n "^## " README.md`, 9 headings) and matched each to its TOC anchor 1:1, including the two double-hyphen edge cases: "Configuration — `.docket.yml`, ..." (em-dash + backticked segment both strip to nothing, producing `configuration--docketyml-...`, l.20) and "Tuning agent models & effort" (`&` strips to nothing, producing `tuning-agent-models--effort`, l.22). Both match the literal anchors in the TOC exactly. |
| 97 | Install § Prerequisites (l.104) | "Claude Code (or another supported agent harness)... each with its own on-disk `skills/` and `agents/` directories that docket writes into" | correct | `link-skills.sh:20-27` (`HARNESS_SKILL_DIRS`: `.claude`, `.codex`, `.cursor`, `.agents`, `.kiro`, `.windsurf` under `$HOME`); `sync-agents.sh:71-77` (`HARNESS_AGENT_DIRS`, the same 6 harnesses, skills→agents). Claude Code and Cursor (the two harnesses the README names as examples elsewhere, e.g. l.363) are both present in this set. |
| 98 | Install § Prerequisites (l.105) | "`git` and the GitHub CLI (`gh`). Every docket operation is a git operation, and the implementer opens pull requests with `gh`" | correct | No non-git network calls exist in `scripts/*.sh`/`install.sh`/`migrate-to-docket.sh` other than `gh`. docket's own skills invoke `gh` throughout the PR lifecycle: `skills/docket-finalize-change/SKILL.md:64` (`gh pr merge`), `:70` (`gh pr view <pr> --comments`), `:203` (`gh pr comment`); `skills/docket-status/SKILL.md:131` (`gh pr list --head feat/<slug>`). `docket-implement-next` directs the resolved finish skill to "push the feature branch and open a PR" (`skills/docket-implement-next/SKILL.md:86`) — PR creation itself is superpowers' `finishing-a-development-branch` skill (external to this repo), consistent with the README's own framing that docket delegates PR-opening mechanics to the workflow engine. |
| 99 | Install § Prerequisites (l.106) | "A GitHub remote for the pull-request flow. docket pushes branches and opens PRs against your `origin`" | correct | `migrate-to-docket.sh:184,191,273-281` (fetches/pushes `origin docket`); `scripts/terminal-publish.sh` and `scripts/ensure-claude-settings.sh:42-47` (`push origin HEAD:$INTEGRATION_BRANCH`) — every push target observed in the scripts is `origin`, never a hard-coded remote name or URL. |
| 100 | Quickstart, step 1 (l.133) | `docket-new-change`: "Describe an idea; docket brainstorms it with you... and commits the change file to the backlog" | correct | `skills/docket-new-change/SKILL.md:45` ("Board, commit & push — refresh `BOARD.md`..., commit the change + spec, and PUSH to `origin/docket`... STOP. Never implements."). |
| 101 | Quickstart, step 2 (l.135) | `docket-groom-next` / `docket-auto-groom`: "(Both write markdown only — never code.)" | correct | `skills/docket-groom-next/SKILL.md:3,10` ("Writes markdown only — never branches, worktrees, or code"); `skills/docket-auto-groom/SKILL.md:3,10` (same phrasing, "It writes markdown only: change files, specs, `BOARD.md` — never branches, worktrees, or code"). |
| 102 | Quickstart, step 3 (l.137) | `docket-implement-next`: "claims the next build-ready change, reconciles it against current reality..., builds it with test-driven development, opens a pull request, and stops. It never merges" | correct | `skills/docket-implement-next/SKILL.md:10` ("select, claim, reconcile, plan, build, review, PR, stop"); `:86` (finish skill directed to "push the feature branch and open a PR — do NOT merge — then stop"). |
| 103 | Quickstart, step 4 (l.139) | "Read the PR it opened. This is your one required checkpoint — merge it yourself, or let `docket-finalize-change` merge it once you have approved it" | correct | `skills/docket-finalize-change/SKILL.md:64` (merges an approved + mergeable PR into the integration branch); `:17` ("A PR was merged via the GitHub button and you want to archive the change immediately" — i.e., self-merge is also a supported path). |
| 104 | Quickstart, step 5 (l.141) | `docket-finalize-change`: "archives the change to `done`, publishes its terminal records, cleans up the branch and worktree, and refreshes the board. (A periodic `docket-status` run also sweeps already-merged PRs to `done`...)" | correct | `skills/docket-finalize-change/SKILL.md:10` (harvest → archive → publish → clean up branch/worktree → refresh board sequence); `skills/docket-status/SKILL.md:10,125` ("sweep any `implemented` change whose PR merged into the archive"; "The bulk safety net: sweep every `implemented` change whose PR has merged into the archive"). |
| 105 | Quickstart, closing line (l.143) | "`docket-status` keeps the board honest in between" | correct | `skills/docket-status/SKILL.md:10` ("`docket-status` gives you a queryable, up-to-date view of the backlog and keeps it clean. It has three jobs: render `BOARD.md`..., sweep..., and run health checks..."). |
| 106 | How it works (l.37) | build-ready means a change "is `proposed`, has a written spec (or is marked `trivial`...), and all its dependencies have already merged" | correct | `skills/docket-convention/SKILL.md:332-334` ("### Build-readiness & selection (shared definition)" — "A change is **build-ready**... only when it is `proposed`, has a `spec:` **or** `trivial: true`, and all `depends_on` are satisfied (`done`)"); `skills/docket-convention/SKILL.md:254` (`depends_on: [4] # change ids that must reach \`done\` (PR merged) first` — "reach `done` (PR merged)" is exactly "have already merged"). **Review fix pass:** new row — this build-readiness clause had no audit row before. |
| 107 | How it works § The change lifecycle (l.43-49) | "the status walks a fixed happy path: `proposed → in-progress → implemented → done`, with three off-ramps: `blocked`..., `deferred`..., and `killed`...; a `proposed` change [with no spec, not trivial] sits in a **needs-brainstorm** state" | correct | `skills/docket-convention/SKILL.md:305-313` ("### Lifecycle — seven states" — the diagram's happy path `proposed ──claim──▶ in-progress ──PR open──▶ implemented ──merge+sweep──▶ done` plus the `blocked`/`deferred`/`killed` off-ramp branches match the README's four-state path and three off-ramps exactly; seven statuses total in both); `skills/docket-convention/SKILL.md:334` ("A `proposed` change with neither a spec nor `trivial: true` is **needs-brainstorm** (not build-ready)"). **Review fix pass:** new row — the change-lifecycle subsection (README l.41-51) had no audit row before. |
| 108 | How it works § The change lifecycle (l.51) | "The board — a generated `BOARD.md` — is the at-a-glance view of every change grouped by status; you regenerate it with `docket-status`" | correct | `skills/docket-status/SKILL.md:10` ("`docket-status` gives you a queryable, up-to-date view of the backlog... render `BOARD.md` from the change files"). **Review fix pass:** new row — the board-regeneration sentence had no audit row before. |

**Row count: 108** (105 prior + 3 new, review fix pass). Verdict counts: **104 `correct`**, **3 `fix`** (#31, #59, #69 — all three re-verified against the final README text and confirmed applied), **1 `cut`** (#95 — re-verified absent from the final README by direct grep). Coverage check: every `## ` (h2) section in the final README has ≥1 row — How it works #7-11,106-108; Why docket #3-6,54-58; Install #19-28,97-99; Quickstart #100-105; Configuration #12-18,29-53; docket-mode #59-77; Tuning agent models & effort #78-90; The eight skills #91-92; Status #93-94; Table of contents #96. All 4 non-`correct` verdicts carry file:line or explicit-absence evidence, each independently re-verified against the actual final README text (not just the plan's stated intent) as part of this task. **Review fix pass note:** rows #106-108 close the coverage gap flagged by a whole-branch review — the "How it works" H2 previously had no row for its "The change lifecycle" H3 subsection (README l.41-51: the four-state happy path, the three off-ramps, the needs-brainstorm state, and board regeneration) or for the build-readiness clause at l.37; a prior reviewer pre-verified the underlying file:line matches (seven states = `skills/docket-convention/SKILL.md:305-313`; "dependencies have already merged" = `SKILL.md:254`; build-readiness definition = `SKILL.md:332-334`), reconfirmed here.

## Suite results

**Step 1 — full suite, run to completion (2026-07-10):**

```
$ cd /Users/homer/dev/docket/.worktrees/readme-doc-critic-refresh
$ fail=0; for t in tests/test_*.sh; do bash "$t" >"/tmp/$(basename "$t").out" 2>&1 || { echo "FAIL: $t"; fail=1; }; done; echo "suite fail=$fail"
suite fail=0
$ grep -l "NOT OK" /tmp/test_*.out || echo "no NOT OK anywhere"
no NOT OK anywhere
```

All 28 `tests/test_*.sh` files ran to completion (including the slow `test_sync_agents.sh`, several minutes of sandboxed generation runs, as expected per the global constraints). Zero failures; zero `NOT OK` occurrences anywhere. No pre-existing-failure comparison against `d7f4a96` was needed since there was nothing to diagnose.

The four sentinel-inventory files were also re-run individually per the global-constraints check command, confirming `rc=0` and `NOT OK` count `0` for each:

```
test_docket_metadata_branch: rc=0   0
test_ensure_claude_settings: rc=0   0
test_results_artifact: rc=0         0
test_sync_agents: rc=0              0
```

**Step 4 — count guard:**

```
$ grep -in "eight\|seven\|six\b" README.md
23:- [The eight skills](#the-eight-skills)
384:## The eight skills
386:The eight skills cover the full loop — create, groom, implement, finalize, report, decide — plus the shared contract they all load.
```

Three hits, all "eight," all describing the same 8-skill set (`ls skills/` confirms 8 directories). No "seven" or "six" hits. Guard passes — the restructure's consolidation of the count into one section (row #1/#3's Task 4 note) means there is now exactly one place this number is asserted, which *reduces* future count-guard risk relative to the baseline (which stated "eight" in three separate places: lead, "What docket is," and "The eight skills"). **Review fix pass:** re-ran this exact command after the fix pass's one-line insertion at l.375; the two hit lines shifted from 383/385 to 384/386 (same text, same guard-passing verdict).

## Spec/reality discrepancies

- **`integration_branch: auto`'s "fallback main" is not implemented in the runtime resolver.** The final README (l.168, l.292) has been **fixed** in this change (rows #31/#69) to describe the actual fail-closed behavior. `skills/docket-convention/SKILL.md` (l.21, l.43) still describes `auto` as falling back to `main` when `origin/HEAD` can't be detected — this is a **pre-existing drift in the shared convention doc**, out of scope for this docs-only README change (global constraints forbid touching `skills/`). **Review fix pass:** a third location carries the same drift — `skills/docket-finalize-change/SKILL.md:64` ("targeting the change's PR against **`<integration_branch>`** (resolved from `.docket.yml`; default `auto` → `origin/HEAD`, fallback `main`)") — also out of scope here for the same reason. Recorded here per LEARNINGS #21; not silently fixed.
- **`finalize.require_pr_approval` is real but absent from the single config resolver's exported set.** `scripts/docket-config.sh --export` is documented as "the single implementation the skills run instead of re-deriving this each session" (`skills/docket-convention/SKILL.md:47`), and its emit list (`scripts/docket-config.sh:249-267`) covers `FINALIZE_GATE` and `FINALIZE_TEST_COMMAND` but has no `REQUIRE_PR_APPROVAL`/equivalent entry. `require_pr_approval` is nonetheless real and implemented directly inside `skills/docket-finalize-change/SKILL.md:33-123` (change 0021 / ADR-0011). The README's line about it (final README l.178) is **kept** (per the global-constraints instruction: it is real, keep it) — this entry records the gap between the convention's documented single-source resolver and this key's actual (skill-internal, not `docket-config.sh`-exported) implementation as a follow-up, not a README defect.

## Deviations from plan

- The task brief's file list scoped read-only inputs to `README.md`, `scripts/*.sh`+`scripts/*.md`, `skills/docket-convention/SKILL.md`, `skills/docket-finalize-change/SKILL.md`, `install.sh`, `migrate-to-docket.sh`, `agents/docket-*.md`, `tests/*.sh`. Verifying the reconcile-superpower section and the producer/implementer claims required reading the *implementing* skills not in that list — `skills/docket-implement-next/SKILL.md` and `skills/docket-new-change/SKILL.md` — because those behaviors are specific to those skills and are not restated in `docket-convention`. This is a read-only deviation (no file outside the allowed-to-modify set was written), directly in service of LEARNINGS #47 ("verify against code, not sibling prose").
- Also read (research only, not modified): `skills/docket-status/SKILL.md`, `skills/docket-groom-next/SKILL.md`, `skills/docket-auto-groom/SKILL.md` (full bodies, for Task 4's Quickstart/Prerequisites rows #97-105), `docs/adrs/0015-0020`, and `scripts/lib/docket-frontmatter.sh`, `scripts/board-checks.sh`, `scripts/render-board.sh`, `scripts/archive-change.sh`, `scripts/cleanup-feature-branch.sh`, `scripts/sync-integration-branch.sh`, `scripts/github-mirror.sh`, `scripts/ensure-claude-settings.sh` — none in the brief's list, all needed to find file:line evidence for claims those scripts (not the ones in the read-only list) actually implement.
- **Task 3's flagged prose note** (carried forward, not independently re-litigated): the final README's clone-identical-guarantee paragraph (l.380, l.379 before the review fix pass's one-line insertion at l.375) reads mildly diff-anchored ("Before this change, committing the generated per-repo files meant... Generation is now all-local, so that guarantee is gone") but was deliberately left as-is by the prose pass for meaning-safety — it accurately narrates a real behavior change (ADR-0020) rather than describing docket's current behavior in a vacuum, and rewording risked losing that precision. Task 4 re-confirmed the underlying claim (#90) is still evidenced and correct; no further action taken.
- No README content beyond the Step-2 reconciliation itself was changed in Task 4 — the "fallback main" fixes (#31, #59, #69) and the Markhaus cut (#95) were already applied in Task 2 (`a3e5f43`) and reconfirmed here, not newly applied by this task. Task 4's `git add README.md` in the Step-5 commit is a no-op safety net per the brief's own note.

## Follow-up candidates

- Fix the "auto → origin/HEAD, fallback main" drift in `skills/docket-convention/SKILL.md` (l.21, l.43) **and** `skills/docket-finalize-change/SKILL.md` (l.64) to match `docket-config.sh`'s actual fail-closed behavior — out of scope for this docs-only README change (global constraints forbid touching `skills/`).
- Wire `require_pr_approval` into `scripts/docket-config.sh --export`'s emitted set (or explicitly document in `docket-convention` that it is read directly by `docket-finalize-change` rather than through the shared resolver) so the "single implementation" claim in `docket-convention` stays accurate as more finalize-only keys are added.
- Consider whether the docket-mode "orphan `docket` branch" description should name both real creation paths (`migrate-to-docket.sh`'s `git worktree add --orphan`, and `docket-config.sh --bootstrap`'s plumbing-only `mktree`/`commit-tree`) rather than a single illustrative command — the current parenthetical (l.268) already does this concisely; a future change could promote it to a fuller aside if newcomers report confusion.
- `docket-config.sh --export`'s emitted-variable set (row #37's evidence) is a natural place to also emit `REQUIRE_PR_APPROVAL` if the second follow-up above is picked up — bundling both into one small change would resolve two related discrepancies at once.

## Merge-gate checks for the human

Automated verification (95+10 claims audited against code, full test suite green, count guard passed) covers correctness of individual claims. These are the things only a human skimming the rendered page will catch:

1. **Render the README on GitHub and click every Table of contents anchor** (l.16-24, 9 links). Row #96 verified the anchors resolve by hand-computing GitHub's slug algorithm — worth one real click-through per link, since GitHub's actual slugger has occasionally surprised even careful hand-computation (e.g. the double-hyphen cases at the "Configuration" and "Tuning" entries).
2. **Skim the Quickstart (l.129-143) as a newcomer who has never run docket**, ignoring everything you already know. Check that the 5 numbered steps read as a coherent day-one narrative and that nothing assumes context only established later in the README (e.g. "docket-mode," referenced in the Quickstart's opening line, is a forward link to a section that appears three H2s later).
3. **Spot-check that the two "count" surfaces agree without you doing the arithmetic**: the Table of contents says "The eight skills" (l.23) and the section itself lists exactly 8 rows (l.390-397) — glance at both and confirm they still match after any future edit; this is the guard this task's Step 4 automated, but a human's eyeball catch on a rendered page is the cheaper final check before merge.
