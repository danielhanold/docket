# Results — 0052 README doc-critic refresh

**Date started:** 2026-07-10
**Change:** 0052 · **Branch:** feat/readme-doc-critic-refresh
**Baseline:** README.md @ origin/main d7f4a96 (336 lines, post-0051)

## Claims audit

| # | README § (line) | Claim | Verdict | Evidence |
|---|---|---|---|---|
| 1 | Lead (l.3) | docket "provides eight skills" | correct | `ls skills/` = 8 dirs: docket-new-change, docket-groom-next, docket-auto-groom, docket-implement-next, docket-finalize-change, docket-status, docket-adr, docket-convention |
| 2 | Lead (l.3) | "all coordinated through git, no CLI or database" | correct | repo root has no CLI entrypoint/binary or DB code; every mechanism is git commits/branches/worktrees driven by `scripts/*.sh` (plumbing only) |
| 3 | What docket is (l.13) | "a thin lifecycle layer — plain markdown files in your repo, eight skills, no CLI" | correct | same evidence as #1; `skills/*/SKILL.md` files are the only entry points, no binary/CLI shipped |
| 4 | What docket is (l.13) | "each invocation point is rebindable via `skills:` in `.docket.yml`" | correct | `scripts/docket-config.sh:195-221` (`skill_role()` resolves brainstorm/plan/build/review/finish from the `skills:` block) |
| 5 | What docket is (l.13) | "The code is always the current-state truth; docket carries no living-spec layer" | correct | `docs/adrs/` is an immutable decision ledger (`skills/docket-convention/SKILL.md:303`, "An `Accepted` ADR is immutable except its `status:` line"); no auto-generated "current architecture" doc exists anywhere in the convention or scripts |
| 6 | What docket is (l.15) | "The eight skills cover the full loop: create, groom, implement, finalize, report, decide — plus the shared contract" | correct | `skills/docket-new-change/SKILL.md:3` (create), `skills/docket-groom-next/SKILL.md:3` + `skills/docket-auto-groom/SKILL.md:3` (groom), `skills/docket-implement-next/SKILL.md:3` (implement), `skills/docket-finalize-change/SKILL.md:3` (finalize), `skills/docket-status/SKILL.md:3` (report), `skills/docket-adr/SKILL.md:3` (decide), `skills/docket-convention/SKILL.md:3` (shared contract) |
| 7 | Producer/implementer loop, table (l.25) | `docket-new-change` — "Interactive — you brainstorm changes into a build-ready backlog" | correct | `skills/docket-new-change/SKILL.md:3,10` |
| 8 | Producer/implementer loop, table (l.26) | `docket-implement-next` — "Autonomous — drains the backlog to open PRs, unsupervised" | correct | `skills/docket-implement-next/SKILL.md:3,10` |
| 9 | Producer/implementer loop (l.28) | "the producer only mints new `proposed` change ids; the implementer claims the next build-ready change atomically (compare-and-swap on the status field)" | correct | `skills/docket-new-change/SKILL.md:10` ("only ever mints new `proposed` ids — scanning the max existing id and incrementing"); `skills/docket-implement-next/SKILL.md:35,37` ("Step 2 — Claim (compare-and-swap)") |
| 10 | Producer/implementer loop (l.30) | "The human stays in the loop at two points only — writing the change and merging the PR" | correct | `skills/docket-new-change/SKILL.md:10` (human brainstorms/writes); `skills/docket-implement-next/SKILL.md:91` ("STOP. The change stays in `active/` as `implemented` until a human merges it") |
| 11 | Producer/implementer loop (l.32) | "dependency chains serialize on the merge gate... the board surfaces this as 'waiting on #N — needs your merge'" | correct | `scripts/lib/docket-frontmatter.sh:16` (`DEP_REASON[id]` = `"not yet built"` \| `"needs your merge"`); `scripts/board-checks.sh:87`; `scripts/render-board.sh:90` |
| 12 | Workflow engine (l.38) | "Each of the five workflow invocation points is a pluggable role... rebinds any of them to a different skill... or to the sentinel `auto`" | correct | `scripts/docket-config.sh:195-221` (5 roles resolved: brainstorm/plan/build/review/finish; `auto` is a literal value, not specially parsed — it flows through as the skill name and is checked by the invoking skill, per `skills/docket-convention/SKILL.md:206-209`) |
| 13 | Workflow engine, table (l.42) | `brainstorm` → default `superpowers:brainstorming` | correct | `scripts/docket-config.sh:207` |
| 14 | Workflow engine, table (l.43) | `plan` → default `superpowers:writing-plans` | correct | `scripts/docket-config.sh:208` |
| 15 | Workflow engine, table (l.44) | `build` → default `superpowers:subagent-driven-development` | correct | `scripts/docket-config.sh:209` |
| 16 | Workflow engine, table (l.45) | `review` → default `superpowers:requesting-code-review` | correct | `scripts/docket-config.sh:210` |
| 17 | Workflow engine, table (l.46) | `finish` → default `superpowers:finishing-a-development-branch`, invoked by `docket-implement-next` and `docket-finalize-change` | correct | `scripts/docket-config.sh:211`; `skills/docket-finalize-change/SKILL.md:208-210` ("Where finishing-a-development-branch fits") |
| 18 | Workflow engine (l.48) | unset `skills:` keys default to the superpowers skills; a resolved-but-unavailable skill degrades to that role's `auto` fallback with a prominent warning | correct | `scripts/docket-config.sh:201-206` (per-key default fallback); `skills/docket-convention/SKILL.md:210-216` ("Missing-skill rule — degrade to auto + warn") |
| 19 | Workflow engine (l.50) | "installing superpowers is the consuming user's responsibility; docket does not bundle or install it" | correct | `install.sh`, `link-skills.sh`, `sync-agents.sh` only reference docket's own `skills/`/`agents/` dirs; no superpowers-fetching code anywhere in the repo |
| 20 | Install (l.59) | `bash ~/dev/docket/install.sh` is "the whole install" | correct | `install.sh:1-30` |
| 21 | Install (l.62) | "`install.sh` runs the three primitives in order — and is idempotent" | correct | `install.sh:4-14,17-30` (calls `link-skills.sh`, `sync-agents.sh`, `scripts/ensure-docket-env.sh` in order; each is independently idempotent) |
| 22 | Install (l.64) | `link-skills.sh` "creates absolute symlinks... only writes into harness directories that already exist" | correct | `link-skills.sh:20-46` (`HARNESS_SKILL_DIRS` loop; skips any harness dir not already present) |
| 23 | Install (l.65) | `sync-agents.sh` generates wrappers from 4-layer config; opted-in repos get the full per-repo agent set as machine-local, gitignored files, never committed | correct | `sync-agents.sh:206-214` (`per_repo_opted_in`), `sync-agents.sh:502-541` (`project_level_pass`, "Always generate the FULL built-in set"), `sync-agents.sh:365-422` (managed `.gitignore` block) |
| 24 | Install (l.65) | "Run `sync-agents.sh --check` in CI to catch a missing/stale `.gitignore` block or an accidentally-tracked generated file" | correct | `sync-agents.sh:543-567` (`check_project_level` legs a/b, both CI-meaningful, `rc=1`) |
| 25 | Install (l.66) | `ensure-docket-env.sh` "exports `DOCKET_SCRIPTS_DIR`... into your shell profile (and Claude Code's user-level `settings.json` `env`)" | correct | `scripts/ensure-docket-env.sh:17-46` (shell profile), `:48-64` (settings.json) |
| 26 | Install (l.66) | "Without it the skills fail loud with the `run docket/install.sh` remedy" | correct | `skills/docket-convention/SKILL.md:49` (`"${DOCKET_SCRIPTS_DIR:?run docket/install.sh}"`) |
| 27 | Install (l.68) | "Migrating an *existing* repo to docket-mode is a separate step — `migrate-to-docket.sh`, run from inside that repo" | correct | `migrate-to-docket.sh:7,44-62` (`REPO_ROOT` resolved from `$PWD` via `git rev-parse --show-toplevel`) |
| 28 | Install (l.70) | "The change data... lives per consuming project, not in the docket repo itself" | correct | none of `install.sh`/`link-skills.sh`/`sync-agents.sh` reference `docs/changes`, `docs/adrs`, or `docs/results` paths — only `scripts/`/`agents/`/`skills/` are the docket-repo-owned machinery |
| 29 | Install (l.72-73) | ".docket.yml... committed on your repo's default branch (`origin/HEAD`)... every clone, agent, and device needs the same values" | correct | `skills/docket-convention/SKILL.md:16` ("committed... never gitignored"), `:43` ("lives on the repo's default branch (`origin/HEAD`)") |
| 30 | Install, yaml (l.77) | `metadata_branch: docket # docket (default) \| main` | correct | `scripts/docket-config.sh:148-153` (`METADATA_BRANCH` default `"docket"`) |
| 31 | Install, yaml (l.78) | `integration_branch: auto # auto (default → origin/HEAD, fallback main) \| main \| develop` | **fix** | `scripts/docket-config.sh:91-95` **fails closed** (`die`) when `origin/HEAD` is unresolvable — it never falls back to `"main"`. Its own header states this is deliberate: "Fail-closed: non-zero + stderr diagnostic on a hard error (unreachable origin, unresolvable origin/HEAD...)" (`scripts/docket-config.sh:7-8`). Only `migrate-to-docket.sh:76-82`'s one-time bootstrap resolver (`default_integration_branch()`, `${head:-main}`) has a real "fallback to main." `docket-config.sh` is the resolver every docket skill actually calls at every startup (the "single implementation," `skills/docket-convention/SKILL.md:47`), so the README's "fallback main" phrasing describes migration-time behavior, not runtime behavior. Same drift pre-exists in `skills/docket-convention/SKILL.md:21,43` — see *Spec/reality discrepancies*. |
| 32 | Install, yaml (l.79) | `changes_dir: docs/changes # default` | correct | `scripts/docket-config.sh:160` |
| 33 | Install, yaml (l.80) | `adrs_dir: docs/adrs # default` | correct | `scripts/docket-config.sh:161` |
| 34 | Install, yaml (l.81) | `results_dir: docs/results # default` | correct | `scripts/docket-config.sh:162` |
| 35 | Install, yaml (l.82) | `auto_groom: false # repo default...per-change auto_groomable overrides` | correct | `scripts/docket-config.sh:165` (default `false`); `skills/docket-convention/SKILL.md:338` (effective auto-groomable definition) |
| 36 | Install, yaml (l.83) | `board_surfaces: [inline] # ...inline (BOARD.md) and/or github; [] disables the board` | correct | `scripts/docket-config.sh:167-193` (default `"inline"`); `skills/docket-convention/SKILL.md:53` (`[]` disables the board entirely) |
| 37 | Install, yaml (l.85-88) | `finalize: gate: local`, `test_command:` unset ⇒ auto-detect, `require_pr_approval: false` | correct | `scripts/docket-config.sh:163-164` (`FINALIZE_GATE` default `local`, `FINALIZE_TEST_COMMAND` unset ⇒ empty); `skills/docket-finalize-change/SKILL.md:107,119-126` (`require_pr_approval` default `false`) — see also the discrepancy noted below: `docket-config.sh` does not itself emit a `REQUIRE_PR_APPROVAL`/similar variable |
| 38 | Install, yaml (l.89) | `# agent_harnesses: [claude] # harnesses the per-repo agent pass generates machine-local files for` | correct | `sync-agents.sh:124-152` (`resolve_agent_harnesses`, default `"claude"`) |
| 39 | Install (l.95) | "With no `.docket.yml` at all, docket runs in its default **docket-mode** (`metadata_branch: docket`, `integration_branch: auto`)" | correct | `scripts/docket-config.sh:99` (absent file ⇒ empty `CFG` ⇒ all defaults), `:148,155-158` |
| 40 | Install (l.97) | "`.docket.yml` is committed (not gitignored) because it governs cross-agent coordination" | correct | `skills/docket-convention/SKILL.md:16` |
| 41 | Global config (l.101) | "Every key resolves per key, across up to four layers, with precedence repo-local > repo-committed > global > built-in... Map-valued keys (`skills:`, `agents:`) merge field-by-field" | correct | `scripts/docket-config.sh:163-165` (per-key `lcl`→`CFG`→`gbl`→default chain), `:201-206` (`skill_role` field-by-field); `sync-agents.sh:265-285` (`resolve_agent_layers` field-by-field) |
| 42 | Global config (l.103) | "`${XDG_CONFIG_HOME:-~/.config}/docket/config.yml`. It accepts the **same schema as `.docket.yml`**" | correct | `scripts/docket-config.sh:108-110` (`GCFG_DIR`/`GCFG`, read with the same `yaml_get`/`yaml_block_body` readers as `.docket.yml`); `sync-agents.sh:58-60` |
| 43 | Global config, yaml (l.109) | `skills: build: auto` resolvable at the global layer | correct | `scripts/docket-config.sh:199,204` (`GSKILLS_BLK` read from `GCFG`) |
| 44 | Global config, yaml (l.110-112) | `agents: default: implement-next: {model, effort}` resolvable at the global layer | correct | `sync-agents.sh:14` (global layer), `skills/docket-convention/SKILL.md:75` (Global row of the Agent-layer table) |
| 45 | Global config, yaml (l.117-118) | "`agent_harnesses: [claude]` # scopes `sync-agents.sh`'s user-level pass ONLY... never the per-repo generation pass" | correct | `sync-agents.sh:154-180` (`resolve_global_agent_harnesses` — user-level pass only) |
| 46 | Global config (l.121) | "Coordination keys are per-repo-only": `metadata_branch`, `integration_branch`, `changes_dir`, `adrs_dir`, `results_dir`, `github_project`, and the `github` token of `board_surfaces`, all warned-and-ignored when set globally or in `.docket.local.yml` | correct | `scripts/docket-config.sh:139-146` (the 6-key fence loop, applied to both `GCFG` and `LCFG`), `:179-192` (github token fence) |
| 47 | Global config (l.123) | "A `~/.config/docket/.docket.yml` is never read — `docket-config.sh` warns and points at `config.yml`" | correct | `scripts/docket-config.sh:127-129` |
| 48 | Global config (l.123) | "A malformed/unreadable `config.yml` (or `.docket.local.yml`) warns and falls back to built-ins for that layer only" | correct | `scripts/docket-config.sh:119-122,132-135` |
| 49 | Global config (l.125) | "the next `sync-agents.sh` (or `install.sh`) run rewrites [`agents.yaml`] under `agents:` in `config.yml` and renames the original to `agents.yaml.migrated`" | correct | `sync-agents.sh:100-114` (`migrate_legacy_global`) |
| 50 | `.docket.local.yml` (l.129) | "an optional, gitignored sibling... accepts exactly the same **global-able** key set as `config.yml` above" | correct | `scripts/docket-config.sh:112-123` (`LCFG` reader, `lcl()`); `sync-agents.sh:62-68` (`LOCAL_CFG`) |
| 51 | `.docket.local.yml` (l.129) | "the coordination-key fence applies here too, verbatim: a fenced key set locally is loudly warned-and-ignored" | correct | `scripts/docket-config.sh:139-146` (loop checks both `GCFG` and `LCFG` identically) |
| 52 | `.docket.local.yml`, yaml (l.141) | `agent_harnesses: [claude] # can opt a tracking-only repo into per-repo agent generation on its own` | correct | `sync-agents.sh:206-214` (`per_repo_opted_in` checks `LOCAL_CFG` before `DOCKET_YML`) |
| 53 | `.docket.local.yml` (l.149) | "Its own path (and every file `sync-agents.sh` generates) is kept out of git by a managed `# docket:generated` block" | correct | `sync-agents.sh:376-378` (`emit_gitignore_block` prints `.docket.local.yml` first) |
| 54 | Reconcile (l.169) | reconcile "runs at the *last responsible moment*... after the change is claimed... but before the worktree and plan are created" | correct | `skills/docket-implement-next/SKILL.md:104` ("last responsible moment: right before the worktree is created, after claim") |
| 55 | Reconcile (l.171-177) | reconcile re-reads against `related`/recently-archived changes, cited/recent ADRs, current code; rewrites body+spec; dated `## Reconcile log` entry; `reconciled: true` audit record | correct | `skills/docket-implement-next/SKILL.md:45` (Step 3 — Reconcile) |
| 56 | Reconcile (l.181) | "entirely obsolete... killed and the implementer loops back to select the next one" | correct | `skills/docket-implement-next/SKILL.md:47-51` (OBSOLETE → kill, archive/publish/prune sequence, "Loop back to Step 1") |
| 57 | Reconcile (l.182) | "fundamentally invalidated... the implementer stops and escalates to you" | correct | `skills/docket-implement-next/SKILL.md:57` ("FUNDAMENTALLY invalidated (not just scope-adjustable) → STOP and escalate to the human") |
| 58 | Reconcile (l.188) | "On any resume of an `in-progress` change, if `reconciled` is still `false`... the full reconcile pass runs again" | correct | `skills/docket-implement-next/SKILL.md:114` (resume-safety guard) |
| 59 | docket-mode, two-branch model (l.198-200) | orphan `docket` branch "is created with `git checkout --orphan docket` (the same well-trodden pattern as `gh-pages`)" | **fix** | No code path anywhere runs `git checkout --orphan`. `migrate-to-docket.sh:247` uses `git worktree add --orphan -b docket "$DOCKET_WT"`; a fresh repo's bootstrap creation (`scripts/docket-config.sh:49-57`, `create_orphan()`) uses plumbing only (`git mktree` + `git commit-tree` + push) with **no working-tree checkout at all**. The `gh-pages` analogy (orphan, no shared history) is directionally sound but the literal command cited is wrong for both real code paths. |
| 60 | docket-mode (l.198) | docket branch "is **always pushed**" | correct | `migrate-to-docket.sh:273-281` (push guard); `scripts/docket-config.sh:54` (`create_orphan` pushes immediately); `skills/docket-convention/SKILL.md:378-380` ("always pushed to its remote immediately") |
| 61 | docket-mode (l.199) | integration branch "stays code-only, except for **published terminal records**... a copy of the archived change + spec + accepted ADRs" | correct | `scripts/terminal-publish.sh:85-113` (copy-set assembly: change file, `spec:`, Accepted-gated ADRs) |
| 62 | docket-mode (l.203) | "A change's `feat/<slug>` branch is always cut from `origin/<integration_branch>`, carries only plan + results + code, and never modifies docket metadata" | correct | `skills/docket-implement-next/SKILL.md:61-68` (cut from `origin/<integration_branch>`), `:126` ("never modifies docket metadata") |
| 63 | Artifact table (l.209) | Change file lives on `docket`; reaches integration via terminal-publish copy (`done`/`killed`); yes (archived) | correct | `scripts/terminal-publish.sh:85-113` (`--id` mode) |
| 64 | Artifact table (l.210) | Spec lives on `docket`; terminal-publish copy; yes | correct | `scripts/terminal-publish.sh:95,98` (`spec_path`, included iff non-empty) |
| 65 | Artifact table (l.211) | ADR lives on `docket`; terminal-publish copy, gated on `Accepted`; yes (the Accepted ADRs) | correct | `scripts/terminal-publish.sh:100-113` (the Accepted gate) |
| 66 | Artifact table (l.212) | `BOARD.md` lives on `docket`; "Never" published; view on `docket` | correct | `scripts/terminal-publish.sh` never references `BOARD.md` in any copy-set; `scripts/terminal-publish.md:15-16` states it explicitly |
| 67 | Artifact table (l.213-215) | Plan/Results/Code live on the feature branch; reach integration via PR merge; yes (`done` only) | correct | `skills/docket-implement-next/SKILL.md:68` (plan written to `docs/superpowers/plans/` on the feature branch); manifest comment `plan:`/`results:` field semantics, `skills/docket-convention/SKILL.md:258-259` |
| 68 | Artifact table (l.216) | `.docket.yml` lives on the default branch; only on integration if default IS integration (trunk); under GitFlow stays on `main`, not `develop` | correct | `scripts/docket-config.sh:93-95` (`DEFAULT_BRANCH`, from `origin/HEAD`) computed independently of `:155-158` (`INTEGRATION_BRANCH`, from `.docket.yml`) — the two can differ, matching the GitFlow claim; `skills/docket-convention/SKILL.md:43` |
| 69 | docket-mode (l.220-227) | `integration_branch: auto` follows `origin/HEAD`, "falling back to `main` if it can't be detected"; the GitFlow caveat that `auto` follows the *default* branch | **fix** (same drift as #31) | `scripts/docket-config.sh:91-95` fails closed rather than falling back to `main`; the GitFlow-caveat framing itself is accurate (confirmed by #68's evidence) — only the "falling back to main" clause is wrong for the runtime resolver |
| 70 | docket-mode, `.docket/` worktree (l.229-231) | "`.docket/` worktree... synced to `origin/docket` before any read. Your main working tree never switches branches" | correct | `skills/docket-implement-next/SKILL.md:29` (`.docket/` sync via `fetch`+`pull --rebase`) |
| 71 | docket-mode (l.233-237) | `.docket/` gitignored, kept separate from `.worktrees/` (slug-collision / lifecycle / cleanup-blast-radius reasons) | correct | `migrate-to-docket.sh:331` (`.docket/` + `.worktrees/` added to `.gitignore`); `scripts/cleanup-feature-branch.sh:2-4,33-42` (provenance guard scoped to `.worktrees/<slug>` only, never `.docket/`) |
| 72 | docket-mode, Finalize→selective publish (l.241) | "terminal-publish... a selective **file copy**, never a branch merge... the live board stays on `docket` and is never published" | correct | `scripts/terminal-publish.sh:144` (`git checkout ... -- <copyset>`, never `git merge`); no `BOARD.md` reference anywhere in the script |
| 73 | docket-mode, Migrating (l.245-250) | `migrate-to-docket.sh` "operates on the git repo containing your **current directory**"; usage `cd <target-repo> && bash /path/to/docket/migrate-to-docket.sh` | correct | `migrate-to-docket.sh:7,44-62` (`REPO_ROOT` from `git rev-parse --show-toplevel` on `$PWD`) |
| 74 | docket-mode, Migrating (l.252) | prompts for confirmation, `--yes`/`-y` skips it; creates orphan `docket` seeded from planning dirs; prunes `active/`, changes README, `BOARD.md`; keeps terminal records + build artifacts; adds `.docket/` + `.worktrees/` to `.gitignore` | correct | `migrate-to-docket.sh:52-58` (`--yes`/`-y`), `:126-133` (prompt), `:226-282` (seed set = `SEED_CANDIDATES`), `:291-322` (prune set = `PRUNE_PATHS`), `:325-347` (`.gitignore`) |
| 75 | docket-mode, Migrating (l.254-258) | migration grants a local, per-repo Claude Code permission (allow-rule for terminal-publish's push, written to `.claude/settings.local.json`, gitignored); a standalone re-grant via `scripts/ensure-claude-settings.sh` | correct | `migrate-to-docket.sh:369-379`; `scripts/ensure-claude-settings.sh:37,42-47` (`RULE="Bash(git -C * push origin HEAD:$INTEGRATION_BRANCH)"`) |
| 76 | docket-mode, Migrating (l.260) | "bootstrap guard STOPs and points you at `migrate-to-docket.sh`... detects a half-finished migration and points back" | correct | `scripts/docket-config.sh:224-246` (`BOOTSTRAP=STOP_MIGRATE` for both `¬DOCKET∧LIVE` and `DOCKET∧LIVE`) |
| 77 | docket-mode, `main`-mode (l.266-271) | pinning `metadata_branch: main` + `integration_branch: main` reproduces single-branch behavior exactly: no `docket` branch, no `.docket/` worktree, no terminal-publish copy | correct | `scripts/docket-config.sh:148-153` (`METADATA_WORKTREE=.` for `main`); `scripts/terminal-publish.sh:58-61` (mode-guard no-op when branches are equal) |
| 78 | Tuning (l.277) | "Each **autonomous** docket skill runs as a model/effort-pinned subagent (`docket-implement-next`, `docket-auto-groom`, `docket-finalize-change`, `docket-status`, `docket-adr`; the two **interactive** skills... stay inline)" | correct | `agents/` contains exactly these 5 skill-wrapping files with `model:`/`effort:` frontmatter, plus 3 non-skill-wrapping helper agents; no `agents/docket-new-change.md` or `agents/docket-groom-next.md` exists (`ls agents/`) |
| 79 | Tuning (l.279-283) | 3 config layers (Global `config.yml`, Repo-committed `.docket.yml`, Repo-local `.docket.local.yml`) override the built-in default, precedence repo-local > repo-committed > global > built-in | correct | `skills/docket-convention/SKILL.md:70-77` (Layered config table) |
| 80 | Tuning (l.285) | "The config **shape**... is documented once in docket-convention's **'Agent layer'**" | correct | `skills/docket-convention/SKILL.md:64` (heading "Agent layer") |
| 81 | Tuning (l.287) | `effort: auto` "drops the effort line entirely so the agent inherits the model default. Omitting the `effort:` key instead *keeps* the built-in effort" | correct | `sync-agents.sh:331` (`if (effort!="auto") print "effort: " effort; next` — `auto` drops the line; empty `effort` param, i.e. key omitted at every layer, leaves the built-in `effort:` line untouched) |
| 82 | Tuning (l.291-293) | `bash sync-agents.sh # or re-run install.sh, which calls it for you` | correct | `install.sh:24-25` |
| 83 | Tuning (l.295) | "A **global** edit rewrites user-level wrappers into every **present** harness root" | correct | `sync-agents.sh:480-500` (`user_level_pass`), `:185-199` (`compute_user_targets`, presence-on-disk by default) |
| 84 | Tuning (l.296) | "A repo-committed or repo-local edit rewrites that repo's per-repo wrappers for each harness in its (local-then-committed) `agent_harnesses:` list (default `[claude]`)" | correct | `sync-agents.sh:124-152` (`resolve_agent_harnesses`, default `"claude"`), `:502-541` (`project_level_pass`) |
| 85 | Tuning (l.298) | "`sync-agents.sh` always writes **both** passes in one run... project wins over global at generation time" | correct | `sync-agents.sh:699,702` (`user_level_pass` and `project_level_pass` both called unconditionally per non-`--check` run); `:528` (`resolve_agent_layers` called with `LOCAL_CFG DOCKET_YML GLOBAL_CFG` — local/committed checked before global) |
| 86 | Tuning (l.300) | "Generated per-repo agent files are machine-local — gitignored, never committed"; `sync-agents.sh` owns the `# docket:generated` block; "prints a loud one-time notice to **commit it once**" | correct | `sync-agents.sh:404-423` (`ensure_gitignore_block`, logs "COMMIT THIS so machine-local generated files stay untracked") |
| 87 | Tuning (l.302) | "Migrating a pre-0051 repo... deletes the stale tracked copies from the working tree, writes the `.gitignore` block, regenerates the local set fresh, and prints the single remedy commit" | correct | `sync-agents.sh:439-456` (`migrate_tracked_wrappers`) |
| 88 | Tuning (l.304-307) | `sync-agents.sh --check` three-part gate: `.gitignore` block + no tracked file (both CI-meaningful), content drift (advisory only, `rc` unaffected) | correct | `sync-agents.sh:543-602` (`check_project_level`: legs a/b set `rc=1`; leg c only logs `advisory:`) |
| 89 | Tuning (l.309) | "A repo listing `cursor` also gets a generated `.cursor/rules/docket-dispatch.mdc`... `sync-agents.sh --check` covers both the generated agents and the dispatch rule" | correct | `sync-agents.sh:339-363` (`assemble_dispatch_rule`/`write_dispatch_rule`), `:586-598` (dispatch-rule drift check) |
| 90 | Tuning (l.311) | "The clone-identical guarantee is retired... Generation is now all-local" | correct | `docs/adrs/0020-generated-agent-artifacts-machine-local.md` (Accepted, supersedes ADR-0017); `skills/docket-convention/SKILL.md:128-132` |
| 91 | The eight skills, table (l.319-326) | each skill's one-line role description matches its `SKILL.md` frontmatter `description:` | correct | `skills/docket-new-change/SKILL.md:3`, `skills/docket-groom-next/SKILL.md:3`, `skills/docket-auto-groom/SKILL.md:3`, `skills/docket-implement-next/SKILL.md:3`, `skills/docket-finalize-change/SKILL.md:3`, `skills/docket-status/SKILL.md:3`, `skills/docket-adr/SKILL.md:3`, `skills/docket-convention/SKILL.md:3` |
| 92 | The eight skills (l.315) | section title/count "eight" matches the actual skill count | correct | same as #1 — no drift on the count guard |
| 93 | Status (l.332) | "docket-mode is the supported default... the bootstrap guard refuses to run against an un-migrated repo rather than touching your data" | correct | `scripts/docket-config.sh:148` (`metadata_branch` default `docket`), `:224-246` (`STOP_MIGRATE` verdict, never auto-mutates) |
| 94 | Status (l.334) | "`main`-mode remains as a simple, fully-supported opt-out: pin `metadata_branch: main` (and `integration_branch: main`)" | correct | `scripts/docket-config.sh:148-153` |
| 95 | Status (l.336) | "Markhaus is the first planned dogfood project; a migration plan exists to move its existing changes and ADRs into the docket format" | **cut** | Unverifiable from this repo — no code or shipped-doc reference to a live "Markhaus" migration outcome; the only artifact is a **plan** file (`docs/superpowers/plans/2026-05-30-markhaus-docket-migration.md`) describing a *planned*, not completed, migration of an external repo (`/Users/homer/dev/macmd`) this repo cannot verify ran or is still current. Matches spec outline item 11 ("cut if stale") and the plan's own pre-recorded decision (`docs/superpowers/plans/2026-07-10-readme-doc-critic-refresh.md:108,140`). |

**Row count: 95.** Verdict counts: 91 `correct`, 3 `fix` (#31, #59, #69), 1 `cut` (#95). Coverage check: every `## ` (h2) section has ≥1 row (What docket is #3-6; Producer/implementer loop #7-11; Workflow engine #12-19; Install #20-40; Global config #41-49; `.docket.local.yml` #50-53; Reconcile #54-58; docket-mode #59-77; Tuning #78-90; The eight skills #91-92; Status #93-95). All 4 non-`correct` verdicts (#31, #59, #69, #95) carry file:line or explicit-absence evidence.

## Spec/reality discrepancies

- **`integration_branch: auto`'s "fallback main" is not implemented in the runtime resolver.** Both README (l.78, l.224) and `skills/docket-convention/SKILL.md` (l.21, l.43) describe `auto` as falling back to `main` when `origin/HEAD` can't be detected. The actual per-skill runtime resolver, `scripts/docket-config.sh`, fails closed (`die`) in that case by explicit design (its own header: "Fail-closed: non-zero + stderr diagnostic on a hard error... unresolvable origin/HEAD"). Only `migrate-to-docket.sh`'s one-time bootstrap helper (`default_integration_branch()`) has a real `${head:-main}` fallback, and it runs once, before a repo is even migrated. This is a pre-existing drift in the shared convention doc, not introduced by this change — per LEARNINGS #21/global constraints, it is recorded here rather than fixed in `docket-convention` (out of this change's scope; `README.md` itself is fixed in Task 2 by softening the claim to describe the actual runtime behavior).
- **`finalize.require_pr_approval` is real but absent from the single config resolver's exported set.** `scripts/docket-config.sh --export` is documented as "the single implementation the skills run instead of re-deriving this each session" (`skills/docket-convention/SKILL.md:47`), and its emit list (`scripts/docket-config.sh:249-267`) covers `FINALIZE_GATE` and `FINALIZE_TEST_COMMAND` but has no `REQUIRE_PR_APPROVAL`/equivalent entry. `require_pr_approval` is nonetheless real and implemented directly inside `skills/docket-finalize-change/SKILL.md:33-123` (change 0021 / ADR-0011, per global constraints). The README's line about it is being **kept** (per the global-constraints instruction: it is real, keep it) — this entry records the gap between the convention's documented single-source resolver and this key's actual (skill-internal, not `docket-config.sh`-exported) implementation as a follow-up, not a README defect.

## Deviations from plan

- The task brief's file list scoped read-only inputs to `README.md`, `scripts/*.sh`+`scripts/*.md`, `skills/docket-convention/SKILL.md`, `skills/docket-finalize-change/SKILL.md`, `install.sh`, `migrate-to-docket.sh`, `agents/docket-*.md`, `tests/*.sh`. Verifying the reconcile-superpower section (README l.153-190) and the producer/implementer claims (l.19-33) required reading the *implementing* skills not in that list — `skills/docket-implement-next/SKILL.md` and `skills/docket-new-change/SKILL.md` — because those behaviors are specific to those skills and are not restated in `docket-convention`. This is a read-only deviation (no file outside the allowed-to-modify set was written), directly in service of LEARNINGS #47 ("verify against code, not sibling prose"); flagging it here for transparency rather than silently expanding scope.
- Also read (research only, not modified): `skills/docket-status/SKILL.md` (frontmatter only, for the eight-skills table check), `docs/adrs/0015-0020` (for the ADR-alignment instruction in global constraints), and `scripts/lib/docket-frontmatter.sh`, `scripts/board-checks.sh`, `scripts/render-board.sh`, `scripts/archive-change.sh`, `scripts/cleanup-feature-branch.sh`, `scripts/sync-integration-branch.sh`, `scripts/github-mirror.sh` — none in the brief's list, all needed to find file:line evidence for claims those scripts (not the ones in the read-only list) actually implement.

## Follow-up candidates

- Fix the "auto → origin/HEAD, fallback main" drift in `skills/docket-convention/SKILL.md` (l.21, l.43) to match `docket-config.sh`'s actual fail-closed behavior — out of scope for this docs-only README change (global constraints forbid touching `skills/`).
- Wire `require_pr_approval` into `scripts/docket-config.sh --export`'s emitted set (or explicitly document in `docket-convention` that it is read directly by `docket-finalize-change` rather than through the shared resolver) so the "single implementation" claim in `docket-convention` stays accurate as more finalize-only keys are added.
- Consider whether the docket-mode "orphan `docket` branch" description should name both real creation paths (`migrate-to-docket.sh`'s `git worktree add --orphan`, and `docket-config.sh --bootstrap`'s plumbing-only `mktree`/`commit-tree`) rather than a single illustrative command — Task 2's rewrite should decide the right level of detail for a newcomer-facing README (this results doc records only that the *current* single command is wrong, not the replacement prose).
