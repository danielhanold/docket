# Complete native Codex runner — bootstrap results

## Disposition

Human-directed plain Git repair for change 432, preserving the pre-created branch and
its starting commit `ed80a72a33ce535ce8c3ab639b6090cb3ba9abc8`. The user authorized this
bootstrap after managed workspace preparation refused the missing ownership manifest.
No ownership manifest was fabricated. Managed artifact attachment and publication
remain pending supported adoption. This is not native acceptance or an implemented verdict.

## Repair

The receipt checker now understands the public `run.gate-claim` envelope. Successful
redemption supplies top-level fresh owner authority and selects `gate.drive.advance`.
An exit-zero facade refusal grants no authority. The checker rejects malformed,
inconsistent and wrong-identity receipts, preserves original captures, and supports
an explicit coordinator run root when no worker assignment exists. Backend ownership,
redemption, deadlines, counters and scope rules are unchanged.

Caller instructions specify operation-specific credential paths, full transport
collection, recovered closed-scope handling, and current-HEAD certification after
results commits. Generated embedded assets follow the source. The pre-existing
formatting defect in `internal/githubcli/comment_integration_test.go` is gofmt-only.

## Observed verification

- Baseline full source suite: 54 of 55 files passed. The sole failure was the pre-existing
  gofmt defect; no budget findings were reported.
- RED: a real successful public facade claim was rejected by the old checker with
  `unknown field "key"`. Coordinator context was initially rejected as an unknown flag.
- GREEN: focused receipt, CLI, repository guard and all four harness packages passed.
- Mutation checks: disabling facade-envelope dispatch, removing expected-drive checking,
  and replaying the literal captured shell command each made the relevant test fail.
  All mutations were restored. Bash and zsh capture tests cover split streams,
  malformed/empty terminal output, original exit status and exactly one invocation.
- Public CLI rehearsal: retained plan and worker commits, worker active validation before
  acknowledgement, real keyed verdict and facade redemption, recovered terminal evidence,
  rejected old-scope acknowledgement, fresh certification scopes, results-driven evidence
  invalidation, valid review entry, exact final evidence, correctly based fixture PR,
  implemented transition and `run-complete`. The outer retry remains unused.
- Real WAITING drives recover through PASSED, FAILED and fingerprint-drift HALTED paths;
  fresh owner rotation, stale-owner/consumed-token refusals and one original execution
  are asserted. The process is released while ownership is handed off.

The deterministic fixture simulates native host/model review and external GitHub effects.
It uses actual CLI producers, the taught receipt checker, Git, scope validation and
canonical evidence operations. Model-level tool cell/session loss and fresh-session role
loading still require native observation; shell chunk tests do not certify those layers.

## Final source gate and candidate

The configured command is `go run ./cmd/docket development test` for both build and
finalize. The complete post-repair suite and clean candidate pin are the next verification
steps; their immutable output belongs in the external candidate evidence directory.
This checkpoint intentionally precedes that final gate. Do not move HEAD just to paste
its timestamp or final evidence hash here.

## Native acceptance boundary

431 and its preserved original checkout have not been modified, resumed, cancelled,
reset or certified by this bootstrap. No global runtime was replaced and no PR merged.
Real completion still requires separately authorized supported old-run accounting and
new admission, actual native review, configured gates, final exact-HEAD evidence,
attached verified results, a PR based on 425, and `run-complete` in a fresh candidate-loaded
session. Candidate source integration and runtime delivery must be explicit before
moving either acceptance branch. A green fixture or source suite does not meet that goal.
