<!-- docket:backlink:start (generated — do not hand-edit) -->
> ↩ **[Change 0306 — Loss-preserving document layer](https://github.com/danielhanold/docket/blob/docket/docs/changes/active/0306-loss-preserving-document-layer.md)**
<!-- docket:backlink:end -->

# Loss-preserving document layer — results
Change: #0306 · Branch: feat/loss-preserving-document-layer · PR: (opened at step 7) · Plan: docs/superpowers/plans/2026-08-13-loss-preserving-document-layer.md · ADRs: none produced (spec-driven decisions recorded in the spec and plan)

## Verify (human)

- [ ] **The frozen corpus's snapshot contract is deliberate.** `testdata/repositories/v0.9.2/documents/` pins five real records by tag (`v0.9.2`) and docket-branch commit (`6d34592c`) and deliberately does NOT track the live originals — active change 0158 and the learning file legitimately keep moving on the `docket` branch. PROVENANCE.md records the pins; `testdata/README.md` carries the immutability rule. Confirm you accept "pinned historical snapshot" (per learning `frozen-copy-needs-a-drift-assert`'s second arm) as the intended contract, as opposed to 0305's byte-tracking fixtures.
- [ ] **Follow-up stub 321 needs a groom.** `render-artifact-backlink.sh` truncated this change's committed plan mid-build (details below); the bash-side fix is stub 0321, needs-brainstorm.

## Findings

- **The backlink renderer ate the committed plan — live data loss, mid-build.** `render-artifact-backlink.sh` matches its start marker with `grep -qF` + awk `index()` (substring, any line), so a marker-shaped example literal inside the authored plan text was taken for the real backlink block and everything from plan Task 8 to EOF was consumed at stamp time — the exact "unbounded range consumes to EOF" failure the always-in-context managed-block rule names, in shipped tooling, against the change whose whole purpose is preventing it. Restored from the authoring context in `d26be712` with the one triggering literal defanged (Go string concatenation); the renderer re-run against the restored file is byte-stable. Bash-side fix minted as **stub 0321** (`discovered_from: 306`); the Go layer built here refuses this by construction (whole-line column-zero grammar + population validation + fence awareness, each mutation-tested).
- **Fuzz campaign found a real serializer defect (fixed in-branch, seed committed).** `FuzzValueRoundTrip` crashed on `String("")`: validation rejected only C0 controls + DEL, but yaml v3.0.4's reader refuses C1 controls, so the builder could render a document its own reparse refused. Fixed by routing `Value.validate` and `validBlockContent` through one `illegalTextRune` predicate (all Unicode Cc plus U+FFFE/U+FFFF); review then extended it to U+2028/U+2029 (YAML line breaks in the scanner, silent value corruption otherwise). Minimized crasher committed at `internal/document/testdata/fuzz/FuzzValueRoundTrip/`.
- **Review (docket-review-deep — rung bumped from standard by the >1500-line diff; 9 findings: 0 blocker / 5 important / 4 minor).** Eight fixed in-branch, one minted (stub 0321); dispositions with SHAs are in the PR body's table. The two behavioral repairs worth naming: the comment-boundary scanner missed `{`/`:` as quote-reopening flow indicators, so a quoted key inside a nested flow mapping could get a truncated value span and a patch could silently comment out data (`786781b4` — now also backstopped by a fail-closed span↔semantics cross-check); anchored values (`key: &anc v`) classified patchable and a patch would silently delete the anchor (`8e4a9767` — now `ShapeUnsupported`).
- **Executed plan deviations (all recorded in the plan's restored copy and commit bodies):** yaml v3 does not error on duplicate mapping keys when decoding into a `yaml.Node` — duplicates are caught by an explicit node walk; field-entry continuation is keyed on indentation, not "next candidate key line" (the draft rule swallowed non-Docket-grammar keys — caught by a 891-record corpus smoke probe); a null sequence element renders as explicit `null` (yaml v3 drops a trailing empty token), while top-level null stays the bare `key:` form.
- **Suite budget advisory (pre-existing, untouched by this branch):** both full-suite gate runs printed `OVER BUDGET: test_sync_agents_runners` (194s vs 60s ceiling) under parallel load — same advisory 0305 recorded; belongs to changes 0251/0273. `tests/test_go_toolchain.sh` ran at 5s of its 20s row with this package's tests and fuzz seeds included.
- **First `.gitattributes` in the repo** (`530aa0e1`): pins `internal/document/testdata/**` and `testdata/repositories/**` as `-text`, since the branch adds the repo's first line-ending-sensitive byte-exact fixtures (CRLF fixtures + the frozen corpus).

## Follow-ups

- **Minted: 0321** — `render-artifact-backlink.sh eats artifact tails on marker-shaped literals — anchor whole-line, validate balance, refuse` (type fix, discovered at review; the reconcile pass surfaced nothing). One mint, no dedup skips, no cap overflow.
