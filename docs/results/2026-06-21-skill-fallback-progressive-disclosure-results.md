# Per-script co-located contracts — results

Change: #37 · Branch: feat/skill-fallback-progressive-disclosure · PR: <set at PR open> · Plan: docs/superpowers/plans/2026-06-21-skill-fallback-progressive-disclosure.md · ADRs: none

## Verify (human)

Automated tests fully cover the change (suite green in both an ambient docket shell and `env -u DOCKET_SCRIPTS_DIR`). The items below are judgment checks for the merge gate, not reproductions of the test suite.

- [ ] **docket-status conflict resolution is what you want.** This branch was rebased onto `origin/main` after PR #47 (change 0036) merged mid-build; the docket-status sweep strip from this change was **dropped as superseded** (0036 already delegated the sweep to `archive-change.sh` and added the #0035 ordering + log-and-continue posture). `skills/docket-status/SKILL.md` is therefore byte-identical to 0036's version — confirm you're happy keeping 0036's wording rather than re-stripping its residual archive-internals prose (see Follow-ups).
- [ ] **Spot-read one heavy contract** (`scripts/terminal-publish.md` or `scripts/docket-config.md`) against its script to sanity-check fidelity — content fidelity rests on co-location + review, not a mechanical test (by design; the audit is existence-only).
- [ ] **Finalize gate** (`finalize.gate: local`) will rebase-onto-base and re-run the suite before merge — no manual test run needed here.

## Findings

- **Mid-build integration drift (notable plan deviation).** PR #47 / change 0036 ("status sweep — delegate archiving to `archive-change.sh`; remove double-archive") merged into `origin/main` *during* this build, advancing the base from `0e68c54` → `d9243e9` and editing `skills/docket-status/SKILL.md` — the same file this change's Task 10 stripped. The pre-0036 strip would have **reverted** two 0036 fixes (the #0035 re-render-before-publish ordering and the per-change log-and-continue failure posture, which my strip had rewritten to finalize's "abort-and-report"). Resolution: rebased onto `origin/main` and resolved docket-status to **0036's version** (my strip there is superseded — 0036 already achieved that slimming by delegating to the script). All other work (13 contracts, convention §4/§2, the finalize/adr/new-change/implement-next strips, the audit, the folded fix) is independent of 0036 and rode the rebase cleanly. Full suite re-validated green post-rebase. *Lesson: when the integration branch advances mid-build into a file you're editing, re-resolve by intent against the landed version — a naive merge can silently revert the newer fix.*
- **Fragile cross-parenthetical sentinel (Task 10, resolved in build).** A test (`test_docket_metadata_branch.sh` K3/K4) pins the literal `the integration branch), performing the archive move` as the **main-mode** archive-degradation clause in both docket-new-change and docket-implement-next. A first strip pass relocated it into a docket-mode bullet to satisfy the grep, producing a garbled, factually-wrong sentence (docket-mode archives on `origin/docket`, not the integration branch). Caught in task review and fixed by restoring it to its meaningful main-mode home. *Lesson: a must-preserve sentinel must stay in its meaningful location as grammatical prose — never relocated/jammed to pass a grep.*
- **Per-task Minor findings (all triaged in the final whole-branch review; two fixed, rest accepted).** Fixed: `board-checks.md` Purpose said "five" checks but enumerated six (`malformed-id` is a guard, not a named check) → made internally consistent, mirroring `adr-checks.md`; `render-board.md` now documents the `# Backlog` H1 it emits. Accepted as-is (documentation-only, faithful to the scripts): minor wording/terseness nits in `terminal-publish.md`, `docket-config.md`, `archive-change.md`, `cleanup-feature-branch.md`, `sync-integration-branch.md`, `render-adr-index.md`, `github-mirror.md`.
- **No ADR minted.** The architectural decision (co-located per-script contracts vs the rejected alternatives — per-skill `operations.md`, per-script refs in the convention dir, keep-inline) was made at brainstorm/spec time and is durably captured in the published spec + the new convention §2 naming rule; it extends ADR-0012's script↔model boundary (cf. ADR-0013). No *non-obvious decision emerged during implementation*, so `adrs:` stays empty.

## Follow-ups

- **(Optional) Re-strip docket-status's residual archive-internals prose** onto `scripts/archive-change.md`. 0036's docket-status step c still narrates what `archive-change.sh` owns (the dated move, status writes, change-file-only commit, push-retry, self-verify); that is now duplicated by `scripts/archive-change.md`. Deferred deliberately — 0036's wording is correct and recently-reviewed, and re-stripping risks disturbing its subtle #0035-ordering / failure-posture semantics for a modest context win. A future small change can trim it against the contract.
- **(Out of scope, noted) Repo-root scripts** (`install.sh`, `link-skills.sh`, `sync-agents.sh`, `migrate-to-docket.sh`) got no contracts — they live at the repo root, not `scripts/`, and are unreachable via `$DOCKET_SCRIPTS_DIR/<name>.md`. Their convention prose is conceptual/operational and stays. Relocating any heavy root-script internals would be a separate change with its own reachability mechanism.
