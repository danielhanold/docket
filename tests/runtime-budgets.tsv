# tests/runtime-budgets.tsv — per-file wall-clock ceilings (change 0227).
# Format: <repo-relative test path><TAB><max seconds><TAB><parallel|serial>
#
# WHY: the suite was 629s because four files grew to 66% of it. A one-time split decays;
# this table is the discipline made durable. Seeded from the measured post-shard run,
# rounded up to the next multiple of 5 plus a 5s margin (min 10s).
#
# A new tests/test_*.sh with NO row here fails tests/test_runtime_budgets.sh. That is
# deliberate: adding a test file is a conscious placement decision. Read tests/README.md
# before adding one — the usual right answer is to EXTEND an existing shard.
#
# NO row may exceed 60 seconds. If a file outgrows its ceiling, shard it or move the new
# assertions into a shard with room. Raising a number here is not the remedy, and it is not
# a quiet edit either: tests/test_runtime_budgets.sh pins the SUM of every ceiling in this
# table (EXPECTED_TOTAL), so a raise of any size reddens whether or not it breaks the 60s
# ceiling. The total does legitimately move when a new test file brings its own row, or when
# a sharded file's rows are re-cut — re-seed it from a measured serial run and say which.
#
# tests/test_docket_status.sh was raised 45 -> 50 and then 50 -> 60 by change 0118, which adds two
# sweep runs, three fault-injection runs, and — from review finding 2 — one wedged-shared-worktree
# sweep run to the one file that owns sweep_execute coverage. The first raise's note argued from a
# pre-change reading taken in another session against a post-change reading that came out FASTER,
# which is not an argument; both were re-taken for review finding 6, PAIRED AND INTERLEAVED in one
# session on one machine, each run standalone and serial as
# `/usr/bin/time -p bash tests/test_docket_status.sh` (the base leg is origin/main's copy of the
# file, run from tests/ under a temporary name so it resolves the same REPO root):
#   base (origin/main):  41.59  41.50  41.75  -> worst 41.75
#   this branch:         44.22  44.92  44.85  -> worst 44.92
# So the six new fixtures cost about 3.2s of real runtime. That is the honest basis: the file was
# NOT already at parity with its old 45 row, and the raise is driven by added runtime rather than by
# noise. Sizing per this header's rule takes the WORST standalone serial reading recorded for the
# post-change file on this machine, which is 51.36 (the finding-2 readings were 45.17, 46.79, 51.36,
# 50.52, 46.12, 45.14; that ~6s spread is machine load, as the tight interleaved pairs above show):
# next multiple of 5 is 55, plus the 5s margin -> 60. The quiet readings alone would have yielded 50
# (44.92 -> 45, plus 5), so 60 is the conservative pick between the two — a ceiling a loaded machine
# still meets. Sharding was considered and REJECTED HERE: changes 0268 and 0154 are both queued
# against this same file, and re-cutting shards is precisely the edit that cannot rebase cleanly
# past them — the shard belongs to whichever change can take the whole file, not to one adding six
# fixtures to it.
#
# THIS FILE IS NOW AT THE 60s CEILING. There is no next raise. The next change that adds runtime
# here — 0268 and 0154 are both queued against it — must SHARD the file first; that is now the only
# available move, and it was already the header's stated remedy.
#
# tests/test_run_tests.sh was raised 20 -> 30 by change 0221, which gates scripts/run-tests.sh on
# the scripts/check-test-source-hygiene.sh preflight. This is the case the header calls a file that
# GOT SLOWER, and the raise is defended by measurement rather than by the feature being new. The
# preflight is per-runner-invocation and this file invokes the runner ~35 times, so it pays the
# preflight ~35 times over. Three standalone serial readings each, one session, one machine, as
# `/usr/bin/time -p bash tests/test_run_tests.sh >/dev/null` (base = origin/main extracted with
# `git archive` into a scratch tree, so it runs its own runner, not this branch's):
#   base (origin/main):        16.16  16.09  16.16  -> worst 16.16
#   branch, sweep unbatched:   27.21  27.24         -> worst 27.24
#   branch, sweep batched:     20.87  20.95  21.03  -> worst 21.03
# The middle row is why this is not simply a raise: rule (a)'s tests-tree sweep forked one awk per
# tests-tree file on EVERY invocation, so the file paid O(#test files x #invocations) and the term
# grew with the suite. Batching the sweep into ONE awk process (verdict-preserving: the checker's
# full output over the live tree and every fixture is byte-identical before and after) gives back
# 6.2s of the 11.1s and, more to the point, makes the remaining cost O(1) awk processes per
# invocation instead of O(#test files). The residual ~4.9s over base is the preflight actually
# running, which is the feature. Sizing per this header's rule from the worst post-change reading,
# 21.03: next multiple of 5 is 25, plus the 5s margin -> 30. Sharding was considered and rejected:
# 21s is nowhere near the 60s ceiling, and the file is one coherent runner-contract suite.
#
# tests/test_docket_stack.sh is a NEW FILE brought by change 0298 — the header's first legitimate
# case for the total moving. It covers a new subsystem: the stacked-changes library
# (scripts/lib/docket-stack.sh) and, once change 0298's later tasks land, the scripts/stack-base.sh
# CLI over it. It is not an extension of an existing shard because no shard owns stack resolution,
# and the two files it would otherwise have joined are both spent — tests/test_docket_status.sh sits
# at the 60s ceiling and tests/test_board_checks.sh at 55s. Sized per this header's rule from three
# standalone serial readings, one session, one machine, as
# `/usr/bin/time -p bash tests/test_docket_stack.sh >/dev/null`:
#   0.23  0.22  0.22  -> worst 0.23
# The file drives sourced shell functions over a fixture tree in $TMPDIR with no git and no network,
# so it costs essentially nothing: next multiple of 5 is 5, plus the 5s margin -> 10, which is also
# this table's floor. EXPECTED_TOTAL moves 1750 -> 1760 for that one row.
#
# tests/test_docket_status_stack.sh is a NEW FILE brought by change 0298 — the second legitimate
# mover in this change. It is a SIBLING SHARD of tests/test_docket_status.sh, not an extension of
# it, and the reason is written three notes above in this very header: that file sits AT the 60s
# ceiling and the header states there is no next raise for it. Its new legs — the sweep flipping a
# child whose PR merged into its stack parent's branch to `stacked-merged`, and the discriminating
# case of a stacked child merged into the integration branch, which still closes out to `done` —
# therefore land in their own file. Sized per this header's rule from three standalone serial
# readings, one session, one machine, as
# `/usr/bin/time -p bash tests/test_docket_status_stack.sh >/dev/null`:
#   1.19  1.27  1.18  -> worst 1.27
# One fixture repo, one stubbed gh, four mocked close-out scripts and no network: next multiple of 5
# is 5, plus the 5s margin -> 10, this table's floor. EXPECTED_TOTAL moves 1760 -> 1770.
#
# tests/test_stack_closeout.sh is a NEW FILE brought by change 0298 — the third and last legitimate
# mover in this change. It owns scripts/stack-closeout.sh, the pass that promotes a merged root's
# `stacked-merged` descendants to `done`, plus stack_descendants in the stacked-changes library. Its
# own file rather than an extension: it drives five independent fixture families (happy path,
# idempotency against the metadata branch, a resumed partial run, a malformed-marker refusal, and a
# cyclic graph) through a real bare origin, and neither sibling shard has room — the two files it
# would otherwise have joined sit at the 60s and 55s ceilings. Sized per this header's rule from
# three standalone serial readings, one session, one machine, as
# `/usr/bin/time -p bash tests/test_stack_closeout.sh >/dev/null`:
#   5.45  5.43  5.36  -> worst 5.45
# The cost is git: one bare origin, two clones and roughly thirty commits and pushes across the five
# families, with every close-out helper mocked and no network beyond the local origin. Next multiple
# of 5 above 5.45 is 10, plus the 5s margin -> 15. EXPECTED_TOTAL moves 1770 -> 1785.
#
# tests/test_board_checks_stack.sh is a NEW FILE brought by change 0298 — the fourth and last
# legitimate mover in this change. It is a SIBLING SHARD of tests/test_board_checks.sh, which sits
# at 55s: it owns the two stacked health checks (`stack-invalid`, `stack-parent-killed`) and the
# separateness pin that keeps them two ids rather than one. The registration guards stay in
# tests/test_board_checks.sh — they are derived from board-checks.sh itself and belong with the rest
# of the registry — so this file carries only the behavioral legs. Sized per this header's rule from
# three standalone serial readings, one session, one machine, as
# `/usr/bin/time -p bash tests/test_board_checks_stack.sh >/dev/null`:
#   1.93  1.92  1.92  -> worst 1.93
# The cost is git: one bare origin, one clone, three branches and one pushed feature branch, then
# two board-checks.sh runs over ten fixtures. Next multiple of 5 is 5, plus the 5s margin -> 10,
# this table's floor. EXPECTED_TOTAL moves 1785 -> 1795.
#
# tests/test_go_toolchain.sh is a NEW FILE brought by change 0304 — the header's first legitimate
# case again, and for a brand-new subsystem in the sense tests/README.md means it: the Go module at
# the repository root. It is the whole-suite Go gate, running gofmt, `go vet ./...`, `go test ./...`
# and the four-tuple CGO-off cross-build. No existing shard could take it — every other file in this
# table drives Bash scripts, and this one's cost is a Go toolchain compiling the module and the four
# approved target tuples. Sized per this header's rule from standalone serial readings, one session,
# one machine, as `scripts/run-tests.sh -j 1 --timings PATH tests/test_go_toolchain.sh`:
#   13  12  12  -> worst 13
# Note what dominates that number: those readings are COLD-CACHE ones. Next multiple of 5 above 13
# is 15, plus the 5s margin -> 20, leaving 8s of margin over the 12s readings. EXPECTED_TOTAL moves
# 1795 -> 1815. Change 0304's review then pinned GOMODCACHE/GOCACHE to a stable per-checkout
# location (the file's own CACHES header has the rationale), so only the first run after a fresh
# clone is cold: re-measured at 12s cold and 2s warm, same command. The ceiling STAYS AT 20 — this
# header sizes from the worst standalone serial reading, and a cold cache is still reachable on any
# fresh clone or CI image. The extra margin is headroom, not slack to reclaim.
#
# tests/test_go_race.sh is the whole-module data-race gate (change 0308), `go test -race ./...`.
# Its history: sharded four ways by changes 0309/0313/0314 to fit the parallel phase's 60s
# ceiling, COLLAPSED to one run and moved to the SERIAL lane by change 0332 under a temporary 300s
# exemption, and RETURNED to the parallel lane at an ordinary 60s row by change 0333 — this re-cut.
# Change 0332's 300s exemption existed only because internal/app's ~190s real-git integration tail
# ran inside `go test -race ./...` and no `go list` shard could split one Go package. Change 0333
# partitions that tail (and internal/githubcli's and internal/gitcli's) behind the `integration`
# build tag, so the default corpus this gate covers no longer carries it. The gate then measures,
# standalone serial, one machine (darwin/arm64, go1.26.5), as
# `/usr/bin/time -p bash tests/test_go_race.sh >/dev/null`:
#   race   53.17 49.27 49.57 -> worst 53.17 -> next multiple of 5 is 55, plus 5 -> 60
# 60 is AT the ceiling with no headroom, which is exactly where change 0308 first sized this gate
# and the standing remedy applies to the next slowdown: shard the tail further behind the tag,
# never raise the number. It returns to the PARALLEL lane because the oversubscription that forced
# the serial pin (change 0332 / 0329's build-gate halt) was `go test -race`'s GOMAXPROCS-wide race
# workers running over internal/app's heavy tail; with that tail partitioned out, the fast-corpus
# race run no longer starves the other parallel jobs. tests/test_go_toolchain.sh's `go test ./...`
# over the same shrunken default corpus was re-measured (`/usr/bin/time -p bash
# tests/test_go_toolchain.sh`: recompile 47.95, warm 1.80 1.82 -> worst 47.95 -> 50 + 5 -> 55) and
# its 55 row is unchanged. EXPECTED_TOTAL moves 2785 -> 2545: -240 for the race gate's 300 -> 60
# re-cut (the toolchain re-measure held its row, so it contributes 0).
#
# tests/test_go_toolchain.sh 20 -> 45 (change 0309): a file that GOT SLOWER, defended by measurement.
# Its `go test ./...` now compiles and runs change 0309's internal/repository/transaction package,
# whose real-git acceptance tests add real uninstrumented cost to the whole-suite Go gate. No cost was
# avoidable to remove first: the package's tests ARE the change's acceptance proof. Re-measured
# standalone serial, one machine, as `scripts/run-tests.sh -j 1 --timings PATH tests/test_go_toolchain.sh`:
#   39  38  38  -> worst 39
# Next multiple of 5 above 39 is 40, plus the 5s margin -> 45, still well under the 60s ceiling.
#
# tests/test_asset_bundle_drift.sh is a NEW FILE brought by change 0311 — the header's new-test-file
# case again, for a subsystem this table has no shard for: the generated, committed embedded
# asset bundle (internal/assets/embedded) and its comparator, `cmd/genassets -check`. It is NOT an
# extension of tests/test_go_toolchain.sh, the only other Go-touching file here: that file is the
# whole-suite Go gate and its `go test ./...` verdict is exactly the cacheable one that CANNOT see
# this drift, since editing skills/ does not change internal/assets' package inputs. Sized per this
# header's rule from three standalone serial readings, one session, one machine, as
# `/usr/bin/time -p bash tests/test_asset_bundle_drift.sh >/dev/null`:
#   0.50  0.49  0.46  -> worst 0.50
# Those are warm-cache readings, and unlike tests/test_go_toolchain.sh the cold case is cheap here
# and was measured rather than assumed: the file compiles ONE small stdlib-only command
# (cmd/genassets and internal/assets pull no module requirements), so a run with GOMODCACHE and
# GOCACHE both pointed at empty scratch directories costs 2.24s — which also makes the file offline
# after a fresh clone. Its other work is a `cp -R` of four small authored roots (~520K, 64 files)
# plus the generated tree, and three comparator runs over them. Next multiple of 5 above 2.24 is 5,
# plus the 5s margin -> 10, this table's floor. EXPECTED_TOTAL moves 1815 -> 1825.
#
# tests/test_sync_agents_runners.sh was SHARDED by change 0324 — the header's re-cut case, not a
# file that got slower. The runner suite sat AT the hard 60s ceiling with no next raise (its row was
# the ceiling itself), and adding the 17th shipped agent (docket-plan-writer) to the runner matrix
# pushed its measured serial cost to ~96s. The table's own remedy for a file over its ceiling is a
# shard, never a bigger number, so the one file was cut along its `# ---` banner seams into three
# siblings — a MOVE with no assertion changed: 308 runtime asserts split 125/98/85, none lost.
#   tests/test_sync_agents_runners.sh        — runner shims + shim_model/shim_effort config (0079/0269)
#   tests/test_sync_agents_runners_gates.sh  — --worktree slot, required-model, atomic generation
#   tests/test_sync_agents_runners_pins.sh   — resolved model/effort pin injection (0168/0173)
# Sized per this header's rule from three standalone serial readings, one session, one machine, as
# `time bash tests/<shard>.sh`:
#   runners        38.76 38.72 38.78 -> worst 38.78 -> next multiple of 5 is 40, plus 5 -> 45
#   runners_gates  26.78 26.87 26.71 -> worst 26.87 -> next multiple of 5 is 30, plus 5 -> 35
#   runners_pins   29.83 30.13 30.36 -> worst 30.36 -> next multiple of 5 is 35, plus 5 -> 40
# The single 60s row becomes three rows summing to 120, so EXPECTED_TOTAL moves 1845 -> 1905.
#
# tests/test_go_toolchain.sh 45 -> 55 (change 0314): a file that GOT SLOWER, defended by measurement.
# Its uninstrumented `go test ./...` now compiles and runs change 0314's internal/process package,
# whose real-process launch/observe/stop/recover tests fork, exec, and wait on real children — real
# wall clock even uninstrumented, and no cost was avoidable to remove first: those tests ARE the
# change's acceptance proof. Re-measured standalone serial, one machine, as
# `scripts/run-tests.sh -j 1 --timings PATH tests/test_go_toolchain.sh` (a cluster of 45/45/45/46 with
# one loaded-machine reading at 48):
#   48  46  46  45  45  45  -> worst 48
# The file now exceeds its old 45 row, so per this header's rule it is re-sized from the worst
# reading: next multiple of 5 above 48 is 50, plus the 5s margin -> 55, still well under the 60s
# ceiling. EXPECTED_TOTAL moves 2105 -> 2115.
#
# tests/test_go_finalize_e2e.sh is a NEW FILE brought by change 0316 (Task 17) — the header's
# new-test-file case. It runs the hermetic finalize end-to-end matrix
# (internal/app/finalize_e2e_test.go), which builds ./cmd/docket and a fake `gh` and drives full
# finalize lifecycles through CLI argv against real bare-remote repositories — genuine wall clock,
# and none avoidable: those lifecycles ARE the acceptance proof. It is deliberately NOT folded into
# tests/test_go_toolchain.sh's `go test ./...`: the matrix carries the `e2e` build tag, so the
# default-tag Go gate excludes it (it neither runs twice nor inflates that 55s row), and this file
# runs it alone behind `-tags e2e`. The build tag also lets the matrix use t.Parallel(), so the
# eight lifecycle tests overlap. Measured standalone serial, one machine, as
# `scripts/run-tests.sh -j 1 --timings PATH tests/test_go_finalize_e2e.sh`:
#   16  16  16  -> worst 16
# Next multiple of 5 above 16 is 20, plus the 5s margin -> 25, well under the 60s ceiling.
# EXPECTED_TOTAL moves 2115 -> 2140.
#
# tests/test_gate_facade.sh is a NEW FILE brought by change 0334 (Task 5) — the header's
# new-test-file case, for a brand-new surface no existing shard owns: the shell run-gate facade
# delegators (scripts/gate-before.sh / scripts/gate-verdict.sh) over the Go binary. It is the
# CROSS-PROCESS half of the gate-facade acceptance matrix — the in-process half lives in Go
# (internal/app/rungate_*_test.go) and cannot prove that the durable gate record survives across
# separate OS processes or that the `docket.sh gate-before|gate-verdict …` spelling carries the
# report line through. It builds ./cmd/docket ONCE and drives roughly a dozen fresh `docket.sh …`
# processes against hermetic bare-remote main-mode fixtures with a tiny fake `gh`. Not folded into
# tests/test_go_toolchain.sh's `go test ./...` (that gate is in-process Go only, never a shell
# facade), nor into tests/test_docket_facade.sh (that file stubs the SCRIPTS_DIR seam and never
# builds or runs the real binary). Sized per this header's rule from the WORST standalone serial
# reading, one machine, as `/usr/bin/time -p bash tests/test_gate_facade.sh >/dev/null`:
#   cold cache (empty GOCACHE/GOMODCACHE): 6.46   warm (shared repo cache): 3.20  2.98  2.96
# The cold reading dominates (it compiles ./cmd/docket) and is the reachable worst on any fresh
# clone or CI image; per the sizing rule next multiple of 5 above 6.46 is 10, plus the 5s margin
# -> 15. EXPECTED_TOTAL moves 2310 -> 2325.
#
# tests/test_go_integration_gitcli_*.sh are FOUR NEW FILES brought by change 0333 — the
# header's new-test-file case, four times, and the first Go integration SHARD runners. Change
# 0333 partitions internal/gitcli's slow real-git corpus behind the `integration` build tag
# (internal/gitcli/*_integration_test.go; completeness proven by
# tests/test_go_integration_contract.sh), and each runner drives one shard through the shared
# executor tests/lib/go-integration-shard.sh — a package/prefix/mode declaration plus
# `go test -tags integration [-race] -run ^<prefix>`. No existing shard could take them: every
# other Go-touching row here runs the DEFAULT-tag corpus, which by construction cannot see this
# tagged one. The partition also drops the default internal/gitcli cost from ~32s to ~0.3s (the
# 12 fast validation/pure tests that stay untagged). Sized per this header's rule from the WORST
# of three standalone serial readings, one session, one machine (darwin/arm64, go1.26.5), as
# `/usr/bin/time -p bash tests/test_go_integration_gitcli_<feature>.sh >/dev/null`:
#   repo         21.61 21.48 21.29 -> worst 21.61 -> next multiple of 5 is 25, plus 5 -> 30
#   process       8.89  8.93  8.83 -> worst  8.93 -> next multiple of 5 is 10, plus 5 -> 15
#   source        3.35  3.40  3.23 -> worst  3.40 -> next multiple of 5 is  5, plus 5 -> 10
#   concurrency   2.89  2.11  2.07 -> worst  2.89 -> next multiple of 5 is  5, plus 5 -> 10 (race mode, measured WITH -race)
# All four ride the parallel lane (each is a single-package go-test invocation, not a
# whole-module -race run). EXPECTED_TOTAL moves 2355 -> 2420: +30 +15 +10 +10.
#
# tests/test_go_integration_githubcli_*.sh are FOUR NEW FILES brought by change 0333 — the
# same new-test-file + integration-SHARD case as the gitcli block above, for internal/githubcli.
# Change 0333 partitions githubcli's fake-gh subprocess corpus behind the `integration` build
# tag (internal/githubcli/*_integration_test.go; completeness proven by
# tests/test_go_integration_contract.sh); each runner drives one shard through the shared
# executor tests/lib/go-integration-shard.sh. No existing shard could take them: every other
# Go-touching row here runs the DEFAULT-tag corpus, which by construction cannot see this tagged
# one. githubcli's tests re-exec the test binary as a fake gh, so the cost is in the -race lane
# (a race-instrumented re-exec is slow to start): the partition drops the default `go test -race`
# cost of internal/githubcli from ~129s to a fast fake-free corpus, while the shards run NORMAL
# mode — no test exercises real concurrent adapter calls, so none earns a -race shard. The
# partition also drops the default (non-race) internal/githubcli cost from ~2.9s to ~0.35s (the
# 19 pure decode/version/field-set/import-boundary tests that stay untagged). Sized per this
# header's rule from the WORST of three standalone serial readings, one session, one machine
# (darwin/arm64, go1.26.5), as
# `/usr/bin/time -p bash tests/test_go_integration_githubcli_<feature>.sh >/dev/null`:
#   ensure    3.23 2.95 2.87 -> worst 3.23 -> next multiple of 5 is 5, plus 5 -> 10
#   merge     0.90 1.01 0.90 -> worst 1.01 -> next multiple of 5 is 5, plus 5 -> 10
#   probe     0.79 0.77 0.75 -> worst 0.79 -> next multiple of 5 is 5, plus 5 -> 10
#   harness   1.05 0.98 1.02 -> worst 1.05 -> next multiple of 5 is 5, plus 5 -> 10
# All four ride the parallel lane (each is a single-package normal-mode go-test invocation).
# EXPECTED_TOTAL moves 2420 -> 2460: +10 +10 +10 +10.
#
# tests/test_go_integration_app_*.sh are EIGHT NEW FILES brought by change 0333 — the same
# new-test-file + integration-SHARD case as the gitcli/githubcli blocks above, for the dominant
# package internal/app. Change 0333 partitions internal/app's slow real-git and process-lifecycle
# corpus behind the `integration` build tag (internal/app/*_integration_test.go plus one
# *_race_integration_test.go; completeness proven by tests/test_go_integration_contract.sh); each
# runner drives one shard through the shared executor tests/lib/go-integration-shard.sh. The
# finalize_e2e corpus (`e2e` tag) is untouched. No existing shard could take them: every other
# Go-touching row here runs the DEFAULT-tag corpus, which by construction cannot see this tagged
# one. The partition drops the default `go test ./internal/app/` cost from ~228s to ~1.8s wall
# (0.6s of tests) — the number that unblocks change 0357 — and pulls internal/app's ~226s real-git
# tail out of the whole-module -race gate. Seven runners ride NORMAL mode; ONE (concurrency) rides
# RACE mode — the six goroutine-bearing tests (concurrent planning mutations and the gate-retry
# O_EXCL CAS) each carry an adjacent rationale in app_concurrency_race_integration_test.go; no
# other app test exercises real concurrent behavior, so none earns a -race shard. All eight ride
# the parallel lane (each is a single-package go-test invocation, not a whole-module -race run).
# Sized per this header's rule from the WORST of the standalone serial readings, one machine
# (darwin/arm64, go1.26.5), as `/usr/bin/time -p bash tests/test_go_integration_app_<feature>.sh`:
#   change      46.57 47.14 47.20 -> worst 47.20 -> next multiple of 5 is 50, plus 5 -> 55
#   cleanup     30.36 30.59 29.99 -> worst 30.59 -> next multiple of 5 is 35, plus 5 -> 40
#   closeout    29.54 30.09 30.07 -> worst 30.09 -> next multiple of 5 is 35, plus 5 -> 40
#   state       40.41 39.77       -> worst 40.41 -> next multiple of 5 is 45, plus 5 -> 50
#   workflow    37.74 38.22       -> worst 38.22 -> next multiple of 5 is 40, plus 5 -> 45
#   merge       21.39 21.07       -> worst 21.39 -> next multiple of 5 is 25, plus 5 -> 30
#   rebase      18.81 18.71       -> worst 18.81 -> next multiple of 5 is 20, plus 5 -> 25
#   concurrency 17.73 15.59 15.47 -> worst 17.73 -> next multiple of 5 is 20, plus 5 -> 25 (race mode, measured WITH -race)
# EXPECTED_TOTAL moves 2460 -> 2770: +55 +40 +40 +50 +45 +30 +25 +25.
#
# tests/test_go_integration_contract.sh is ONE NEW FILE brought by change 0333 — the fail-closed
# completeness contract over the whole Go integration partition (the sixteen shard runners above).
# It is a new surface, not an extension of any shard: the shard runners each execute ONE feature's
# tagged tests, while this file proves the partition is total — every tagged test lands in exactly
# one runner at the correct race mode, no tagged test leaks into the default corpus, no runner is a
# stale no-op, and `go vet -tags integration` passes. Its cost is dominated by the tagged-corpus
# `go test -list` per package and the three-package `go vet -tags integration` compile; it runs no
# test binary. Sized per this header's rule from the WORST reading, one machine (darwin/arm64,
# go1.26.5), as `/usr/bin/time -p bash tests/test_go_integration_contract.sh >/dev/null`:
#   contract   warm 4.17 4.17 3.96, cold-cache 9.74 -> worst 9.74 -> next multiple of 5 is 10, plus 5 -> 15
# The cold-cache reading (GOCACHE/GOMODCACHE pointed at empty scratch dirs, the drift-test
# methodology) is the reachable worst — the tags compile is the cold-sensitive term, warm in the
# suite (the Go-touching shards share GOCACHE). Rides the parallel lane. EXPECTED_TOTAL moves
# 2770 -> 2785: +15.
tests/test_adr_checks.sh	10	parallel
tests/test_artifact_backlink_coverage.sh	10	parallel
tests/test_assert_hygiene.sh	10	parallel
tests/test_asset_bundle_drift.sh	10	parallel
tests/test_auto_groom.sh	10	parallel
tests/test_backfill_change_types.sh	10	parallel
tests/test_bash_runtime_install.sh	10	parallel
tests/test_bash_runtime_routing.sh	10	parallel
tests/test_board_checks.sh	55	parallel
tests/test_board_checks_stack.sh	10	parallel
tests/test_board_refresh.sh	10	parallel
tests/test_board_refresh_on_transition.sh	10	parallel
tests/test_branch_reconstruction_guard.sh	10	parallel
tests/test_bsd_tool_defaults.sh	10	parallel
tests/test_change_links_coverage.sh	10	parallel
tests/test_change_types.sh	10	parallel
tests/test_closeout.sh	20	parallel
tests/test_codex_runbook.sh	10	parallel
tests/test_comment_anchor_style.sh	10	parallel
tests/test_composition_wiring.sh	10	parallel
tests/test_config_read_channel.sh	10	parallel
tests/test_configured_bash_finalize.sh	10	parallel
tests/test_consultant_brainstorm.sh	10	parallel
tests/test_consuming_repo_scripts.sh	10	parallel
tests/test_convention_extraction.sh	10	parallel
tests/test_critic_return_channel.sh	10	parallel
tests/test_cursor_contract_docs.sh	10	parallel
tests/test_cursor_dispatch_rule.sh	10	parallel
tests/test_cursor_permissions_docs.sh	10	parallel
tests/test_dispatch_block_budget.sh	10	parallel
tests/test_dispatch_capability.sh	10	parallel
tests/test_docket_build.sh	10	parallel
tests/test_docket_config.sh	45	parallel
tests/test_docket_config_guards.sh	35	parallel
tests/test_docket_example_yml.sh	15	parallel
tests/test_docket_facade.sh	10	parallel
tests/test_docket_frontmatter.sh	10	parallel
tests/test_docket_gitignore_block.sh	10	parallel
tests/test_docket_liveness.sh	10	parallel
tests/test_docket_metadata_branch.sh	10	parallel
tests/test_docket_preflight.sh	10	parallel
tests/test_docket_review.sh	10	parallel
tests/test_docket_root.sh	10	parallel
tests/test_docket_runtime_lib.sh	10	parallel
tests/test_docket_stack.sh	10	parallel
tests/test_docket_status.sh	60	parallel
tests/test_docket_status_stack.sh	10	parallel
tests/test_dummy_mode.sh	10	parallel
tests/test_ensure_claude_settings.sh	10	parallel
tests/test_ensure_docket_env.sh	10	parallel
tests/test_ensure_global_config.sh	10	parallel
tests/test_finalize_closeout_notes.sh	10	parallel
tests/test_finalize_disposition.sh	10	parallel
tests/test_finalize_gate.sh	10	parallel
tests/test_frontmatter_read_shapes.sh	10	parallel
tests/test_gate_caller_loop.sh	15	parallel
tests/test_gate_driver_boundary.sh	10	parallel
tests/test_gate_driver_boundary_mutation.sh	10	parallel
tests/test_gate_execution_posture.sh	10	parallel
tests/test_gate_facade.sh	15	parallel
tests/test_github_mirror.sh	10	parallel
tests/test_go_finalize_e2e.sh	25	parallel
tests/test_go_integration_app_change.sh	55	parallel
tests/test_go_integration_app_cleanup.sh	40	parallel
tests/test_go_integration_app_closeout.sh	40	parallel
tests/test_go_integration_app_concurrency.sh	25	parallel
tests/test_go_integration_app_merge.sh	30	parallel
tests/test_go_integration_app_rebase.sh	25	parallel
tests/test_go_integration_app_state.sh	50	parallel
tests/test_go_integration_app_workflow.sh	45	parallel
tests/test_go_integration_contract.sh	15	parallel
tests/test_go_integration_gitcli_concurrency.sh	10	parallel
tests/test_go_integration_gitcli_process.sh	15	parallel
tests/test_go_integration_gitcli_repo.sh	30	parallel
tests/test_go_integration_gitcli_source.sh	10	parallel
tests/test_go_integration_githubcli_ensure.sh	10	parallel
tests/test_go_integration_githubcli_harness.sh	10	parallel
tests/test_go_integration_githubcli_merge.sh	10	parallel
tests/test_go_integration_githubcli_probe.sh	10	parallel
tests/test_go_toolchain.sh	55	parallel
tests/test_go_race.sh	60	parallel
tests/test_grep_portability.sh	10	parallel
tests/test_groom_recap.sh	10	parallel
tests/test_harness_defaults.sh	45	parallel
tests/test_harness_defaults_flow_map.sh	40	parallel
tests/test_harness_defaults_validator.sh	50	parallel
tests/test_inline_role_stop_scoping.sh	10	parallel
tests/test_install_bootstrap.sh	10	parallel
tests/test_learnings_ledger.sh	10	parallel
tests/test_link_skills.sh	10	parallel
tests/test_loop_continuation.sh	10	parallel
tests/test_mark_publish_deferred.sh	10	parallel
tests/test_metadata_worktree_hooks.sh	10	parallel
tests/test_mint_stub.sh	15	parallel
tests/test_pipe_shapes.sh	10	parallel
tests/test_plan_writer_agent.sh	10	parallel
tests/test_plan_writer_step4.sh	10	parallel
tests/test_readme_finalize_docs.sh	10	parallel
tests/test_readme_skill_catalog.sh	10	parallel
tests/test_reclaim_claims.sh	10	parallel
tests/test_render_adr_index.sh	10	parallel
tests/test_render_artifact_backlink.sh	10	parallel
tests/test_render_board.sh	25	parallel
tests/test_render_change_links.sh	10	parallel
tests/test_render_learnings_index.sh	10	parallel
tests/test_results_artifact.sh	10	parallel
tests/test_role_skill_self_description.sh	10	parallel
tests/test_run_tests.sh	30	parallel
tests/test_run_tests_budget_state.sh	30	parallel
tests/test_runner_cursor.sh	10	parallel
tests/test_runner_dispatch.sh	20	parallel
tests/test_runner_dispatch_build_gate.sh	10	parallel
tests/test_runner_dispatch_detach.sh	15	parallel
tests/test_runner_dispatch_observe.sh	30	parallel
tests/test_runner_opencode.sh	10	parallel
tests/test_runtime_budgets.sh	10	parallel
tests/test_script_contracts_coverage.sh	10	parallel
tests/test_shared_worktree_commit_scope.sh	10	parallel
tests/test_skill_facade_wiring.sh	10	parallel
tests/test_skill_fork_dispatch.sh	10	parallel
tests/test_skill_handoff_precedence.sh	10	parallel
tests/test_skill_size_budgets.sh	10	parallel
tests/test_skip_allowlist_invisibility.sh	10	parallel
tests/test_stack_closeout.sh	15	parallel
tests/test_sync_agents.sh	50	parallel
tests/test_sync_agents_claude_surface.sh	45	parallel
tests/test_sync_agents_codex.sh	30	parallel
tests/test_sync_agents_codex_dispatch.sh	50	parallel
tests/test_sync_agents_cursor.sh	15	parallel
tests/test_sync_agents_defaults.sh	50	parallel
tests/test_sync_agents_drift_docs.sh	55	parallel
tests/test_sync_agents_harness_gaps.sh	20	parallel
tests/test_sync_agents_opencode.sh	10	parallel
tests/test_sync_agents_recursion_guard.sh	10	parallel
tests/test_sync_agents_run_gate.sh	10	parallel
tests/test_sync_agents_runners.sh	45	parallel
tests/test_sync_agents_runners_gates.sh	35	parallel
tests/test_sync_agents_runners_pins.sh	40	parallel
tests/test_sync_agents_surface_containment.sh	20	parallel
tests/test_sync_agents_validator.sh	15	parallel
tests/test_sync_integration_branch.sh	10	parallel
tests/test_terminal_publish.sh	10	parallel
tests/test_typed_changes_docs.sh	10	parallel
tests/test_verify_run.sh	10	parallel
tests/test_worktree_hooks_wiring.sh	10	parallel
